# 디자인 캔버스 원본

2026-08-22 의 UI 리디자인에 쓴 목업이다. 열 화면 + 방향 참고 셋.
발행본: https://claude.ai/code/artifact/daaa8d16-8f3c-48f0-9b6c-419737f522e2

`.dc.html` 하나가 아트보드 하나이고, `canvas.json` 이 배치와 페이지를 정한다.
빌드 산출물(`*.html`)은 `.gitignore` 에서 뺐다 — 에디터가 통째로 박혀 있어 2.3MB 다.

**색은 `design-system/tokens.css` 값을 그대로 쓴다.** 목업이 토큰을 따르지
토큰이 목업을 따르지 않는다 — 캔버스에 14.5 · 15.5px 같은 반 단계를 썼다가
토큰 스케일에 맞춰 되돌린 적이 있다.

다시 그리려면 여기서 시작한다. 시드 방법은 `/design` 스킬이 안다.
