{
  "name": "onework-mobile",
  "version": "1.0.0",
  "description": "ONE 워크스페이스 모바일 앱 (Capacitor — Android/iOS)",
  "license": "UNLICENSED",
  "scripts": {
    "sync:web": "node copy-web.js",
    "sync": "npm run sync:web && cap sync",
    "android:open": "npm run sync && cap open android",
    "ios:open": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
