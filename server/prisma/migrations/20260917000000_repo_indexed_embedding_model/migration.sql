-- 인덱스를 만든 임베딩 모델 기록 (CLAUDE.md §5 알려진 빚).
--
-- 모델을 바꾼 뒤 push 만 하면 증분이 돌아 옛 벡터와 새 벡터가 섞이고, 오류 없이
-- 순위만 틀렸다. 이 값이 지금 설정과 다르면 전체 재인덱싱으로 떨어진다.
--
-- **기존 행은 NULL 로 둔다** — 어떤 모델로 만들었는지 모르므로, 다음 인덱싱이
-- 한 번 전체로 돈다. 짐작해서 채우면 틀린 짐작이 영영 증분을 허락한다.
--
-- 손으로 썼다. migrate diff 가 HNSW 인덱스 DROP 을 끼워 넣는 일을 피하려는 것이다.
ALTER TABLE "repos" ADD COLUMN "indexed_embedding_model" TEXT;
