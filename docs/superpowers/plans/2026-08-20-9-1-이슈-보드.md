# 9-1 이슈 보드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스페이스마다 칸반 보드가 생긴다 — 이슈를 만들고, 컬럼 사이로 옮기고, 밖에서 생긴 변경이 실시간으로 뜬다.

**Architecture:** 서버는 `issues/` 모듈 하나를 새로 쓴다(옛 `src/issues` 는 지운다). 모든 라우트가 `/api/spaces/:spaceId/issues` 아래에서 `SpaceGuard` 를 지나고, 서비스 메서드는 `spaceId` 를 첫 인자로 받는다. 변경은 `RealtimeEmitter.toSpace()` 로 스페이스 룸에 쏜다. 앱은 대화와 같은 규칙을 쓴다 — REST 와 소켓이 drift 캐시를 갱신하고 **화면은 drift 만 구독한다.** 쓰기는 온라인 전용이고 낙관적으로 그린 뒤 실패하면 되돌린다.

**Tech Stack:** NestJS 11 · Prisma 5.22(Postgres 18) · Socket.IO · Flutter 3.47 · Riverpod 3 · drift 2.34 · freezed

**Spec:** [docs/superpowers/specs/2026-08-20-이슈-보드-design.md](../specs/2026-08-20-이슈-보드-design.md)

## Global Constraints

- **주석 · 커밋 메시지 · 문서는 전부 한국어.** 커밋에 `Co-Authored-By: Claude` 를 넣지 않는다
- **테넌트 격리**: 스페이스 소속 테이블은 `spaceId` 를 직접 갖고, 모든 쿼리 `WHERE` 에 넣는다. 서비스 메서드는 `spaceId` 를 첫 인자로 받는다. `:spaceId` 라우트에는 `SpaceGuard` 를 건다. **볼 수 없으면 403 이 아니라 404**
- **입력은 전부 class-validator DTO.** 전역 파이프가 `whitelist: true, forbidNonWhitelisted: true` 라 DTO 밖 필드는 400 이다
- **raw SQL 에서 id 를 `::uuid` 로 캐스팅하지 않는다.** Prisma 의 `String @id` 는 `text` 컬럼이다
- **`prisma migrate dev` 는 이 환경에서 못 쓴다**(비대화형 + pgvector HNSW 드리프트). `prisma migrate diff` 로 SQL 을 만들어 **읽고 손질한 뒤** `prisma:deploy`
- **자동 생성된 마이그레이션 SQL 을 그대로 믿지 않는다.** HNSW 인덱스를 지우는 구문이 섞인 적이 7-1 · 7-3 두 번 있었다
- **앱 모델을 고치면 `dart run build_runner build`** 를 돌리고 `.freezed.dart` · `.g.dart` 를 커밋한다
- **drift 캐시 테이블을 더하면 `schemaVersion` 을 올린다.** 빠뜨리면 `no such table` 로 앱이 멈춘다
- **`@DriftDatabase` 애노테이션과 클래스 사이에 아무것도 끼우지 않는다**
- 검증 명령: `npm --prefix server run typecheck` · `npm run server:test` · `npm run server:lint` · `cd app && flutter analyze` · `flutter test`

---

## 파일 구조

**서버 — 새로 만드는 것**

| 파일 | 책임 |
|---|---|
| `server/src/issues/issue-key.ts` | slug → 이슈 키 접두사. 순수 함수 |
| `server/src/issues/issue-key.spec.ts` | 위 테스트 |
| `server/src/issues/position.ts` | 칸반 정렬 키 계산. 순수 함수 |
| `server/src/issues/position.spec.ts` | 위 테스트 |
| `server/src/issues/issue-status.ts` | `closedAt` 전이 규칙. 순수 함수 |
| `server/src/issues/issue-status.spec.ts` | 위 테스트 |
| `server/src/issues/issues.service.ts` | 목록 · 생성 · 조회 · 수정 · 이동 |
| `server/src/issues/issues.controller.ts` | `/api/spaces/:spaceId/issues` |
| `server/src/issues/issues.module.ts` | 모듈 등록 |
| `server/src/issues/dto/list-issues.dto.ts` | 목록 필터 |
| `server/src/issues/dto/create-issue.dto.ts` | 생성 |
| `server/src/issues/dto/update-issue.dto.ts` | 수정 |
| `server/src/issues/dto/move-issue.dto.ts` | 자리 이동 |
| `server/scripts/check-issues.mjs` | 실서버 · 실DB · 실소켓 계약 검증 |

**서버 — 고치는 것**

| 파일 | 무엇 |
|---|---|
| `server/prisma/schema.prisma` | `Space.issueSeq` · `Issue.reporterId` |
| `server/prisma/migrations/<새 폴더>/migration.sql` | 위 두 컬럼 |
| `server/src/app.module.ts` | `IssuesModule` 등록 |
| `server/tsconfig.json` · `tsconfig.build.json` | `exclude` 에서 `src/issues` 제거 |
| `package.json` (루트) | `check:issues` 스크립트 |

**서버 — 지우는 것**: `src/issues/` 의 `issue-provider.interface.ts` · `native-issue.provider.ts` · 옛 `issues.service.ts` · `issues.controller.ts` · `issues.module.ts` · `dto/*`

**앱 — 새로 만드는 것**

| 파일 | 책임 |
|---|---|
| `app/lib/domain/models/issue.dart` | freezed 모델 + enum |
| `app/lib/data/api/issues_api.dart` | 목록 · 생성 · 수정 · 이동 |
| `app/lib/data/repositories/issue_repository.dart` | REST · 소켓 → 캐시 |
| `app/lib/features/issue/board_controller.dart` | 상태별 묶기 · 낙관적 이동 |
| `app/lib/features/issue/board_screen.dart` | 4열 칸반 |
| `app/lib/features/issue/issue_card.dart` | 카드 + 상태 이동 메뉴 |
| `app/lib/features/issue/new_issue_sheet.dart` | 생성 시트 |
| `app/test/issue_test.dart` | 인메모리 drift 로 캐시 왕복 |

**앱 — 고치는 것**: `data/local/app_database.dart`(`CachedIssues` + `schemaVersion` 13) · `core/router.dart` · `features/shell/app_shell.dart` · `features/shell/channel_pane.dart` · `features/realtime/socket_controller.dart`

---

## Task 1: 스키마 — 채번 카운터와 작성자

**Files:**
- Modify: `server/prisma/schema.prisma`
- Create: `server/prisma/migrations/20260820000000_issue_seq_and_reporter/migration.sql`

**Interfaces:**
- Produces: `spaces.issue_seq int not null default 0` · `issues.reporter_id text null` (FK → `users.id`, `ON DELETE SET NULL`)

- [ ] **Step 1: 스키마에 컬럼 두 개를 더한다**

`model Space` 안, `storageQuotaBytes` 아래:

```prisma
  /// 이슈 키(NEXUS-138)의 연번. 생성 트랜잭션에서 UPDATE ... RETURNING 으로
  /// 원자적으로 올린다. count() 로 세면 동시 생성이 같은 키를 받고, 이슈를
  /// 지우면 번호가 되돌아간다 — 연번은 세는 것이 아니라 발급하는 것이다.
  issueSeq Int @default(0) @map("issue_seq")
```

`model Issue` 안, `assigneeId` 아래:

```prisma
  /// 이슈를 만든 사람. 담당자(assigneeId)와 다르다. 저장소 연동이 밀어 넣는
  /// 이슈에는 이 스페이스의 작성자가 없어 null 을 허용한다.
  reporterId  String?       @map("reporter_id")
```

`model Issue` 의 relation 블록에 `assignee` 아래 한 줄:

```prisma
  reporter      User?            @relation("IssueReporter", fields: [reporterId], references: [id], onDelete: SetNull)
```

`model User` 의 relation 목록, 기존 `IssueAssignee` 줄 옆에 한 줄:

```prisma
  reportedIssues   Issue[]          @relation("IssueReporter")
```

- [ ] **Step 2: 마이그레이션 SQL 을 만든다**

```bash
cd server && npx prisma migrate diff --from-schema-datasource prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma --script > ../scratch-issue-seq.sql
```

- [ ] **Step 3: 만들어진 SQL 을 읽는다 — 이 단계를 건너뛰지 않는다**

`cat scratch-issue-seq.sql` 로 전문을 확인한다. **`DROP INDEX` 가 한 줄이라도 있으면 지운다.** Prisma 는 pgvector HNSW 인덱스(`repo_index_chunks.embedding`)를 표현하지 못해 드리프트로 오인하고 삭제 구문을 끼워 넣는다 — 7-1 과 7-3 에서 두 번 겪었다.

기대하는 내용은 이 셋뿐이다:

```sql
ALTER TABLE "spaces" ADD COLUMN "issue_seq" INTEGER NOT NULL DEFAULT 0;
ALTER TABLE "issues" ADD COLUMN "reporter_id" TEXT;
ALTER TABLE "issues" ADD CONSTRAINT "issues_reporter_id_fkey" FOREIGN KEY ("reporter_id") REFERENCES "users"("id") ON DELETE SET NULL ON UPDATE CASCADE;
```

- [ ] **Step 4: 마이그레이션 폴더에 손질한 SQL 을 넣는다**

```bash
mkdir -p server/prisma/migrations/20260820000000_issue_seq_and_reporter && mv scratch-issue-seq.sql server/prisma/migrations/20260820000000_issue_seq_and_reporter/migration.sql
```

- [ ] **Step 5: 적용하고 클라이언트를 다시 만든다**

```bash
npm run db:up && npm --prefix server run prisma:deploy && npm --prefix server run prisma:generate
```

기대: `All migrations have been successfully applied.` · `Generated Prisma Client`

- [ ] **Step 6: 컬럼이 실제로 생겼는지 DB 로 확인한다**

```bash
cd server && node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.$queryRaw`select table_name, column_name from information_schema.columns where column_name in ('issue_seq','reporter_id')`.then(r=>{console.log(r);return p.$disconnect()})"
```

기대: `spaces/issue_seq` 와 `issues/reporter_id` 두 행.

- [ ] **Step 7: 커밋**

```bash
git add server/prisma && git commit -m "feat(server): 이슈 채번 카운터와 작성자 컬럼"
```

---

## Task 2: 이슈 키 접두사 — 순수 함수

**Files:**
- Create: `server/src/issues/issue-key.ts`
- Test: `server/src/issues/issue-key.spec.ts`

**Interfaces:**
- Produces: `issueKeyPrefix(slug: string): string` · `formatIssueKey(slug: string, seq: number): string`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/issues/issue-key.spec.ts`:

```ts
import { formatIssueKey, issueKeyPrefix } from './issue-key';

/**
 * 이슈 키는 사람이 대화에서 부르는 이름이다("NEXUS-12 고쳤어").
 * 스페이스 slug 는 불변이므로 한 스페이스 안에서 접두사가 섞이지 않는다.
 */
describe('issueKeyPrefix', () => {
  it('한 마디 slug 는 그대로 대문자가 된다', () => {
    expect(issueKeyPrefix('nexus')).toBe('NEXUS');
  });

  it('첫 마디가 짧으면 다음 마디를 이어 붙인다', () => {
    // 'my' 만으로는 무엇의 키인지 읽히지 않는다.
    expect(issueKeyPrefix('my-cool-project')).toBe('MYCOOL');
  });

  it('6자를 넘지 않는다', () => {
    expect(issueKeyPrefix('supercalifragilistic')).toBe('SUPERC');
  });

  it('한글 이름의 폴백 slug 도 읽을 수 있는 접두사가 된다', () => {
    // slugify() 는 한글 이름에서 'space-a1b2c3d4' 같은 폴백을 만든다.
    expect(issueKeyPrefix('space-a1b2c3d4')).toBe('SPACE');
  });

  it('영숫자가 하나도 없으면 ISSUE 로 떨어진다', () => {
    expect(issueKeyPrefix('---')).toBe('ISSUE');
    expect(issueKeyPrefix('')).toBe('ISSUE');
  });
});

describe('formatIssueKey', () => {
  it('접두사와 연번을 붙인다', () => {
    expect(formatIssueKey('nexus', 12)).toBe('NEXUS-12');
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npm --prefix server test -- issue-key`
기대: FAIL — `Cannot find module './issue-key'`

- [ ] **Step 3: 최소 구현**

`server/src/issues/issue-key.ts`:

```ts
/**
 * 스페이스 slug 에서 이슈 키 접두사를 만든다 (NEXUS-138 의 'NEXUS').
 *
 * slug 에서 뽑는 이유는 `UpdateSpaceDto` 에 slug 가 없어 **불변**이기 때문이다 —
 * 한 스페이스 안에서 접두사가 섞이지 않는다. 사람이 접두사를 고르는 UI 가
 * 필요해지면 `Space.issueKeyPrefix` 컬럼으로 승격한다.
 */
export function issueKeyPrefix(slug: string): string {
  const parts = slug.toUpperCase().split(/[^A-Z0-9]+/).filter(Boolean);

  // 첫 마디가 두 글자 이하면 무엇의 키인지 읽히지 않아 다음 마디를 잇는다.
  let prefix = '';
  for (const part of parts) {
    prefix += part;
    if (prefix.length >= 3) break;
  }

  return prefix.slice(0, 6) || 'ISSUE';
}

/** `NEXUS-12`. 연번은 Space.issueSeq 가 발급한다. */
export function formatIssueKey(slug: string, seq: number): string {
  return `${issueKeyPrefix(slug)}-${seq}`;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npm --prefix server test -- issue-key`
기대: PASS 6개

- [ ] **Step 5: 커밋**

```bash
git add server/src/issues/issue-key.ts server/src/issues/issue-key.spec.ts && git commit -m "feat(server): 이슈 키 접두사를 slug 에서 만든다"
```

---

## Task 3: 정렬 키 — 순수 함수

**Files:**
- Create: `server/src/issues/position.ts`
- Test: `server/src/issues/position.spec.ts`

**Interfaces:**
- Consumes: `Prisma.Decimal` (`@prisma/client`)
- Produces: `POSITION_STEP: Prisma.Decimal` · `positionBetween(prev: Prisma.Decimal | null, next: Prisma.Decimal | null): Prisma.Decimal` · `needsRenumber(value: Prisma.Decimal, prev: Prisma.Decimal | null, next: Prisma.Decimal | null): boolean`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/issues/position.spec.ts`:

```ts
import { Prisma } from '@prisma/client';
import { needsRenumber, positionBetween, POSITION_STEP } from './position';

const d = (n: string | number) => new Prisma.Decimal(n);

/**
 * 칸반은 사이 삽입이 기본 동작이라, 두 카드 사이에 넣을 때 중간값을 줘서
 * 재정렬 쓰기를 1건으로 끝낸다 (docs/백엔드-설계.md §3).
 */
describe('positionBetween', () => {
  it('두 카드 사이는 중간값이다', () => {
    expect(positionBetween(d(0), d(1000)).toString()).toBe('500');
  });

  it('위 카드만 주면 그 아래로 한 칸 내려간다', () => {
    expect(positionBetween(d(1000), null).toString()).toBe('2000');
  });

  it('아래 카드만 주면 그 위로 한 칸 올라간다', () => {
    expect(positionBetween(null, d(1000)).toString()).toBe('0');
  });

  it('빈 컬럼이면 0 이다', () => {
    expect(positionBetween(null, null).toString()).toBe('0');
  });

  it('음수 자리도 만든다 — 맨 위로 계속 올릴 수 있어야 한다', () => {
    expect(positionBetween(null, d(0)).toString()).toBe('-1000');
  });

  it('결과는 언제나 두 이웃 사이에 엄격히 들어간다', () => {
    let prev = d(0);
    const next = d(1);
    for (let i = 0; i < 10; i++) {
      const mid = positionBetween(prev, next);
      expect(mid.greaterThan(prev)).toBe(true);
      expect(mid.lessThan(next)).toBe(true);
      prev = mid;
    }
  });
});

/**
 * Decimal 의 유효 자릿수가 소진되면 중간값이 이웃과 같아진다. 그때는 그
 * 컬럼만 다시 채번해야 한다 — 조용히 같은 값을 쓰면 두 카드의 순서가
 * tie-break 로 넘어가 흔들린다(전송 큐에서 시각이 동률이던 것과 같다).
 */
describe('needsRenumber', () => {
  it('중간값이 이웃과 같아지면 참이다', () => {
    expect(needsRenumber(d(5), d(5), d(6))).toBe(true);
    expect(needsRenumber(d(6), d(5), d(6))).toBe(true);
  });

  it('사이에 들어가면 거짓이다', () => {
    expect(needsRenumber(d('5.5'), d(5), d(6))).toBe(false);
  });

  it('이웃이 없으면 거짓이다', () => {
    expect(needsRenumber(d(0), null, null)).toBe(false);
  });
});

describe('POSITION_STEP', () => {
  it('1000 이다 — 재채번 간격과 같아야 한다', () => {
    expect(POSITION_STEP.toString()).toBe('1000');
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npm --prefix server test -- position`
기대: FAIL — `Cannot find module './position'`

- [ ] **Step 3: 최소 구현**

`server/src/issues/position.ts`:

```ts
import { Prisma } from '@prisma/client';

/** 이웃이 하나뿐일 때 벌리는 간격. 재채번도 같은 간격을 쓴다. */
export const POSITION_STEP = new Prisma.Decimal(1000);

/**
 * 칸반 카드의 정렬 키를 정한다.
 *
 * `prev` 는 위 카드(작은 position), `next` 는 아래 카드(큰 position)다.
 * 둘 사이면 중간값 — 그래야 재정렬 쓰기가 옮긴 카드 1건으로 끝난다.
 * 카테고리가 쓰는 Int + "맨 뒤에 붙이기"와 다른 이유는 칸반이 사이 삽입을
 * 기본으로 하기 때문이다.
 */
export function positionBetween(
  prev: Prisma.Decimal | null,
  next: Prisma.Decimal | null,
): Prisma.Decimal {
  if (prev && next) return prev.plus(next).dividedBy(2);
  if (prev) return prev.plus(POSITION_STEP);
  if (next) return next.minus(POSITION_STEP);
  return new Prisma.Decimal(0);
}

/**
 * 자릿수가 소진돼 중간값이 이웃과 같아졌는지. 참이면 그 컬럼만 재채번한다.
 */
export function needsRenumber(
  value: Prisma.Decimal,
  prev: Prisma.Decimal | null,
  next: Prisma.Decimal | null,
): boolean {
  if (prev?.equals(value)) return true;
  if (next?.equals(value)) return true;
  return false;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npm --prefix server test -- position`
기대: PASS 10개

- [ ] **Step 5: 커밋**

```bash
git add server/src/issues/position.ts server/src/issues/position.spec.ts && git commit -m "feat(server): 칸반 정렬 키를 중간값으로 정한다"
```

---

## Task 4: `closedAt` 전이 규칙 — 순수 함수

**Files:**
- Create: `server/src/issues/issue-status.ts`
- Test: `server/src/issues/issue-status.spec.ts`

**Interfaces:**
- Produces: `resolveClosedAt(prev: IssueStatus, next: IssueStatus | undefined, now: Date): Date | null | undefined` — `undefined` 는 "건드리지 않는다", `null` 은 "비운다"

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/issues/issue-status.spec.ts`:

```ts
import { IssueStatus } from '@prisma/client';
import { resolveClosedAt } from './issue-status';

const NOW = new Date('2026-08-20T00:00:00.000Z');

/**
 * closedAt 은 번다운의 유일한 입력이다 — 상태 전이 이력 테이블을 두지 않기로
 * 했으므로(설계 §3), 상태를 바꾸는 모든 경로가 같은 규칙을 타야 한다.
 */
describe('resolveClosedAt', () => {
  it('done 이 되면 지금 시각을 찍는다', () => {
    expect(resolveClosedAt(IssueStatus.doing, IssueStatus.done, NOW)).toEqual(NOW);
  });

  it('done 에서 벗어나면 비운다', () => {
    expect(resolveClosedAt(IssueStatus.done, IssueStatus.doing, NOW)).toBeNull();
  });

  it('done 안에서 자리만 바뀌면 건드리지 않는다', () => {
    // 다시 찍으면 닫힌 시각이 밀려 번다운이 틀어진다.
    expect(resolveClosedAt(IssueStatus.done, IssueStatus.done, NOW)).toBeUndefined();
  });

  it('done 과 무관한 전이는 건드리지 않는다', () => {
    expect(resolveClosedAt(IssueStatus.backlog, IssueStatus.doing, NOW)).toBeUndefined();
  });

  it('상태를 바꾸지 않으면 건드리지 않는다', () => {
    expect(resolveClosedAt(IssueStatus.done, undefined, NOW)).toBeUndefined();
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npm --prefix server test -- issue-status`
기대: FAIL — `Cannot find module './issue-status'`

- [ ] **Step 3: 최소 구현**

`server/src/issues/issue-status.ts`:

```ts
import { IssueStatus } from '@prisma/client';

/**
 * 상태 전이가 `closedAt` 을 정한다. 클라이언트가 보내는 값은 받지 않는다 —
 * 사용자가 고칠 수 있으면 번다운이 사실이 아니게 된다.
 *
 * 반환값의 `undefined` 는 "건드리지 않는다"이고 `null` 은 "비운다"이다.
 * 이 둘을 구분해야 done 안에서 자리만 옮길 때 닫힌 시각이 밀리지 않는다.
 */
export function resolveClosedAt(
  prev: IssueStatus,
  next: IssueStatus | undefined,
  now: Date,
): Date | null | undefined {
  if (next === undefined || next === prev) return undefined;
  if (next === IssueStatus.done) return now;
  if (prev === IssueStatus.done) return null;
  return undefined;
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npm --prefix server test -- issue-status`
기대: PASS 5개

- [ ] **Step 5: 커밋**

```bash
git add server/src/issues/issue-status.ts server/src/issues/issue-status.spec.ts && git commit -m "feat(server): closedAt 을 상태 전이가 정한다"
```

---

## Task 5: 옛 issues 모듈을 지우고 DTO 를 새로 쓴다

**Files:**
- Delete: `server/src/issues/issue-provider.interface.ts` · `native-issue.provider.ts` · `issues.service.ts` · `issues.controller.ts` · `issues.module.ts` · `dto/create-issue.dto.ts` · `dto/update-issue.dto.ts` · `dto/list-issues.dto.ts` · `dto/create-comment.dto.ts`
- Create: `server/src/issues/dto/list-issues.dto.ts` · `create-issue.dto.ts` · `update-issue.dto.ts` · `move-issue.dto.ts`

**Interfaces:**
- Produces: `ListIssuesDto` · `CreateIssueDto` · `UpdateIssueDto` · `MoveIssueDto`

- [ ] **Step 1: 옛 소스를 지운다**

```bash
git rm server/src/issues/issue-provider.interface.ts server/src/issues/native-issue.provider.ts server/src/issues/issues.service.ts server/src/issues/issues.controller.ts server/src/issues/issues.module.ts server/src/issues/dto/create-issue.dto.ts server/src/issues/dto/update-issue.dto.ts server/src/issues/dto/list-issues.dto.ts server/src/issues/dto/create-comment.dto.ts
```

지우는 근거는 스펙 §1 이다 — 살아남는 것이 DTO 골격뿐이고, `IssueProvider` 추상화는 지금 계획에 없는 Redmine 프로바이더를 위한 것이라 남기면 오해를 함께 되살린다.

- [ ] **Step 2: 목록 필터 DTO**

`server/src/issues/dto/list-issues.dto.ts`:

```ts
import { IssueStatus } from '@prisma/client';
import { IsEnum, IsOptional, IsString, IsUUID, MaxLength } from 'class-validator';

export class ListIssuesDto {
  /** 주면 그 컬럼만. 안 주면 보드 전체. */
  @IsOptional()
  @IsEnum(IssueStatus)
  status?: IssueStatus;

  @IsOptional()
  @IsUUID()
  assigneeId?: string;

  @IsOptional()
  @IsUUID()
  sprintId?: string;

  /** 제목 부분 일치. 대소문자를 가리지 않는다. */
  @IsOptional()
  @IsString()
  @MaxLength(80)
  q?: string;
}
```

- [ ] **Step 3: 생성 DTO**

`server/src/issues/dto/create-issue.dto.ts`:

```ts
import { IssuePriority, IssueStatus } from '@prisma/client';
import {
  IsEnum,
  IsInt,
  IsOptional,
  IsString,
  IsUUID,
  Max,
  MaxLength,
  Min,
  MinLength,
} from 'class-validator';

export class CreateIssueDto {
  @IsString()
  @MinLength(1)
  @MaxLength(200)
  title!: string;

  @IsOptional()
  @IsString()
  @MaxLength(20000)
  description?: string;

  /** 비우면 backlog. */
  @IsOptional()
  @IsEnum(IssueStatus)
  status?: IssueStatus;

  /** 비우면 mid. */
  @IsOptional()
  @IsEnum(IssuePriority)
  priority?: IssuePriority;

  @IsOptional()
  @IsUUID()
  assigneeId?: string;

  @IsOptional()
  @IsUUID()
  sprintId?: string;

  /** 에픽. 한 단계만 허용한다 — 에픽의 에픽은 400 이다. */
  @IsOptional()
  @IsUUID()
  parentId?: string;

  @IsOptional()
  @IsInt()
  @Min(0)
  @Max(100)
  storyPoints?: number;
}
```

- [ ] **Step 4: 수정 DTO**

`server/src/issues/dto/update-issue.dto.ts`:

```ts
import { IssuePriority, IssueStatus } from '@prisma/client';
import {
  IsEnum,
  IsInt,
  IsOptional,
  IsString,
  IsUUID,
  Max,
  MaxLength,
  Min,
  MinLength,
} from 'class-validator';

/**
 * `closedAt` 은 여기 없다. 상태 전이가 정하는 값이라 사용자가 보내는 것을
 * 받지 않는다 — 고칠 수 있으면 번다운이 사실이 아니게 된다.
 */
export class UpdateIssueDto {
  @IsOptional()
  @IsString()
  @MinLength(1)
  @MaxLength(200)
  title?: string;

  @IsOptional()
  @IsString()
  @MaxLength(20000)
  description?: string;

  /** 컬럼만 옮긴다 — 대상 컬럼 맨 위로 간다. 자리까지 정하려면 PUT position. */
  @IsOptional()
  @IsEnum(IssueStatus)
  status?: IssueStatus;

  @IsOptional()
  @IsEnum(IssuePriority)
  priority?: IssuePriority;

  @IsOptional()
  @IsUUID()
  assigneeId?: string | null;

  @IsOptional()
  @IsUUID()
  sprintId?: string | null;

  @IsOptional()
  @IsInt()
  @Min(0)
  @Max(100)
  storyPoints?: number | null;
}
```

- [ ] **Step 5: 이동 DTO**

`server/src/issues/dto/move-issue.dto.ts`:

```ts
import { IssueStatus } from '@prisma/client';
import { IsEnum, IsOptional, IsUUID } from 'class-validator';

/**
 * 드래그가 쓴다. `afterId` 는 "이 카드 뒤에", `beforeId` 는 "이 카드 앞에"다.
 * 둘 다 없으면 그 컬럼 맨 위. 두 이웃은 모두 `status` 컬럼에 있어야 한다 —
 * 다른 컬럼의 카드를 기준으로 자리를 정하면 순서가 뜻을 잃는다.
 */
export class MoveIssueDto {
  @IsEnum(IssueStatus)
  status!: IssueStatus;

  @IsOptional()
  @IsUUID()
  afterId?: string;

  @IsOptional()
  @IsUUID()
  beforeId?: string;
}
```

- [ ] **Step 6: 커밋**

```bash
git add -A server/src/issues && git commit -m "refactor(server): 옛 issues 모듈을 지우고 DTO 를 다시 쓴다"
```

---

## Task 6: IssuesService — 목록과 생성

**Files:**
- Create: `server/src/issues/issues.service.ts`

**Interfaces:**
- Consumes: `formatIssueKey` (Task 2) · `positionBetween`, `POSITION_STEP` (Task 3) · `PrismaService` · `RealtimeEmitter`
- Produces: `IssuesService.list(spaceId, dto): Promise<{ issues: IssueView[]; truncated: IssueStatus[] }>` · `IssuesService.create(spaceId, reporterId, dto): Promise<IssueView>` · `IssueView` · `ISSUE_INCLUDE` · `toView` · `IssueRow`

- [ ] **Step 1: 서비스의 목록 · 생성 부분을 쓴다**

`server/src/issues/issues.service.ts`:

```ts
import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';
import { IssueStatus, Prisma } from '@prisma/client';
import { PrismaService } from '../prisma/prisma.service';
import { RealtimeEmitter } from '../realtime/realtime-emitter';
import { formatIssueKey } from './issue-key';
import { positionBetween } from './position';
import { CreateIssueDto } from './dto/create-issue.dto';
import { ListIssuesDto } from './dto/list-issues.dto';

/** 컬럼 하나가 한 번에 주는 최대 건수. 넘치면 truncated 로 알린다. */
const COLUMN_LIMIT = 200;

const ISSUE_INCLUDE = {
  assignee: { select: { id: true, name: true, avatarUrl: true } },
} satisfies Prisma.IssueInclude;

type IssueRow = Prisma.IssueGetPayload<{ include: typeof ISSUE_INCLUDE }>;

/**
 * `position` 은 Decimal 이라 JSON 으로 나가면 정밀도가 깎인다. 문자열로 준다 —
 * 앱은 정렬에만 쓰고 산술을 하지 않는다.
 */
function toView(row: IssueRow) {
  return { ...row, position: row.position.toString() };
}

type IssueView = ReturnType<typeof toView>;

/**
 * 이슈 보드. 스페이스에 속한다.
 *
 * 모든 메서드가 `spaceId` 를 첫 인자로 받아 WHERE 에 넣는다 — SpaceGuard 를
 * 지났더라도 다른 스페이스의 이슈 id 를 넘기면 404 여야 한다(설계 §2 규칙 2·4).
 */
@Injectable()
export class IssuesService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly realtime: RealtimeEmitter,
  ) {}

  /**
   * 목록은 평평하게 준다. 상태별로 묶지 않는 이유는 앱이 이것을 drift 에 행
   * 단위로 넣기 때문이다 — 서버가 묶어 주면 캐시에 넣을 때 다시 풀어야 한다.
   *
   * 커서를 두지 않고 **컬럼별 상한 + truncated** 로 간다. 정렬 키가
   * (status, position) 복합이라 커서는 컬럼마다 따로 이어야 하는데, Phase 0
   * 규모에서 그 복잡도를 살 이유가 없다. 조용히 자르지 않는 것이 핵심이다.
   */
  async list(spaceId: string, dto: ListIssuesDto) {
    const base: Prisma.IssueWhereInput = {
      spaceId,
      ...(dto.assigneeId ? { assigneeId: dto.assigneeId } : {}),
      ...(dto.sprintId ? { sprintId: dto.sprintId } : {}),
      ...(dto.q ? { title: { contains: dto.q, mode: 'insensitive' as const } } : {}),
    };

    const columns = dto.status ? [dto.status] : Object.values(IssueStatus);
    const issues: IssueRow[] = [];
    const truncated: IssueStatus[] = [];

    for (const status of columns) {
      // 상한보다 한 건 더 받아 "잘렸는지"를 개수로 안다.
      const rows = await this.prisma.issue.findMany({
        where: { ...base, status },
        orderBy: [{ position: 'asc' }, { createdAt: 'asc' }],
        take: COLUMN_LIMIT + 1,
        include: ISSUE_INCLUDE,
      });
      if (rows.length > COLUMN_LIMIT) {
        truncated.push(status);
        rows.length = COLUMN_LIMIT;
      }
      issues.push(...rows);
    }

    return { issues: issues.map(toView), truncated };
  }

  /**
   * 새 이슈는 컬럼 **맨 위**에 놓는다. 방금 만든 것이 보이지 않으면 만든
   * 사람은 실패했다고 믿는다.
   *
   * 키 발급과 행 삽입은 한 트랜잭션이다. 사이에서 끊기면 연번만 올라가 번호에
   * 구멍이 남는데, 구멍은 견딜 수 있어도 중복은 못 견딘다.
   */
  async create(spaceId: string, reporterId: string, dto: CreateIssueDto) {
    if (dto.parentId) await this.requireEpicCandidate(spaceId, dto.parentId);

    const status = dto.status ?? IssueStatus.backlog;

    const created = await this.prisma.$transaction(async (tx) => {
      // 스페이스 행을 잠가 동시 생성을 직렬화한다.
      const [seqRow] = await tx.$queryRaw<{ issue_seq: number; slug: string }[]>`
        UPDATE spaces SET issue_seq = issue_seq + 1
        WHERE id = ${spaceId}
        RETURNING issue_seq, slug
      `;
      if (!seqRow) throw new NotFoundException('스페이스를 찾을 수 없습니다');

      const top = await tx.issue.findFirst({
        where: { spaceId, status },
        orderBy: { position: 'asc' },
        select: { position: true },
      });

      return tx.issue.create({
        data: {
          spaceId,
          key: formatIssueKey(seqRow.slug, seqRow.issue_seq),
          title: dto.title.trim(),
          description: dto.description ?? null,
          status,
          ...(dto.priority ? { priority: dto.priority } : {}),
          assigneeId: dto.assigneeId ?? null,
          sprintId: dto.sprintId ?? null,
          parentId: dto.parentId ?? null,
          storyPoints: dto.storyPoints ?? null,
          reporterId,
          position: positionBetween(null, top?.position ?? null),
        },
        include: ISSUE_INCLUDE,
      });
    });

    const view = toView(created);
    this.realtime.toSpace(spaceId, 'issue:created', view);
    return view;
  }

  /** 에픽 → 스토리는 한 단계만이다. 부모가 이미 자식이면 거부한다. */
  private async requireEpicCandidate(spaceId: string, parentId: string) {
    const parent = await this.prisma.issue.findFirst({
      where: { id: parentId, spaceId },
      select: { parentId: true },
    });
    if (!parent) throw new NotFoundException('상위 이슈를 찾을 수 없습니다');
    if (parent.parentId) {
      throw new BadRequestException('에픽은 한 단계까지만 묶을 수 있습니다');
    }
  }
}

export { COLUMN_LIMIT, ISSUE_INCLUDE, toView };
export type { IssueRow, IssueView };
```

- [ ] **Step 2: 타입 검사로 확인한다**

Run: `npm --prefix server run typecheck`
기대: `src/issues` 가 아직 `exclude` 에 있으므로 검사 대상 밖이다 — 오류 0건. 파일 자체 확인은 Task 8 Step 5 에서 `exclude` 를 뺀 뒤 이뤄진다.

- [ ] **Step 3: 커밋**

```bash
git add server/src/issues/issues.service.ts && git commit -m "feat(server): 이슈 목록과 생성"
```

---

## Task 7: IssuesService — 조회 · 수정 · 이동

**Files:**
- Modify: `server/src/issues/issues.service.ts`

**Interfaces:**
- Consumes: `resolveClosedAt` (Task 4) · `needsRenumber`, `POSITION_STEP` (Task 3) · `MoveIssueDto`, `UpdateIssueDto` (Task 5)
- Produces: `IssuesService.get(spaceId, issueId): Promise<IssueView>` · `IssuesService.update(spaceId, issueId, dto): Promise<IssueView>` · `IssuesService.move(spaceId, issueId, dto): Promise<IssueView>`

- [ ] **Step 1: import 를 늘린다**

```ts
import { needsRenumber, POSITION_STEP, positionBetween } from './position';
import { resolveClosedAt } from './issue-status';
import { MoveIssueDto } from './dto/move-issue.dto';
import { UpdateIssueDto } from './dto/update-issue.dto';
```

- [ ] **Step 2: 세 메서드와 헬퍼를 `create()` 아래에 더한다**

```ts
  async get(spaceId: string, issueId: string): Promise<IssueView> {
    return toView(await this.requireIssue(spaceId, issueId));
  }

  /**
   * `status` 를 주면 **컬럼만** 옮긴다 — 대상 컬럼 맨 위로 간다.
   * 자리까지 정하려면 `move()` 다. 메뉴는 이웃을 모르므로, 그때 서버가 맨
   * 위를 고르는 것이 규칙이어야 앱이 이웃을 지어내지 않는다.
   */
  async update(spaceId: string, issueId: string, dto: UpdateIssueDto) {
    const current = await this.requireIssue(spaceId, issueId);
    const closedAt = resolveClosedAt(current.status, dto.status, new Date());

    let position: Prisma.Decimal | undefined;
    if (dto.status && dto.status !== current.status) {
      const top = await this.prisma.issue.findFirst({
        where: { spaceId, status: dto.status },
        orderBy: { position: 'asc' },
        select: { position: true },
      });
      position = positionBetween(null, top?.position ?? null);
    }

    const updated = await this.prisma.issue.update({
      where: { id: issueId },
      data: {
        ...(dto.title !== undefined ? { title: dto.title.trim() } : {}),
        ...(dto.description !== undefined ? { description: dto.description } : {}),
        ...(dto.status !== undefined ? { status: dto.status } : {}),
        ...(dto.priority !== undefined ? { priority: dto.priority } : {}),
        ...(dto.assigneeId !== undefined ? { assigneeId: dto.assigneeId } : {}),
        ...(dto.sprintId !== undefined ? { sprintId: dto.sprintId } : {}),
        ...(dto.storyPoints !== undefined ? { storyPoints: dto.storyPoints } : {}),
        ...(closedAt !== undefined ? { closedAt } : {}),
        ...(position !== undefined ? { position } : {}),
      },
      include: ISSUE_INCLUDE,
    });

    const view = toView(updated);
    this.realtime.toSpace(spaceId, 'issue:updated', view);
    return view;
  }

  /**
   * 드래그가 쓴다. 두 이웃 사이 중간값을 받는다.
   *
   * 이웃은 모두 `dto.status` 컬럼에 있어야 한다 — 다른 컬럼의 카드를 기준으로
   * 자리를 정하면 순서가 뜻을 잃는다.
   *
   * `renumbered` 는 재귀 깊이를 1로 묶는다. 재채번 뒤에도 자리가 나지 않는
   * 것은 계산이 틀렸다는 뜻이라, 조용히 반복하면 스택이 터질 때까지 돈다.
   */
  async move(
    spaceId: string,
    issueId: string,
    dto: MoveIssueDto,
    renumbered = false,
  ): Promise<IssueView> {
    const current = await this.requireIssue(spaceId, issueId);

    const prev = dto.afterId
      ? await this.requireNeighbour(spaceId, dto.afterId, dto.status)
      : null;
    const next = dto.beforeId
      ? await this.requireNeighbour(spaceId, dto.beforeId, dto.status)
      : null;

    if (prev && next && !prev.position.lessThan(next.position)) {
      throw new BadRequestException('기준 이슈 두 개의 순서가 뒤바뀌었습니다');
    }

    const position = positionBetween(prev?.position ?? null, next?.position ?? null);

    if (needsRenumber(position, prev?.position ?? null, next?.position ?? null)) {
      if (renumbered) {
        throw new BadRequestException('정렬 자리를 만들지 못했습니다');
      }
      await this.renumber(spaceId, dto.status);
      return this.move(spaceId, issueId, dto, true);
    }

    const closedAt = resolveClosedAt(current.status, dto.status, new Date());

    const updated = await this.prisma.issue.update({
      where: { id: issueId },
      data: {
        status: dto.status,
        position,
        ...(closedAt !== undefined ? { closedAt } : {}),
      },
      include: ISSUE_INCLUDE,
    });

    const view = toView(updated);
    this.realtime.toSpace(spaceId, 'issue:updated', view);
    return view;
  }

  private async requireIssue(spaceId: string, issueId: string): Promise<IssueRow> {
    const issue = await this.prisma.issue.findFirst({
      where: { id: issueId, spaceId },
      include: ISSUE_INCLUDE,
    });
    // 403 이 아니라 404 다 — 403 은 "그 이슈가 존재한다"를 알려 준다.
    if (!issue) throw new NotFoundException('이슈를 찾을 수 없습니다');
    return issue;
  }

  private async requireNeighbour(
    spaceId: string,
    neighbourId: string,
    status: IssueStatus,
  ) {
    const neighbour = await this.prisma.issue.findFirst({
      where: { id: neighbourId, spaceId },
      select: { id: true, position: true, status: true },
    });
    if (!neighbour) throw new NotFoundException('기준 이슈를 찾을 수 없습니다');
    if (neighbour.status !== status) {
      throw new BadRequestException('기준 이슈가 다른 컬럼에 있습니다');
    }
    return neighbour;
  }

  /** 자릿수가 소진된 컬럼을 1000 간격으로 다시 매긴다. */
  private async renumber(spaceId: string, status: IssueStatus) {
    const rows = await this.prisma.issue.findMany({
      where: { spaceId, status },
      orderBy: [{ position: 'asc' }, { createdAt: 'asc' }],
      select: { id: true },
    });
    await this.prisma.$transaction(
      rows.map((row, i) =>
        this.prisma.issue.update({
          where: { id: row.id },
          data: { position: POSITION_STEP.times(i) },
        }),
      ),
    );
  }
```

- [ ] **Step 3: 커밋**

```bash
git add server/src/issues/issues.service.ts && git commit -m "feat(server): 이슈 조회 · 수정 · 이동"
```

---

## Task 8: 컨트롤러 · 모듈 · 빌드 편입

**Files:**
- Create: `server/src/issues/issues.controller.ts` · `server/src/issues/issues.module.ts`
- Modify: `server/src/app.module.ts` · `server/tsconfig.json` · `server/tsconfig.build.json`

**Interfaces:**
- Consumes: `IssuesService` (Task 6·7)
- Produces: `GET|POST /api/spaces/:spaceId/issues` · `GET|PATCH /api/spaces/:spaceId/issues/:issueId` · `PUT /api/spaces/:spaceId/issues/:issueId/position`

- [ ] **Step 1: 컨트롤러**

`server/src/issues/issues.controller.ts`:

```ts
import {
  Body,
  Controller,
  Get,
  Param,
  ParseUUIDPipe,
  Patch,
  Post,
  Put,
  Query,
  UseGuards,
} from '@nestjs/common';
import { AuthUser, CurrentUser } from '../common/decorators/current-user.decorator';
import { SpaceGuard } from '../spaces/guards/space.guard';
import { SpaceRoleGuard } from '../spaces/guards/space-role.guard';
import { MinRole } from '../spaces/decorators/min-role.decorator';
import { IssuesService } from './issues.service';
import { CreateIssueDto } from './dto/create-issue.dto';
import { ListIssuesDto } from './dto/list-issues.dto';
import { MoveIssueDto } from './dto/move-issue.dto';
import { UpdateIssueDto } from './dto/update-issue.dto';

/**
 * 이슈 보드 (docs/백엔드-설계.md §4).
 *
 * 쓰기가 `member` 이상인 이유는 성격이다 — 채널·카테고리는 스페이스의 뼈대라
 * admin 이 지키지만, **이슈는 일하는 사람이 만드는 것**이다. guest 는 읽는다.
 */
@Controller('spaces/:spaceId/issues')
@UseGuards(SpaceGuard, SpaceRoleGuard)
export class IssuesController {
  constructor(private readonly issues: IssuesService) {}

  @Get()
  list(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Query() query: ListIssuesDto,
  ) {
    return this.issues.list(spaceId, query);
  }

  @Post()
  @MinRole('member')
  create(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Body() dto: CreateIssueDto,
    @CurrentUser() user: AuthUser,
  ) {
    return this.issues.create(spaceId, user.id, dto);
  }

  @Get(':issueId')
  get(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('issueId', new ParseUUIDPipe()) issueId: string,
  ) {
    return this.issues.get(spaceId, issueId);
  }

  @Patch(':issueId')
  @MinRole('member')
  update(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('issueId', new ParseUUIDPipe()) issueId: string,
    @Body() dto: UpdateIssueDto,
  ) {
    return this.issues.update(spaceId, issueId, dto);
  }

  @Put(':issueId/position')
  @MinRole('member')
  move(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('issueId', new ParseUUIDPipe()) issueId: string,
    @Body() dto: MoveIssueDto,
  ) {
    return this.issues.move(spaceId, issueId, dto);
  }
}
```

- [ ] **Step 2: 모듈**

`server/src/issues/issues.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { IssuesService } from './issues.service';
import { IssuesController } from './issues.controller';
import { SpacesModule } from '../spaces/spaces.module';
import { RealtimeEmitterModule } from '../realtime/realtime-emitter.module';

@Module({
  imports: [SpacesModule, RealtimeEmitterModule],
  controllers: [IssuesController],
  providers: [IssuesService],
  exports: [IssuesService],
})
export class IssuesModule {}
```

- [ ] **Step 3: `exclude` 에서 `src/issues` 를 뺀다**

`server/tsconfig.json` 과 `server/tsconfig.build.json` 두 파일 모두에서 `"src/issues"` 줄을 지운다. 주석의 "9단계~ → issues, gitlab (repos 로 개명), ai" 도 `gitlab` · `ai` 만 남도록 고친다.

- [ ] **Step 4: `app.module.ts` 에 등록한다**

`import { IssuesModule } from './issues/issues.module';` 를 더하고 `imports` 배열의 `AttachmentsModule` 아래에 `IssuesModule,` 을 넣는다.

- [ ] **Step 5: 타입 검사 · 린트 · 테스트**

```bash
npm --prefix server run typecheck && npm run server:lint && npm run server:test
```

기대: 오류 0건, 테스트 **83개**(기존 62 + Task 2·3·4 의 21) 통과

- [ ] **Step 6: 서버를 띄워 라우트가 실제로 붙는지 본다**

```bash
npm run db:up && npm run server:dev
```

로그에 `Mapped {/api/spaces/:spaceId/issues, GET}` 을 포함한 다섯 줄이 보여야 한다.

- [ ] **Step 7: 커밋**

```bash
git add server/src/issues server/src/app.module.ts server/tsconfig.json server/tsconfig.build.json && git commit -m "feat(server): 이슈 보드 API 를 빌드에 편입한다"
```

---

## Task 9: check:issues — 실서버 · 실DB · 실소켓 검증

**Files:**
- Create: `server/scripts/check-issues.mjs`
- Modify: `package.json` (루트)

**Interfaces:**
- Consumes: Task 8 의 라우트, `issue:created` · `issue:updated`
- Produces: `npm run check:issues`

`server/scripts/check-pins.mjs` 의 뼈대(`check` · `api` · `connect` · `waitFor` · `signup`)를 그대로 복사해 쓴다. **자체 계정과 스페이스를 만들어 쓰므로 비밀번호 인자가 필요 없다.**

- [ ] **Step 1: 스크립트를 쓴다 — 덮어야 할 것 여덟 가지**

스펙 §6 이 정한 목록이다. 각 항목이 `check(...)` 한 줄 이상으로 나타나야 한다.

```js
// 1. 테넌트 격리 — 남의 스페이스 이슈는 404
const foreign = await api('GET', `/spaces/${spaceId}/issues/${otherIssueId}`, { token: alice.token });
check('타 스페이스 이슈 조회는 404', foreign.status === 404, `status=${foreign.status}`);

// 2. 채번 — 동시에 만들어도 키가 겹치지 않는다
const burst = await Promise.all(
  Array.from({ length: 10 }, (_, i) =>
    api('POST', `/spaces/${spaceId}/issues`, { token: alice.token, body: { title: `동시 ${i}` } })),
);
const keys = burst.map((r) => r.json?.key);
check('동시 생성 10건의 키가 모두 다르다', new Set(keys).size === 10, keys.join(','));
check('키가 슬러그 접두사를 쓴다', keys.every((k) => k?.startsWith(prefix)), keys[0]);

// 3. position — 두 카드 사이로 옮기면 목록 순서가 바뀐다
const moved = await api('PUT', `/spaces/${spaceId}/issues/${third.id}/position`, {
  token: alice.token,
  body: { status: 'backlog', afterId: first.id, beforeId: second.id },
});
check('사이로 옮기면 200', moved.status === 200, `status=${moved.status}`);
const order = (await api('GET', `/spaces/${spaceId}/issues?status=backlog`, { token: alice.token }))
  .json.issues.map((i) => i.id);
check('목록 순서가 first · third · second 다',
  order.indexOf(first.id) < order.indexOf(third.id) &&
  order.indexOf(third.id) < order.indexOf(second.id), order.join(','));

// 4. 이웃 검증 — 다른 컬럼의 카드를 기준으로 옮기면 400
const cross = await api('PUT', `/spaces/${spaceId}/issues/${third.id}/position`, {
  token: alice.token, body: { status: 'doing', afterId: first.id },
});
check('다른 컬럼 카드를 기준으로 옮기면 400', cross.status === 400, `status=${cross.status}`);

// 5. 권한 — guest 는 생성·이동이 거부되고 열람은 된다
check('guest 생성 거부', guestCreate.status === 403, `status=${guestCreate.status}`);
check('guest 열람 허용', guestList.status === 200, `status=${guestList.status}`);

// 6. 소켓 — 밖에서 만든 이슈가 issue:created 로 온다 / 비멤버에게는 안 간다
check('issue:created 도착', createdEvent?.key === made.json.key, JSON.stringify(createdEvent));
check('비멤버에게는 가지 않는다', outsiderEvent === null);

// 7. 상한 — 컬럼이 200 을 넘기면 truncated 에 담긴다
check('상한을 넘기면 truncated 에 담긴다', big.json.truncated.includes('backlog'),
  JSON.stringify(big.json.truncated));

// 8. closedAt — done 으로 / done 에서 / done 안에서
check('done 으로 옮기면 closedAt 이 찍힌다', toDone.json.closedAt !== null);
check('done 에서 나오면 closedAt 이 비워진다', fromDone.json.closedAt === null);
check('done 안에서 자리만 옮기면 closedAt 이 그대로다',
  withinDone.json.closedAt === toDone.json.closedAt,
  `${withinDone.json.closedAt} vs ${toDone.json.closedAt}`);
```

**7번의 201건 만들기를 `Promise.all` 로 묶지 말 것** — 채번이 스페이스 행을 잠그므로 동시에 200건을 던지면 잠금 대기로 느려진다. 20건씩 끊어 보낸다.

- [ ] **Step 2: 루트 `package.json` 에 스크립트를 더한다**

`"check:attachments"` 아래:

```json
    "check:issues": "node server/scripts/check-issues.mjs",
```

- [ ] **Step 3: 서버를 띄우고 돌린다**

```bash
npm run db:up && npm run server:dev
```

다른 터미널에서:

```bash
npm run check:issues
```

기대: 전부 `OK`, 실패 0

- [ ] **Step 4: 실패가 있으면 코드를 고친다 — 스크립트를 고치지 않는다**

이 스크립트가 계약이다. 기대가 틀렸다고 판단되면 스펙을 먼저 고치고 근거를 남긴다.

- [ ] **Step 5: 커밋**

```bash
git add server/scripts/check-issues.mjs package.json && git commit -m "test(server): 이슈 계약 검증 — 실서버 · 실DB · 실소켓"
```

---

## Task 10: 앱 — 모델과 API

**Files:**
- Create: `app/lib/domain/models/issue.dart` · `app/lib/data/api/issues_api.dart`

**Interfaces:**
- Consumes: 서버 응답 `{ issues: [...], truncated: [...] }`
- Produces: `Issue` · `IssueAuthor` · `IssueStatus` · `IssuePriority` · `IssuePage` · `IssuesApi.list/create/update/move`

- [ ] **Step 1: 모델**

`app/lib/domain/models/issue.dart`:

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'issue.freezed.dart';
part 'issue.g.dart';

/// 보드 컬럼. 서버 enum 과 이름이 같아야 한다.
enum IssueStatus { backlog, doing, review, done }

enum IssuePriority { low, mid, high }

@freezed
abstract class IssueAuthor with _$IssueAuthor {
  const factory IssueAuthor({
    required String id,
    required String name,
    String? avatarUrl,
  }) = _IssueAuthor;

  factory IssueAuthor.fromJson(Map<String, dynamic> json) =>
      _$IssueAuthorFromJson(json);
}

@freezed
abstract class Issue with _$Issue {
  const factory Issue({
    required String id,
    required String key,
    required String title,
    String? description,
    required IssueStatus status,
    required IssuePriority priority,
    IssueAuthor? assignee,
    String? sprintId,
    String? parentId,
    int? storyPoints,
    /// 서버가 문자열로 준다 — Decimal 을 double 로 받으면 정밀도가 깎인다.
    /// 앱은 정렬에만 쓰고 산술을 하지 않는다.
    required String position,
    DateTime? closedAt,
    required DateTime createdAt,
    required DateTime updatedAt,
  }) = _Issue;

  factory Issue.fromJson(Map<String, dynamic> json) => _$IssueFromJson(json);
}
```

- [ ] **Step 2: 코드 생성**

```bash
cd app && dart run build_runner build --delete-conflicting-outputs
```

기대: `issue.freezed.dart` · `issue.g.dart` 생성

- [ ] **Step 3: API**

`app/lib/data/api/issues_api.dart` — `channels_api.dart` 와 같은 모양이다. 실패는 전부 `ApiException(classifyDioException(e))` 로 바꾼다(서버 문구를 화면에 그대로 쓰지 않는다).

```dart
import 'package:dio/dio.dart';

import '../../domain/models/issue.dart';
import 'api_client.dart';
import 'api_failure.dart';

/// 목록 응답. `truncated` 는 컬럼별 상한에 걸려 잘린 컬럼이다 —
/// 화면이 "더 있다"를 말할 수 있어야 한다.
class IssuePage {
  const IssuePage({required this.issues, required this.truncated});

  final List<Issue> issues;
  final List<IssueStatus> truncated;
}

class IssuesApi {
  IssuesApi(this._client);

  final ApiClient _client;

  /// GET /api/spaces/:spaceId/issues
  Future<IssuePage> list(String spaceId) async {
    try {
      final res =
          await _client.dio.get<Map<String, dynamic>>('/spaces/$spaceId/issues');
      final data = res.data ?? const <String, dynamic>{};
      return IssuePage(
        issues: (data['issues'] as List<dynamic>? ?? const [])
            .cast<Map<String, dynamic>>()
            .map(Issue.fromJson)
            .toList(growable: false),
        truncated: (data['truncated'] as List<dynamic>? ?? const [])
            .map((v) => IssueStatus.values.byName(v as String))
            .toList(growable: false),
      );
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }

  /// POST /api/spaces/:spaceId/issues
  Future<Issue> create(
    String spaceId, {
    required String title,
    String? description,
    IssueStatus? status,
    IssuePriority? priority,
    String? assigneeId,
  }) async {
    try {
      final res = await _client.dio.post<Map<String, dynamic>>(
        '/spaces/$spaceId/issues',
        data: {
          'title': title,
          if (description != null && description.isNotEmpty)
            'description': description,
          if (status != null) 'status': status.name,
          if (priority != null) 'priority': priority.name,
          if (assigneeId != null) 'assigneeId': assigneeId,
        },
      );
      return Issue.fromJson(res.data!);
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }

  /// PATCH /api/spaces/:spaceId/issues/:issueId — 컬럼만 옮긴다(대상 맨 위).
  Future<Issue> update(
    String spaceId,
    String issueId, {
    IssueStatus? status,
    String? title,
    IssuePriority? priority,
  }) async {
    try {
      final res = await _client.dio.patch<Map<String, dynamic>>(
        '/spaces/$spaceId/issues/$issueId',
        data: {
          if (status != null) 'status': status.name,
          if (title != null) 'title': title,
          if (priority != null) 'priority': priority.name,
        },
      );
      return Issue.fromJson(res.data!);
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }

  /// PUT /api/spaces/:spaceId/issues/:issueId/position — 자리까지 정한다.
  Future<Issue> move(
    String spaceId,
    String issueId, {
    required IssueStatus status,
    String? afterId,
    String? beforeId,
  }) async {
    try {
      final res = await _client.dio.put<Map<String, dynamic>>(
        '/spaces/$spaceId/issues/$issueId/position',
        data: {
          'status': status.name,
          if (afterId != null) 'afterId': afterId,
          if (beforeId != null) 'beforeId': beforeId,
        },
      );
      return Issue.fromJson(res.data!);
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }
}
```

- [ ] **Step 4: 정적 분석**

Run: `cd app && flutter analyze`
기대: 0건

- [ ] **Step 5: 커밋**

```bash
git add app/lib/domain/models/issue.dart app/lib/domain/models/issue.freezed.dart app/lib/domain/models/issue.g.dart app/lib/data/api/issues_api.dart && git commit -m "feat(app): 이슈 모델과 API"
```

---

## Task 11: 앱 — drift 캐시와 리포지토리

**Files:**
- Modify: `app/lib/data/local/app_database.dart`
- Create: `app/lib/data/repositories/issue_repository.dart`
- Test: `app/test/issue_test.dart`

**Interfaces:**
- Consumes: `IssuesApi` (Task 10) · `AppDatabase`
- Produces: `CachedIssues` 테이블 · `AppDatabase.watchIssues(spaceId)` · `replaceIssues(spaceId, issues)` · `upsertIssue(spaceId, issue)` · `deleteIssue(issueId)` · `IssueRepository.watchIssues/refreshIssues/create/moveTo/applyUpsert/applyDelete`

- [ ] **Step 1: 실패하는 테스트를 먼저 쓴다**

`app/test/issue_test.dart`:

```dart
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:nexus_app/data/local/app_database.dart';
import 'package:nexus_app/domain/models/issue.dart';

/// 인메모리 drift 로 실제 DB 동작까지 덮는다. 화면은 여기만 구독하므로
/// 캐시가 틀리면 보드가 통째로 틀린다.
void main() {
  late AppDatabase db;

  setUp(() => db = AppDatabase(NativeDatabase.memory()));
  tearDown(() => db.close());

  Issue issue(
    String id, {
    IssueStatus status = IssueStatus.backlog,
    String position = '0',
  }) =>
      Issue(
        id: id,
        key: 'NEXUS-$id',
        title: '이슈 $id',
        status: status,
        priority: IssuePriority.mid,
        position: position,
        createdAt: DateTime.utc(2026, 8, 20),
        updatedAt: DateTime.utc(2026, 8, 20),
      );

  test('캐시에 넣은 이슈를 position 순으로 돌려준다', () async {
    await db.replaceIssues('s1', [
      issue('b', position: '2000'),
      issue('a', position: '1000'),
    ]);

    final rows = await db.watchIssues('s1').first;

    expect(rows.map((r) => r.id), ['a', 'b']);
  });

  test('음수 position 이 맨 앞에 온다 — 문자열 정렬이면 뒤집힌다', () async {
    await db.replaceIssues('s1', [
      issue('a', position: '1000'),
      issue('b', position: '-1000'),
    ]);

    final rows = await db.watchIssues('s1').first;

    expect(rows.map((r) => r.id), ['b', 'a']);
  });

  test('다른 스페이스의 이슈는 섞이지 않는다', () async {
    await db.replaceIssues('s1', [issue('a')]);
    await db.replaceIssues('s2', [issue('b')]);

    final rows = await db.watchIssues('s1').first;

    expect(rows.map((r) => r.id), ['a']);
  });

  test('갱신은 같은 id 를 덮어쓴다 — 소켓과 REST 가 겹쳐도 중복되지 않는다', () async {
    await db.replaceIssues('s1', [issue('a')]);
    await db.upsertIssue('s1', issue('a', status: IssueStatus.doing));

    final rows = await db.watchIssues('s1').first;

    expect(rows.length, 1);
    expect(rows.single.status, IssueStatus.doing.name);
  });

  test('replaceIssues 는 사라진 이슈를 지운다', () async {
    await db.replaceIssues('s1', [issue('a'), issue('b')]);
    await db.replaceIssues('s1', [issue('a')]);

    final rows = await db.watchIssues('s1').first;

    expect(rows.map((r) => r.id), ['a']);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd app && flutter test test/issue_test.dart`
기대: FAIL — `replaceIssues` 가 정의되지 않았다는 컴파일 오류

- [ ] **Step 3: 테이블을 더한다**

`app/lib/data/local/app_database.dart` 의 `CachedSpaces` 아래:

```dart
/// 이슈 보드 캐시. 화면은 이것만 구독한다 — REST 와 소켓은 여기를 갱신할 뿐이다.
///
/// **큐가 없다.** 쓰기는 온라인 전용이라(스펙 §5) 오프라인에서 만든 이슈를
/// 보관할 곳이 필요 없다. 칸반 이동은 같은 카드를 두 곳에서 옮겼을 때 순서를
/// 합칠 방법이 없어, 메시지처럼 append 로 풀리지 않는다.
class CachedIssues extends Table {
  TextColumn get id => text()();
  TextColumn get spaceId => text()();
  TextColumn get key => text()();
  TextColumn get title => text()();
  TextColumn get description => text().nullable()();
  TextColumn get status => text()();
  TextColumn get priority => text()();
  TextColumn get assigneeId => text().nullable()();
  TextColumn get assigneeName => text().nullable()();
  TextColumn get assigneeAvatarUrl => text().nullable()();
  TextColumn get sprintId => text().nullable()();
  TextColumn get parentId => text().nullable()();
  IntColumn get storyPoints => integer().nullable()();

  /// 서버가 준 Decimal 문자열을 그대로 둔다. 이동 요청에 되돌려 보낼 값이다.
  TextColumn get position => text()();

  /// **정렬은 이것이 한다.** position 문자열은 사전순이 수 순서와 달라
  /// ('-1000' < '500' < '2000' 이 되지 않는다) 그대로 정렬하면 뒤집힌다.
  RealColumn get sortKey => real()();

  IntColumn get closedAt => integer().map(const _UtcMicros()).nullable()();
  IntColumn get createdAt => integer().map(const _UtcMicros())();
  IntColumn get updatedAt => integer().map(const _UtcMicros())();

  @override
  Set<Column> get primaryKey => {id};
}
```

- [ ] **Step 4: 데이터베이스 선언과 쿼리를 고친다**

`@DriftDatabase` 의 `tables` 에 `CachedIssues` 를 더하고, **`schemaVersion` 을 12 → 13 으로 올린다.** `_rebuildableCaches` 목록에도 `cachedIssues` 를 더한다(캐시라 통째 재생성 대상이다).

`AppDatabase` 안에 쿼리를 더한다:

```dart
  Stream<List<CachedIssue>> watchIssues(String spaceId) =>
      (select(cachedIssues)
            ..where((t) => t.spaceId.equals(spaceId))
            ..orderBy([
              (t) => OrderingTerm(expression: t.status),
              (t) => OrderingTerm(expression: t.sortKey),
              (t) => OrderingTerm(expression: t.createdAt),
            ]))
          .watch();

  /// 서버 목록으로 통째 교체한다. 사라진 이슈는 지운다 — 남겨 두면 다른
  /// 곳에서 지운 이슈가 이 기기에만 영원히 남는다.
  Future<void> replaceIssues(String spaceId, List<Issue> issues) =>
      transaction(() async {
        await (delete(cachedIssues)..where((t) => t.spaceId.equals(spaceId))).go();
        await batch((b) => b.insertAll(
              cachedIssues,
              issues.map((i) => _issueRow(spaceId, i)).toList(growable: false),
            ));
      });

  Future<void> upsertIssue(String spaceId, Issue issue) =>
      into(cachedIssues).insertOnConflictUpdate(_issueRow(spaceId, issue));

  Future<void> deleteIssue(String issueId) =>
      (delete(cachedIssues)..where((t) => t.id.equals(issueId))).go();

  CachedIssuesCompanion _issueRow(String spaceId, Issue i) =>
      CachedIssuesCompanion.insert(
        id: i.id,
        spaceId: spaceId,
        key: i.key,
        title: i.title,
        description: Value(i.description),
        status: i.status.name,
        priority: i.priority.name,
        assigneeId: Value(i.assignee?.id),
        assigneeName: Value(i.assignee?.name),
        assigneeAvatarUrl: Value(i.assignee?.avatarUrl),
        sprintId: Value(i.sprintId),
        parentId: Value(i.parentId),
        storyPoints: Value(i.storyPoints),
        position: i.position,
        sortKey: double.tryParse(i.position) ?? 0,
        closedAt: Value(i.closedAt),
        createdAt: i.createdAt,
        updatedAt: i.updatedAt,
      );
```

`import '../../domain/models/issue.dart';` 를 파일 상단에 더한다.

- [ ] **Step 5: 코드 생성 후 테스트**

```bash
cd app && dart run build_runner build --delete-conflicting-outputs && flutter test test/issue_test.dart
```

기대: PASS 5개

- [ ] **Step 6: 리포지토리**

`app/lib/data/repositories/issue_repository.dart` — `workspace_repository.dart` 와 같은 모양이다. 캐시 행을 도메인 모델로 되돌리는 `_toIssue` 를 두고, 아래 셋을 규칙대로 쓴다.

```dart
  Stream<List<Issue>> watchIssues(String spaceId) =>
      _db.watchIssues(spaceId).map(
            (rows) => rows.map(_toIssue).toList(growable: false),
          );

  /// **실패를 던지지 않고 false 를 돌려준다.** 오프라인은 오류가 아니라 정상
  /// 경로다. 캐시를 빈 값으로 덮어쓰지 않는 것이 핵심이다.
  Future<bool> refreshIssues(String spaceId) async {
    try {
      final page = await _api.list(spaceId);
      await _db.replaceIssues(spaceId, page.issues);
      return true;
    } on ApiException {
      return false;
    }
  }

  /// 낙관적으로 옮긴 뒤 실패하면 서버 값으로 되돌린다. 큐에 넣지 않는 이유는
  /// 칸반 이동이 오프라인 병합으로 풀리지 않기 때문이다.
  Future<bool> moveTo(String spaceId, Issue issue, IssueStatus status) async {
    await _db.upsertIssue(spaceId, issue.copyWith(status: status));
    try {
      final updated = await _api.update(spaceId, issue.id, status: status);
      await _db.upsertIssue(spaceId, updated);
      return true;
    } on ApiException {
      await _db.upsertIssue(spaceId, issue); // 되돌린다
      return false;
    }
  }
```

- [ ] **Step 7: 분석 · 전체 테스트**

```bash
cd app && flutter analyze && flutter test
```

기대: analyze 0건, 테스트 **111개**(기존 106 + 5)

- [ ] **Step 8: 커밋**

```bash
git add app/lib/data app/test/issue_test.dart && git commit -m "feat(app): 이슈 캐시와 리포지토리"
```

---

## Task 12: 앱 — 보드 화면

**Files:**
- Create: `app/lib/features/issue/board_controller.dart` · `board_screen.dart` · `issue_card.dart` · `new_issue_sheet.dart`
- Modify: `app/lib/core/router.dart` · `app/lib/features/shell/app_shell.dart` · `app/lib/features/shell/channel_pane.dart` · `app/lib/features/realtime/socket_controller.dart`

**Interfaces:**
- Consumes: `IssueRepository` (Task 11)
- Produces: `/s/:spaceId/issues` 라우트 · `boardProvider` → `Map<IssueStatus, List<Issue>>`

- [ ] **Step 1: 컨트롤러 — 상태별 묶기**

`board_controller.dart` 는 `channelGroupsProvider`(채널을 카테고리로 묶는 것)와 같은 모양이다. **모든 컬럼이 빈 목록으로라도 존재해야 한다** — 빈 컬럼이 사라지면 거기로 옮길 수 없다.

```dart
final boardProvider = Provider<Map<IssueStatus, List<Issue>>>((ref) {
  final issues = ref.watch(issueListProvider).valueOrNull ?? const <Issue>[];
  // 빈 컬럼도 자리를 지킨다.
  final board = {for (final s in IssueStatus.values) s: <Issue>[]};
  for (final issue in issues) {
    board[issue.status]!.add(issue);
  }
  return board;
});
```

- [ ] **Step 2: 보드 화면**

- 컬럼 넷을 가로 스크롤로 그린다. 열 너비는 폭에 따라 달라지지만 **분기가 아니라 제약이다** — `app_shell.dart` 밖에서 `Layout` 을 보지 않는다
- 각 열 머리에 이름과 개수. `truncated` 에 든 컬럼은 개수를 `200+` 로 보인다
- 오른쪽 아래에 `FloatingActionButton` — 새 이슈(`new_issue_sheet.dart`)

- [ ] **Step 3: 카드와 상태 이동 메뉴**

카드에 키(`NEXUS-12`) · 제목 · 담당자 아바타 · 우선순위. 오른쪽 위 메뉴를 누르면 **현재 컬럼을 뺀 나머지 셋**이 뜬다(눌러 봐야 실패하는 항목은 넣지 않는다 — 7-5 의 판단과 같다). 고르면 `moveTo()`.

실패하면 스낵바로 "옮기지 못했습니다"를 띄우고 카드가 제자리로 돌아간다 — 서버 문구를 그대로 쓰지 않는다.

- [ ] **Step 4: 라우트와 진입점**

`router.dart` 의 `/s/:spaceId` 아래, `files` 옆:

```dart
          // 보드는 파일 목록과 같이 셸 위에 덮어서 연다. 셸 본문을 탭으로
          // 갈아 끼우면 "라우트가 진실의 원천"이라는 셸의 전제가 깨진다.
          GoRoute(
            path: 'issues',
            builder: (_, state) =>
                BoardScreen(spaceId: state.pathParameters['spaceId']!),
          ),
```

진입점 둘:
- `app_shell.dart` 의 하단 탭 '보드'(인덱스 1) → `context.push('/s/$spaceId/issues')` 후 탭을 0으로 되돌린다
- `channel_pane.dart` 헤더에 보드 버튼(`Icons.dashboard_outlined`)

- [ ] **Step 5: 소켓 반영**

`socket_controller.dart` 에 `issue:created` · `issue:updated` → `db.upsertIssue`, `issue:deleted` → `db.deleteIssue` 를 잇는다. 이벤트 이름이 셋이라 앱은 "이 id 가 캐시에 있나"를 먼저 묻지 않아도 된다.

- [ ] **Step 6: 분석 · 테스트**

```bash
cd app && flutter analyze && flutter test
```

기대: 0건 · 111개 통과

- [ ] **Step 7: 커밋**

```bash
git add app/lib && git commit -m "feat(app): 칸반 보드 화면"
```

---

## Task 13: 실기기 확인과 문서 갱신

**Files:**
- Modify: `CLAUDE.md`(§4 · §5) · `docs/전환-계획.md`(§3.3 · 9단계 진행) · `docs/앱-설계.md`(§5 라우팅 표)

- [ ] **Step 1: Windows 데스크톱으로 띄운다**

```bash
npm run db:up && npm run server:dev
```

```bash
cd app && flutter run -d windows --dart-define=API_BASE=http://127.0.0.1:3000
```

- [ ] **Step 2: 다섯 가지를 눈으로 확인한다**

1. 보드가 네 컬럼으로 뜬다 — **빈 컬럼도 보인다**
2. 새 이슈를 만들면 backlog **맨 위**에 뜨고 키가 `NEXUS-N` 이다
3. 카드 메뉴로 doing 으로 옮기면 그 컬럼 맨 위로 간다
4. **밖에서 만든 이슈가 앱을 건드리지 않아도 나타난다** — 다른 터미널에서 `curl` 로 만든다
5. 서버를 끄고 앱을 재시작해도 보드가 캐시로 그려진다

- [ ] **Step 3: 확인한 것과 확인하지 못한 것을 나눠 적는다**

`CLAUDE.md` §4 에 "9-1 완료" 절을 더한다. 형식은 기존 절(7-5 · 8-3)과 같다 — 갈린 결정과 그 이유, 확인한 것, **확인하지 못한 것**.

`CLAUDE.md` §5 의 표에 9-1 행을 ✅ 로 바꾸고, "자주 쓰는 명령" 표에 `npm run check:issues` 를 더한다.

`docs/전환-계획.md` §3.3 의 `issues` 항목을 체크하고, 8단계 절 아래에 "9단계 진행" 절을 만든다.

`docs/앱-설계.md` §5 라우팅 표의 `/s/:spaceId/issues` 를 ✅ 로 바꾼다.

- [ ] **Step 4: 커밋**

```bash
git add CLAUDE.md docs && git commit -m "docs: 9-1 이슈 보드 완료를 반영한다"
```

---

## 자체 검토

**스펙 대조** — 스펙의 각 절이 어느 태스크에 있는가:

| 스펙 | 태스크 |
|---|---|
| §2 마이그레이션(`issueSeq` · `reporterId`) | Task 1 |
| §2 채번 · 접두사 | Task 2 · Task 6 |
| §2 `position` 중간값 · 재채번 | Task 3 · Task 7 |
| §3 목록(평평 · 상한 · `truncated`) | Task 6 |
| §3 권한(`member` 이상) | Task 8 |
| §3 404 규칙 | Task 7 (`requireIssue`) |
| §3 PATCH vs PUT 의 차이 | Task 7 |
| §3 `closedAt` 전이 | Task 4 · Task 7 |
| §3 이웃 컬럼 검증 | Task 7 (`requireNeighbour`) |
| §4 `issue:created` · `issue:updated` | Task 6 · Task 7 |
| §4 `issue:deleted` | 9-2 — 스펙이 그렇게 정했다. 앱의 수신부만 Task 12 에 미리 둔다 |
| §5 drift 캐시 · `schemaVersion` | Task 11 |
| §5 온라인 전용 쓰기 · 낙관적 되돌리기 | Task 11 |
| §5 라우트 · 진입점 | Task 12 |
| §6 `check:issues` 8항목 | Task 9 |
| §6 단위 테스트 | Task 2 · 3 · 4 |
| §6 앱 테스트 | Task 11 |
| §6 실기기 | Task 13 |

빠진 것 없음.

**이름 대조** — `positionBetween` · `needsRenumber` · `POSITION_STEP` · `resolveClosedAt` · `formatIssueKey` · `issueKeyPrefix` · `toView` · `IssueView` · `ISSUE_INCLUDE` · `IssueRow` · `replaceIssues` · `upsertIssue` · `deleteIssue` · `watchIssues` 가 정의된 태스크와 쓰는 태스크에서 같은 철자다.

**남은 위험**

- `Prisma.Decimal` 의 유효 자릿수는 decimal.js 기본값(20)이라 `DECIMAL(65,30)` 보다 먼저 소진된다. `needsRenumber` 가 그것을 잡는 자리이고, `move()` 의 `renumbered` 플래그가 재귀를 1회로 묶는다
- 캐시의 `sortKey`(`double`)는 position 문자열의 근사값이다. 두 카드의 position 이 `double` 정밀도(약 15자리) 안에서 구분되지 않을 만큼 가까워지면 화면 정렬이 흔들릴 수 있다 — 그 지점은 서버가 재채번하는 지점보다 뒤이므로 실제로는 닿지 않지만, 보드 순서가 이상하면 여기를 먼저 볼 것
