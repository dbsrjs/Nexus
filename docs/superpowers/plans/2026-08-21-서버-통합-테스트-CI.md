# 서버 통합 테스트를 CI 에 태운다 — 구현 계획

> **에이전트 작업자에게:** 이 계획은 `superpowers:subagent-driven-development`
> 또는 `superpowers:executing-plans` 로 task 단위로 실행한다. 단계는 체크박스
> (`- [ ]`)로 추적한다.

**Goal:** 계약 검증 스크립트 12종(439 케이스)이 push 마다 CI 에서 자동으로 돌게 한다.

**Architecture:** `ci.yml` 에 `서버 통합` 잡을 하나 더한다. pgvector 서비스
컨테이너를 띄우고, 마이그레이션·시드 후 **빌드본(`dist/main`)** 을 백그라운드로
기동한 뒤, 기존 스크립트를 **고치지 않고 그대로** 순차 실행한다. 그 앞에
전제조건 헬퍼를 붙여, 준비 단계가 무너졌을 때 케이스 실패 37줄이 아니라 한 줄로
말하게 한다.

**Tech Stack:** GitHub Actions · Node 22 · `pgvector/pgvector:pg16` · Prisma ·
순수 ESM 스크립트(프레임워크 없음)

**설계 문서:** [2026-08-21-서버-통합-테스트-CI-design.md](../specs/2026-08-21-서버-통합-테스트-CI-design.md)

## Global Constraints

- **주석 · 커밋 메시지 · 문서는 전부 한국어.** 커밋에
  `Co-Authored-By: Claude` 를 넣지 않는다
- **DB 이미지는 `pgvector/pgvector:pg16`.** 순정 `postgres` 는 init
  마이그레이션이 `CREATE EXTENSION "vector"` 에서 죽는다
- **`JWT_SECRET` 은 32자 이상.** 미만이면 서버가 부팅을 거부한다
  (`server/src/config/jwt.config.ts` 의 `MIN_SECRET_LENGTH`)
- **`OAUTH_TOKEN_KEY` 는 32바이트 base64url(43자).** 길이가 틀리면 부팅을
  멈춘다. 아예 없으면 부팅은 되고 연결만 503
- **`REDIS_URL` 은 넣지 않는다.** 빌드 대상 모듈 중 이 값을 읽는 곳이 없다
  (`redis-io.adapter.ts` 는 `tsconfig.build.json` 에서 제외돼 있다)
- **`process.exit()` 는 열린 핸들이 있을 때 쓰지 않는다.** Windows 에서
  libuv 가 `UV_HANDLE_CLOSING` 으로 죽는다 — 기존 스크립트들이 전부
  `process.exitCode` 만 세팅하는 이유다
- **기존 케이스 수를 바꾸지 않는다.** `check:browse` 56 · `check:oauth` 76 은
  CLAUDE.md 에 적힌 값이다

---

### Task 1: 전제조건 헬퍼 — 준비 실패를 한 줄로 말한다

**왜 CI 보다 먼저인가.** CI 가 빨갛게 됐을 때 읽히지 않으면 CI 를 붙인 값이
없다. 2026-08-21 에 `.env` 의 `PUBLIC_BASE_URL` 하나가 없어 `check:browse` 가
37개 실패로 무너졌는데, 원인은 첫 줄의 400 응답 본문 안에만 있었다.

**Files:**
- Create: `server/scripts/lib/preflight.mjs`
- Modify: `server/scripts/check-attachments.mjs` · `check-browse.mjs` ·
  `check-issues.mjs` · `check-mentions.mjs` · `check-oauth.mjs` ·
  `check-pins.mjs` · `check-quotes.mjs` · `check-reactions.mjs` ·
  `check-realtime.mjs` · `check-repos.mjs` · `check-threads.mjs` (11개)
- Modify: `server/.env.example`

**Interfaces:**
- Consumes: 없음 (첫 task)
- Produces:
  - `requireServer(base: string): Promise<void>` — 서버가 안 답하면 안내를
    찍고 `process.exit(1)`
  - `abortUnless(ok: boolean, reason: string, json?: unknown): void` —
    거짓이면 `PreflightAbort` 를 던진다
  - `hintFor(json: unknown): string | null`
  - `class PreflightAbort extends Error` — `.reason: string` · `.hint: string | null`

- [ ] **Step 1: 실패하는 상태를 먼저 눈으로 확인한다**

서버를 끈 채로 돌린다. **이것이 고치려는 "실패" 상태다.**

```bash
cd server && node scripts/check-pins.mjs
```

기대: `TypeError: fetch failed` 와 Node 스택 트레이스가 쏟아진다.
어디를 봐야 하는지에 대한 안내는 한 줄도 없다.

- [ ] **Step 2: 헬퍼를 만든다**

`server/scripts/lib/preflight.mjs` 를 아래 내용으로 만든다.

```js
// 계약 검증 스크립트의 공용 전제조건 검사.
//
// **왜 있는가.** 2026-08-21, server/.env 에 PUBLIC_BASE_URL 이 없다는 이유로
// check:browse 가 37개 실패로 무너졌다. 화면에 보인 것은 빨간 줄 37개였고,
// 원인은 그중 첫 줄의 400 응답 본문 안에만 있었다.
//
// 진짜 문제는 설정이 아니라 **준비 단계의 실패를 케이스 실패로 취급한 것**이다.
// 준비가 안 됐으면 뒤의 케이스들은 "틀린 것"이 아니라 **검증하지 못한 것**이다.
// 둘을 같은 FAIL 로 찍으면 읽는 사람이 원인을 찾지 못한다.

/**
 * 준비 단계가 무너졌을 때 던진다. 각 스크립트의 최상위 catch 가 받아
 * 가짜 서버를 닫은 뒤 종료 코드를 세팅한다 — 그 정리 경로를 그대로 쓰려고
 * 예외로 만들었다. 여기서 process.exit() 를 부르면 열려 있는 핸들과 경합해
 * Windows 에서 UV_HANDLE_CLOSING 으로 죽는다.
 */
export class PreflightAbort extends Error {
  constructor(reason, hint) {
    super(reason);
    this.name = 'PreflightAbort';
    this.reason = reason;
    this.hint = hint ?? null;
  }
}

/**
 * 알려진 실패를 알아보고 어디를 봐야 하는지 알려 준다.
 *
 * **실제로 겪은 것만 넣는다.** 겪지 않은 실패에 힌트를 붙이면 확인할 수 없는
 * 가정 위에 코드를 쌓게 되고, 틀린 힌트는 없느니만 못하다. 모르는 실패는
 * 서버가 준 메시지를 그대로 보여 주는 것으로 충분하다.
 */
export function hintFor(json) {
  const message =
    json && typeof json === 'object' && typeof json.message === 'string'
      ? json.message
      : '';

  if (message.includes('PUBLIC_BASE_URL')) {
    return [
      'server/.env 에 PUBLIC_BASE_URL 이 없습니다.',
      '',
      '계약 검증에서는 아무 http 주소면 됩니다 — 가짜 GitHub 이 훅 URL 을',
      '검사하지 않습니다:',
      '',
      '  PUBLIC_BASE_URL=http://127.0.0.1:3000',
      '',
      '값을 넣은 뒤 서버를 **재시작**해야 합니다(.env 는 부팅 때 읽습니다).',
      '자세한 것은 docs/10-2a-인수인계.md §2.3 을 볼 것.',
    ].join('\n');
  }

  return null;
}

/** 준비 단계 호출이 실패하면 중단한다. 뒤를 더 돌아 봐야 읽을 것이 없다. */
export function abortUnless(ok, reason, json) {
  if (ok) return;
  throw new PreflightAbort(reason, hintFor(json));
}

/**
 * 서버가 떠 있는지 확인한다. 안 떠 있으면 한 줄로 안내하고 끝낸다.
 *
 * /api/me 는 인증이 필요해 401 이 온다 — 여기서 보려는 것은 "서버가
 * 답하는가" 하나다. 이 시점에는 소켓도 가짜 서버도 아직 열리지 않아
 * 열린 핸들이 없으므로 process.exit() 가 안전하다.
 */
export async function requireServer(base) {
  try {
    await fetch(`${base}/me`, { signal: AbortSignal.timeout(3000) });
    return;
  } catch {
    /* 아래에서 안내한다 */
  }

  console.error('');
  console.error(`서버에 닿지 못했습니다 — ${base}`);
  console.error('');
  console.error('계약 검증은 실제로 부팅한 서버에 밖에서 붙습니다. 먼저 띄우십시오:');
  console.error('');
  console.error('  npm run db:up        (다른 터미널)');
  console.error('  npm run server:dev   (다른 터미널)');
  console.error('');
  process.exit(1);
}
```

- [ ] **Step 3: 11개 스크립트에 두 줄씩 넣는다**

각 파일에서 `const BASE = 'http://127.0.0.1:3000/api';` 줄을 찾아,
**바로 위에 import 를, 바로 아래에 호출을** 넣는다.

```js
import { requireServer } from './lib/preflight.mjs';
const BASE = 'http://127.0.0.1:3000/api';
await requireServer(BASE);
```

대상 11개 (전부 같은 모양이다):

```
check-attachments.mjs   check-browse.mjs      check-issues.mjs
check-mentions.mjs      check-oauth.mjs       check-pins.mjs
check-quotes.mjs        check-reactions.mjs   check-realtime.mjs
check-repos.mjs         check-threads.mjs
```

**`check-migrations.mjs` 는 대상이 아니다** — DB 도 서버도 없이 도는 정적
검사다. `const BASE` 선언 자체가 없다.

import 가 파일 중간에 오는 것처럼 보이지만, 모든 파일에서 `const BASE` 는
import 블록 바로 다음 줄이라 실제로는 import 블록 끝에 붙는다. ESLint 는
`src` 만 보므로(`eslint src`) 스크립트는 검사 대상이 아니다.

- [ ] **Step 4: 서버가 꺼진 상태에서 확인한다**

서버가 떠 있으면 먼저 끈다(`dist/main` 프로세스만 골라 끈다 —
`taskkill node.exe` 는 WSL 세션과 DB 까지 죽인다).

```bash
cd server && node scripts/check-pins.mjs
```

기대: 스택 트레이스 대신 아래가 나오고 종료 코드 1.

```
서버에 닿지 못했습니다 — http://127.0.0.1:3000/api

계약 검증은 실제로 부팅한 서버에 밖에서 붙습니다. 먼저 띄우십시오:

  npm run db:up        (다른 터미널)
  npm run server:dev   (다른 터미널)
```

11개 전부 확인한다:

```bash
cd server && for s in attachments browse issues mentions oauth pins quotes reactions realtime repos threads; do printf "%-14s " "$s"; node "scripts/check-$s.mjs" foo 2>&1 | grep -c "서버에 닿지 못했습니다"; done
```

기대: 11줄 전부 `1`.

- [ ] **Step 5: browse 의 준비 단계에 게이트를 단다**

`server/scripts/check-browse.mjs` 의 `저장소 준비` 줄 **바로 아래**에 한 줄을
더한다. `check()` 는 **지우지 않는다** — 통과할 때의 케이스 수(56)를 그대로
두기 위함이다.

```js
  check('저장소 준비', connected.status === 201, JSON.stringify(connected.json));
  abortUnless(
    connected.status === 201,
    `저장소 준비 실패 (status=${connected.status})`,
    connected.json,
  );
```

Step 3 에서 넣은 import 를 늘린다:

```js
import { requireServer, abortUnless, PreflightAbort } from './lib/preflight.mjs';
```

- [ ] **Step 6: browse 의 최상위 catch 가 중단을 알아보게 한다**

`check-browse.mjs` 끝의 블록을 바꾼다. **`fake.close()` 위치를 건드리지
않는다** — Windows libuv 문제 때문에 그 자리에 있다.

바꾸기 전:

```js
if (crashed) {
  console.error('\n검증 중 예외:', crashed);
  process.exitCode = 1;
}
```

바꾼 뒤:

```js
if (crashed instanceof PreflightAbort) {
  console.error(`\n중단: ${crashed.reason}`);
  if (crashed.hint) console.error(`\n${crashed.hint}`);
  console.error('\n뒤의 케이스는 실패한 것이 아니라 검증하지 못한 것입니다.\n');
  process.exitCode = 1;
} else if (crashed) {
  console.error('\n검증 중 예외:', crashed);
  process.exitCode = 1;
}
```

- [ ] **Step 7: oauth 에도 같은 게이트를 단다**

`server/scripts/check-oauth.mjs` 의 `자동 등록 201` 줄 바로 아래:

```js
  check('자동 등록 201', connected.status === 201, JSON.stringify(connected.json));
  abortUnless(
    connected.status === 201,
    `자동 등록 실패 (status=${connected.status})`,
    connected.json,
  );
```

import:

```js
import { requireServer, abortUnless, PreflightAbort } from './lib/preflight.mjs';
```

끝의 catch 블록 — 바꾸기 전:

```js
} catch (err) {
  crashed = err;
  console.error('예외로 중단:', err);
}
```

바꾼 뒤:

```js
} catch (err) {
  crashed = err;
  if (err instanceof PreflightAbort) {
    console.error(`\n중단: ${err.reason}`);
    if (err.hint) console.error(`\n${err.hint}`);
    console.error('\n뒤의 케이스는 실패한 것이 아니라 검증하지 못한 것입니다.\n');
  } else {
    console.error('예외로 중단:', err);
  }
}
```

- [ ] **Step 8: 전제조건이 빠진 상태를 실제로 만들어 확인한다**

`server/.env` 의 `PUBLIC_BASE_URL` 값을 잠시 비우고 **서버를 재시작한 뒤**
돌린다(`.env` 는 부팅 때 읽는다).

```bash
cd server && node scripts/check-browse.mjs 2>&1 | tail -20
```

기대: `FAIL` 이 37줄이 아니라 `중단: 저장소 준비 실패 (status=400)` 한 줄과
`.env` 를 어떻게 고치는지가 나온다. 종료 코드 1.

확인이 끝나면 값을 되돌리고 서버를 다시 재시작한다.

- [ ] **Step 9: 정상 상태에서 케이스 수가 그대로인지 확인한다**

```bash
npm run check:browse && npm run check:oauth
```

기대: **`통과 56 · 실패 0`** 과 **`통과 76 · 실패 0`**.
게이트를 달았다고 숫자가 바뀌면 안 된다.

- [ ] **Step 10: `.env.example` 에 경고 한 줄을 더한다**

`server/.env.example` 의 `PUBLIC_BASE_URL=` 줄 **바로 위**에 더한다.
**값은 비운 채로 둔다** — 빈 값도 정당한 설정이다(자동 등록만 400 이고
수동 등록은 된다).

```
# ⚠ 비워 두면 계약 검증이 대량 실패한다 — check:oauth 22개 · check:browse 37개.
#   그때는 아무 http 주소면 된다(가짜 GitHub 이 훅 URL 을 검사하지 않는다):
#     PUBLIC_BASE_URL=http://127.0.0.1:3000
```

- [ ] **Step 11: 나머지 스크립트가 안 깨졌는지 확인한다**

```bash
npm run check:reactions && npm run check:threads && npm run check:quotes && npm run check:mentions && npm run check:pins && npm run check:attachments && npm run check:issues && npm run check:repos && npm run check:migrations
```

기대: 24 · 25 · 17 · 18 · 20 · 43 · 89 · 30 케이스가 각각 `실패 0`,
migrations 통과.

- [ ] **Step 12: 커밋**

```bash
git add server/scripts/lib/preflight.mjs server/scripts/check-*.mjs server/.env.example
```

커밋 메시지:

```
test(server): 계약 검증의 전제조건을 한 줄로 말한다

PUBLIC_BASE_URL 하나가 없어 check:browse 가 37개 실패로 무너지는 것을
겪었다. 화면에 보인 것은 빨간 줄 37개였고 원인은 그중 첫 줄의 400 응답
본문 안에만 있었다.

진짜 문제는 설정이 아니라 스크립트가 준비 단계의 실패를 케이스 실패로
취급한 것이다. 준비가 안 됐으면 뒤의 케이스는 틀린 것이 아니라 검증하지
못한 것인데, 지금 출력은 둘을 구분하지 못했다.

공용 헬퍼를 두어 서버가 안 떠 있으면 한 줄로 안내하고, 준비 단계가
무너지면 즉시 중단하며 어디를 고쳐야 하는지 알려 준다. 힌트는 실제로
겪은 것만 넣었다 - 겪지 않은 실패에 힌트를 붙이면 확인할 수 없는 가정
위에 코드를 쌓게 된다.

중단을 예외로 만든 것은 각 스크립트가 이미 가진 정리 경로(가짜 서버를
닫고 exitCode 만 세팅)를 그대로 쓰기 위함이다. 그 자리에서
process.exit() 를 부르면 열린 핸들과 경합해 Windows 에서
UV_HANDLE_CLOSING 으로 죽는다.

check() 줄은 지우지 않아 통과 시 케이스 수는 그대로다(56 · 76).
```

---

### Task 2: CI 잡 — push 마다 439 케이스를 돌린다

**Files:**
- Modify: `.github/workflows/ci.yml` (`jobs:` 아래 맨 끝에 잡 하나 추가)

**Interfaces:**
- Consumes: Task 1 의 `requireServer` — CI 에서 서버 기동이 실패하면
  스크립트가 스택 대신 한 줄로 말한다
- Produces: 없음 (마지막 task)

- [ ] **Step 1: 지금 CI 가 DB 없이 도는 것을 확인한다**

```bash
grep -c "postgres" .github/workflows/ci.yml
```

기대: `0`. **이것이 고치려는 상태다** — 계약 검증 12종이 CI 에서 한 번도
돌지 않는다.

- [ ] **Step 2: 잡을 추가한다**

`.github/workflows/ci.yml` 의 `app:` 잡 **뒤에**(파일 맨 끝) 붙인다.

```yaml
  server-integration:
    name: 서버 통합 (계약 검증 12종)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server

    # 순정 postgres 를 쓰면 init 마이그레이션이 CREATE EXTENSION "vector" 에서
    # 죽는다. docker-compose.yml 과 같은 이미지를 쓴다.
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_DB: nexus
          POSTGRES_USER: nexus
          POSTGRES_PASSWORD: nexus
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U nexus -d nexus"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    # **전부 검증 전용 가짜값이다. secrets 를 쓰지 않는다.**
    # secrets 에 넣으면 포크에서 온 PR 이 조용히 못 돌고, 새 PC 에서 재현도
    # 안 된다. 그리고 가짜값을 secrets 에 두는 것 자체가 "여기 진짜 비밀이
    # 있다"는 잘못된 신호가 되어, 나중에 누군가 그 자리에 진짜 키를 넣는다.
    # 이 DB 는 잡이 끝나면 사라진다.
    env:
      DATABASE_URL: postgresql://nexus:nexus@127.0.0.1:5432/nexus
      # 32자 미만이면 서버가 부팅을 거부한다(jwt.config.ts).
      JWT_SECRET: nM31nP4SCY7cbL8jVTdQ3E72yaK4VocwQ0a6ZHAW2d-XruMK0rc3xThqEFVr0_CF
      JWT_REFRESH_SECRET: vUs9kRXk8kJd1aLjV86t01pvGFeWyae9lx7Puut1WY8ow64l9Ai8GGLCKGTvXeNk
      JWT_EXPIRES_IN: 15m
      JWT_REFRESH_EXPIRES_IN: 7d
      CORS_ORIGINS: http://localhost:5173
      COOKIE_SECURE: 'false'
      STORAGE_DRIVER: local
      STORAGE_LOCAL_DIR: /tmp/nexus-storage
      GITHUB_CLIENT_ID: check-client-id
      GITHUB_CLIENT_SECRET: check-client-secret
      GITHUB_CALLBACK_URL: http://127.0.0.1:3000/api/auth/github/callback
      # check:oauth · check:browse 가 이 포트에 가짜 GitHub 을 스스로 띄운다.
      GITHUB_OAUTH_BASE: http://127.0.0.1:4599
      GITHUB_API_BASE: http://127.0.0.1:4599
      # 32바이트 base64url. 길이가 틀리면 부팅이 멈춘다.
      OAUTH_TOKEN_KEY: 28zA3oxkP03A-XaFJAa95xge7vGvgYjBQIQRmdUqBv4
      # 가짜 GitHub 이 훅 URL 을 검사하지 않으므로 아무 http 주소면 된다.
      PUBLIC_BASE_URL: http://127.0.0.1:3000
      # check:realtime 만 시드 계정으로 로그인한다. 시드는 기존 계정의
      # 비밀번호를 덮어쓰지 않지만(upsert 의 update 가 name 만 건드린다)
      # CI 는 DB 가 매번 새것이라 이 값으로 생성된다.
      SEED_PASSWORD: ci-seed-password-1234
      PORT: '3000'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: server/package-lock.json

      - run: npm ci

      # Prisma Client 는 생성물이라 저장소에 없다.
      - run: npm run prisma:generate

      - run: npm run prisma:deploy

      - run: npm run seed

      - run: npm run build

      # **빌드본을 띄운다(start:dev 가 아니다).** enableBigIntSerialization() ·
      # rawBody: true · 전역 파이프 · 전역 예외 필터가 전부 main.ts 에 있고,
      # 이 층이 존재하는 이유가 그것을 태우는 것이다. CLAUDE.md §6 의 BigInt
      # 버그도 10-3a 의 Retry-After 결함도 정확히 그 자리에 있었다.
      - name: 서버 기동
        run: node dist/main > /tmp/server.log 2>&1 &

      # 고정 sleep 을 쓰지 않는다 — 느린 러너에서 먼저 깨지고 빠른 러너에서는
      # 시간을 버린다.
      - name: 서버가 응답할 때까지 대기
        run: |
          for i in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" -m 2 http://127.0.0.1:3000/api/me || true)
            if [ "$code" != "000" ]; then
              echo "준비됨 (HTTP $code, ${i}회 시도)"
              exit 0
            fi
            sleep 1
          done
          echo "::error::서버가 60초 안에 응답하지 않았습니다"
          cat /tmp/server.log
          exit 1

      - name: 계약 검증 12종
        run: |
          set -e
          echo "── check:migrations ──"
          node scripts/check-migrations.mjs
          echo "── check:realtime ──"
          node scripts/check-realtime.mjs "$SEED_PASSWORD"
          for s in reactions threads quotes mentions pins attachments issues repos oauth browse; do
            echo "── check:$s ──"
            node "scripts/check-$s.mjs"
          done

      # 없으면 CI 의 빨간 X 를 보고도 원인을 못 찾는다. 2026-08-21 에
      # PUBLIC_BASE_URL 하나로 37개가 무너졌을 때, 원인은 서버가 준 400 응답
      # 본문 안에만 있었다.
      - name: 실패 시 서버 로그
        if: failure()
        run: cat /tmp/server.log
```

- [ ] **Step 3: YAML 이 유효한지 확인한다**

```bash
python -c "import yaml,io; d=yaml.safe_load(io.open('.github/workflows/ci.yml',encoding='utf-8')); print('잡:', list(d['jobs'].keys()))"
```

기대: `잡: ['server', 'app', 'server-integration']`

- [ ] **Step 4: 명령 순서를 로컬에서 그대로 태워 본다**

CI 가 돌릴 것과 같은 순서를 이 PC 에서 돌려 스크립트 이름과 인자가 맞는지
확인한다(서버가 떠 있어야 한다).

```bash
cd server && set -e && node scripts/check-migrations.mjs && for s in reactions threads quotes mentions pins attachments issues repos oauth browse; do echo "── check:$s ──"; node "scripts/check-$s.mjs"; done
```

기대: 전부 `실패 0` 으로 끝나고 종료 코드 0.

`check-realtime.mjs` 는 시드 비밀번호를 알아야 해서 이 PC 에서는 못 돈다 —
시드가 기존 계정의 비밀번호를 덮어쓰지 않기 때문이다. **CI 로그로 확인한다**
(Step 6).

- [ ] **Step 5: 커밋하고 브랜치로 push 한다**

`main` 에 바로 올리지 않는다. 이 변경의 검증 대상이 CI 자신이라, 한 번
빨갛게 될 것을 전제로 브랜치에서 확인한다.

```bash
git checkout -b chore/ci-server-integration && git add .github/workflows/ci.yml
```

커밋 메시지:

```
ci: 계약 검증 12종을 CI 에 태운다

계약 검증 스크립트 11종(4,765줄 · 439 케이스)이 CI 에서 한 번도 돌지
않았다. ci.yml 의 서버 잡에는 DB 가 없어, 컨트롤러 · 서비스 · 스키마 ·
직렬화를 덮는 유일한 층이 사람의 기억에 달려 있었다.

pgvector 서비스 컨테이너를 띄우고 빌드본(dist/main)을 기동한 뒤 기존
스크립트를 그대로 순차 실행한다. 스크립트는 한 줄도 고치지 않았다.

빌드본을 띄우는 것이 이 층의 존재 이유다. enableBigIntSerialization() ·
rawBody · 전역 파이프 · 전역 예외 필터가 전부 main.ts 에 있고, CLAUDE.md
§6 의 BigInt 버그도 10-3a 의 Retry-After 결함도 그 자리에 있었다.

측정해 보니 전체가 30초 안쪽이라 계층을 두지 않았다 - 전부 매번 돈다.

env 를 secrets 로 두지 않은 이유는 값이 전부 검증 전용 가짜이기
때문이다. secrets 에 넣으면 포크 PR 이 조용히 못 돌고, 가짜값을 거기
두는 것 자체가 나중에 진짜 키를 넣게 만드는 신호가 된다.
```

그리고 push 한다(**사용자에게 먼저 확인받는다** — 저장소 밖으로 나가는
첫 동작이다).

```bash
git push -u origin chore/ci-server-integration
```

- [ ] **Step 6: CI 결과를 읽는다**

```bash
gh run list --branch chore/ci-server-integration --limit 3
```

끝날 때까지 기다린 뒤, **`check:realtime` 이 실제로 돌았는지**와 전체 소요
시간을 로그에서 확인한다:

```bash
gh run view --log --job "서버 통합 (계약 검증 12종)" | grep -E "── check:|통과 [0-9]+|준비됨"
```

기대: 12개 구간이 전부 보이고 `check:realtime` 이 `통과 41` 로 끝난다.

**백그라운드 서버가 단계 사이를 넘어 살아 있는지도 여기서 확인된다.** 각
`run:` 은 자기 셸에서 도는데, 배경 프로세스가 러너에서 살아남는 것에 기대고
있다(로그를 파일로 돌린 것도 그래서다 — stdout 을 잡고 있으면 단계가 끝나지
않는다). 살아남지 못하면 대기 단계가 60초 후 서버 로그와 함께 실패한다.

- [ ] **Step 7: 기존 두 잡이 그대로인지 확인한다**

새 잡을 더하면서 기존 것을 건드리지 않았는지 본다. **이 변경이 고치려는 것은
빠진 층이지 있는 층이 아니다.**

```bash
gh run view --json jobs --jq '.jobs[] | "\(.conclusion)\t\(.name)"'
```

기대: 세 줄이고 전부 `success`.

```
success	서버 (lint · build · test)
success	앱 (analyze · test)
success	서버 통합 (계약 검증 12종)
```

- [ ] **Step 8: 일부러 깨뜨려 CI 가 빨개지는지 확인한다**

검증이 실제로 지키고 있는지 확인한다. 케이스 하나를 뒤집는다.

```bash
cd server && sed -i "s/pinned.status === 201/pinned.status === 999/" scripts/check-pins.mjs && git commit -am "tmp: CI 가 실패를 잡는지 확인" && git push
```

기대: CI 가 **빨갛게 되고**, 로그에서 `FAIL  고정` 이 읽히며, `실패 시
서버 로그` 단계가 돈다.

확인 후 되돌린다:

```bash
git revert --no-edit HEAD && git push
```

- [ ] **Step 9: main 으로 합친다**

```bash
git checkout main && git merge --ff-only chore/ci-server-integration && git push
```

- [ ] **Step 10: 문서를 갱신한다**

`CLAUDE.md` §5 "알려진 빚" 의 첫 항목을 고친다.

바꾸기 전:

```
- **테스트 범위가 얕다.** 서버 단위 테스트 44개는 순수 로직 · 가드 · 권한 규칙만 덮는다. 컨트롤러 · 서비스의 **실 DB 통합 테스트는 없고**, 그 층은 여전히 `npm run check:realtime` 이 담당한다. 이 경계는 의도한 것이다 — 단위 테스트로 DB 동작을 증명하려 하면 §6 의 실수를 반복한다.
```

바꾼 뒤:

```
- **컨트롤러 · 서비스의 실 DB 검증은 계약 검증 스크립트가 담당한다.** 서버 단위 테스트 160개는 순수 로직 · 가드 · 권한 규칙만 덮는다. 이 경계는 의도한 것이다 — 단위 테스트로 DB 동작을 증명하려 하면 §6 의 실수를 반복한다. **2026-08-21 부터 계약 검증 12종(439 케이스)이 CI 에서 push 마다 돈다** — 그 전까지는 사람이 기억해 타이핑할 때만 돌았다. 남은 빚은 **러너**다: 손으로 짠 `check()` 가 거짓 통과를 구조적으로 막지 못한다(10-2b 에서 `undefined === undefined` 로 겪었다).
```

그리고 §1 "자주 쓰는 명령" 표 **바로 위**에 한 줄을 더한다:

```
계약 검증(`check:*`)은 **CI 에서 push 마다 자동으로 돈다.** 아래 명령은 고치는
중에 손으로 돌려 볼 때 쓴다 — 서버가 안 떠 있으면 한 줄로 알려 준다.
```

커밋:

```bash
git add CLAUDE.md && git commit -m "docs: 계약 검증이 CI 에서 돈다는 것을 반영한다" && git push
```
