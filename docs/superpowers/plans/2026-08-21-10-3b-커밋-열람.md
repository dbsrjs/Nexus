# 10-3b 커밋 열람 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 채널의 "커밋 9개를 올렸습니다"를 누르면 그 아홉 개가 보이고, 하나를 누르면 무엇이 바뀌었는지 보인다.

**Architecture:** push 이벤트의 커밋과 변경 파일은 **`repo_events.payload` 에 이미 있다** — GitHub 을 부르지 않는다. 브랜치 이력과 임의 커밋 상세만 GitHub 을 프록시한다. 메시지에는 `repoEventId` 하나를 실어 "파고들 수 있다"를 알린다.

**Tech Stack:** NestJS · Prisma · Node `fetch` · Flutter(Riverpod · go_router · drift · dio)

**Spec:** [docs/superpowers/specs/2026-08-21-커밋-열람-design.md](../specs/2026-08-21-커밋-열람-design.md)

## Global Constraints

- **설계 스펙에서 갈라지면 스펙을 먼저 고친다.**
- **주석 · 커밋 메시지 · 문서는 한국어.** 커밋에 `Co-Authored-By: Claude` 를 넣지 않는다.
- **브랜치는 `feat/commit-browse`.** main 에서 직접 작업하지 않는다(10-3a 에서 어겼다).
- **테넌트 격리:** 모든 라우트가 `SpaceGuard` 아래다. **볼 수 없으면 403 이 아니라 404.**
- **읽기에 `@MinRole` 을 걸지 않는다**(10-3a §2 와 같다).
- **N+1 을 만들지 않는다.** 메시지 목록의 `repoEventId` 는 `messageId in (…)` 한 번으로 읽는다.
- **캐시를 두지 않는다**(10-3a §3). 메시지의 `repoEventId` 만 메시지 캐시에 함께 들어간다.
- **새 npm · Flutter 의존성을 넣지 않는다.**
- **마이그레이션이 없다.** 서버 스키마를 건드리지 않는다 — `repo_events.messageId` 가 이미 있다.
- **앱 `schemaVersion` 을 15 → 16 으로 올린다.** 빠뜨리면 `no such column` 으로 앱이 멈춘다(실제로 겪었다).
- **Riverpod 3.3.2** — `Override` 미export, `AsyncValue.value`.

**검증 명령**

| | |
|---|---|
| 서버 | `npm --prefix server run test` · `typecheck` · `npm run server:lint` |
| 계약 검증 | `npm run check:browse` (10-3a 의 33개에 더한다) |
| 앱 | `cd app && flutter analyze && flutter test` |

**`.env` 를 가짜 GitHub(4599)으로 두고 작업한다.** 10-3a 종단 확인 뒤 진짜 GitHub 을 보고 있다.

---

## File Structure

**서버**

| 파일 | 책임 |
|---|---|
| `src/repos/push-commits.ts` | **신규.** payload → 커밋 요약. **순수 함수** |
| `src/repos/repo-events.controller.ts` | **신규.** `GET /spaces/:spaceId/repo-events/:eventId` |
| `src/repos/repo-browse.service.ts` | **수정.** `commits` · `commitDetail` · `eventCommits` |
| `src/repos/repos.controller.ts` | **수정.** 커밋 라우트 둘 |
| `src/repos/webhooks.service.ts` | **수정.** 소켓 페이로드에 `repoEventId` |
| `src/messages/messages.service.ts` | **수정.** 목록에 `repoEventId` |
| `src/oauth/github-oauth.client.ts` | **수정.** `listCommits` · `getCommit` |

**`push-commits.ts` 를 가르는 이유**는 10-3a 의 `blob-content.ts` 와 같다 — payload 해석은 **네트워크 없이 테스트되는 부분**이고, 서비스에 묻으면 실제 웹훅 없이 확인할 수 없다.

**앱**

| 파일 | 책임 |
|---|---|
| `lib/domain/models/repo_browse.dart` | **수정.** `CommitSummary` · `CommitDetail` · `ChangedFile` |
| `lib/domain/models/message.dart` | **수정.** `repoEventId` |
| `lib/data/local/app_database.dart` | **수정.** 컬럼 + `schemaVersion` 16 |
| `lib/data/api/browse_api.dart` | **수정.** `eventCommits` · `commits` · `commit` |
| `lib/features/repo/commits_screen.dart` | **신규.** 커밋 목록(이벤트 · 브랜치 공용) |
| `lib/features/repo/commit_detail_screen.dart` | **신규.** 변경 파일 목록 |
| `lib/features/repo/browse_screen.dart` | **수정.** '커밋' 탭 |
| `lib/features/chat/…` | **수정.** 메시지 진입점 |
| `lib/core/router.dart` | **수정.** 라우트 둘 |

---

## Task 1: payload → 커밋 요약 (순수 함수)

**Files:**
- Create: `server/src/repos/push-commits.ts`
- Test: `server/src/repos/push-commits.spec.ts`

**Interfaces:**
- Produces:
  - `interface CommitSummary { sha: string; message: string; authorName: string | null; committedAt: string | null; changedCount: number | null }`
  - `interface PushView { ref: string | null; commits: CommitSummary[] }`
  - `readPushCommits(payload: unknown): PushView`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/repos/push-commits.spec.ts`:

```ts
import { readPushCommits } from './push-commits';

/** 실제 웹훅 payload 의 모양(실측으로 확인한 필드만). */
function pushPayload(overrides: Record<string, unknown> = {}) {
  return {
    ref: 'refs/heads/main',
    before: 'aaa',
    after: 'bbb',
    commits: [
      {
        id: 'c1',
        message: 'feat: 첫 커밋\n\n본문',
        timestamp: '2026-08-21T00:00:00Z',
        author: { name: 'dbsrjs', username: 'dbsrjs' },
        added: ['a.ts'],
        removed: [],
        modified: ['b.ts', 'c.ts'],
      },
    ],
    ...overrides,
  };
}

describe('readPushCommits', () => {
  it('커밋을 요약으로 바꾼다', () => {
    const view = readPushCommits({ raw: pushPayload() });

    expect(view.ref).toBe('main');
    expect(view.commits).toHaveLength(1);
    expect(view.commits[0].sha).toBe('c1');
    expect(view.commits[0].authorName).toBe('dbsrjs');
  });

  it('메시지를 자르지 않는다 — 상세에서 본문까지 읽혀야 한다', () => {
    const view = readPushCommits({ raw: pushPayload() });

    expect(view.commits[0].message).toBe('feat: 첫 커밋\n\n본문');
  });

  it('changedCount 는 added + removed + modified 다', () => {
    const view = readPushCommits({ raw: pushPayload() });

    expect(view.commits[0].changedCount).toBe(3);
  });

  it('refs/heads/ 를 벗긴다', () => {
    const view = readPushCommits({ raw: pushPayload({ ref: 'refs/heads/feat/x' }) });

    expect(view.ref).toBe('feat/x');
  });

  it('push 가 아닌 payload 는 빈 목록이다', () => {
    // PR · 이슈 · 릴리스가 여기로 온다. 던지지 않는다.
    const view = readPushCommits({ raw: { action: 'opened', pull_request: {} } });

    expect(view.commits).toEqual([]);
    expect(view.ref).toBeNull();
  });

  it('모양이 깨져도 던지지 않는다', () => {
    // 남이 보내는 것이라 우리가 모양을 강제할 수 없다(10-1 과 같은 판단).
    expect(readPushCommits(null).commits).toEqual([]);
    expect(readPushCommits({ raw: { commits: 'nope' } }).commits).toEqual([]);
    expect(readPushCommits({ raw: { commits: [{ }] } }).commits).toEqual([]);
  });

  it('id 가 없는 커밋은 버린다 — sha 없이는 열 수 없다', () => {
    const view = readPushCommits({
      raw: pushPayload({ commits: [{ message: 'x' }, { id: 'c2', message: 'y' }] }),
    });

    expect(view.commits.map((c) => c.sha)).toEqual(['c2']);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
npm --prefix server run test -- push-commits
```

Expected: FAIL — `Cannot find module './push-commits'`

- [ ] **Step 3: 구현한다**

`server/src/repos/push-commits.ts`:

```ts
/**
 * `repo_events.payload` 에서 커밋을 읽는다.
 *
 * **GitHub 을 부르지 않는다.** push payload 는 커밋 목록과 각 커밋의
 * `added` · `removed` · `modified` 를 이미 들고 있다 (설계 §0) — 실측으로
 * 27개까지 잘리지 않는 것을 확인했다.
 *
 * **던지지 않는다.** 남이 보낸 것을 우리가 저장해 둔 것이라 모양을 강제할 수
 * 없다. 10-1 의 `describeGithubEvent` 와 같은 판단이다.
 */
export interface CommitSummary {
  sha: string;
  /** **자르지 않는다** — 목록은 화면이 한 줄로 자르고, 상세는 본문까지 읽는다. */
  message: string;
  authorName: string | null;
  committedAt: string | null;
  changedCount: number | null;
}

export interface PushView {
  /** `refs/heads/` 를 벗긴 브랜치. push 가 아니면 `null`. */
  ref: string | null;
  commits: CommitSummary[];
}

export function readPushCommits(payload: unknown): PushView {
  const raw = asRecord(asRecord(payload)?.raw);
  if (!raw) return { ref: null, commits: [] };

  const list = Array.isArray(raw.commits) ? raw.commits : [];
  const commits: CommitSummary[] = [];

  for (const item of list) {
    const commit = asRecord(item);
    const sha = str(commit?.id);
    // sha 가 없으면 열 수 없다 — 목록에 두면 눌러 봐야 실패하는 줄이 된다.
    if (!commit || !sha) continue;

    commits.push({
      sha,
      message: str(commit.message) ?? '',
      authorName: str(asRecord(commit.author)?.name),
      committedAt: str(commit.timestamp),
      changedCount:
        count(commit.added) + count(commit.removed) + count(commit.modified),
    });
  }

  const ref = str(raw.ref)?.replace(/^refs\/heads\//, '') ?? null;
  return { ref: commits.length > 0 ? ref : null, commits };
}

function count(value: unknown): number {
  return Array.isArray(value) ? value.length : 0;
}

function asRecord(value: unknown): Record<string, unknown> | null {
  return typeof value === 'object' && value !== null && !Array.isArray(value)
    ? (value as Record<string, unknown>)
    : null;
}

function str(value: unknown): string | null {
  return typeof value === 'string' && value.length > 0 ? value : null;
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
npm --prefix server run test -- push-commits
```

Expected: PASS (7개)

- [ ] **Step 5: 커밋**

```bash
git add server/src/repos/push-commits.ts server/src/repos/push-commits.spec.ts
git commit -m "feat(server): push payload 에서 커밋 읽기 — 10-3b"
```

---

## Task 2: 메시지에 `repoEventId`

**Files:**
- Modify: `server/src/messages/messages.service.ts`
- Modify: `server/src/repos/webhooks.service.ts`

**Interfaces:**
- Produces: 메시지 응답에 `repoEventId: string | null`

**어디에 싣나** — **채널 목록과 소켓 `message:new` 둘뿐이다.** 스레드 답글에는
봇이 쓰지 않고, 핀 목록도 시스템 메시지를 고정할 일이 없다. 필요해지면 그때
같은 헬퍼를 부른다.

- [ ] **Step 1: 목록에 붙인다**

`server/src/messages/messages.service.ts` 의 `listForChannel` 에서 `Promise.all`
배열에 하나를 더한다:

```ts
    const [reactions, quoted, mentions, attachments, repoEvents] = await Promise.all([
      this.reactions.summarizeMany(ids, member),
      this.loadQuoted(page, member),
      this.mentions.summarizeMany(ids, member),
      this.attachments.summarizeMany(ids, member),
      this.loadRepoEvents(ids, member.spaceId),
    ]);
```

`items` 조립에 한 줄:

```ts
        attachments: attachments.get(message.id) ?? [],
        // 저장소 이벤트가 만든 메시지면 그 이벤트 id. 앱은 이것이 있을 때만
        // 파고들 수 있다 (설계 §1).
        repoEventId: repoEvents.get(message.id) ?? null,
```

그리고 클래스에 메서드를 더한다:

```ts
  /**
   * 메시지 → 저장소 이벤트 id.
   *
   * **`messageId in (…)` 한 번으로 읽는다** — 메시지마다 조회하면 N+1 이 된다
   * (리액션 · 멘션과 같은 규칙).
   */
  private async loadRepoEvents(
    messageIds: string[],
    spaceId: string,
  ): Promise<Map<string, string>> {
    if (messageIds.length === 0) return new Map();

    const rows = await this.prisma.repoEvent.findMany({
      // 테넌트 조건은 예외 없이 모든 쿼리에 넣는다.
      where: { spaceId, messageId: { in: messageIds } },
      select: { id: true, messageId: true },
    });

    return new Map(
      rows.filter((r) => r.messageId).map((r) => [r.messageId as string, r.id]),
    );
  }
```

- [ ] **Step 2: 소켓 페이로드에 붙인다**

`server/src/repos/webhooks.service.ts` — 트랜잭션이 이벤트 id 도 돌려주게 고친다.

```ts
    const result = await this.prisma.$transaction(async (tx) => {
      let created: { id: string; channelId: string } | null = null;

      if (repo.linkedChannelId && author) {
        created = await tx.message.create({
          data: {
            spaceId: repo.spaceId,
            channelId: repo.linkedChannelId,
            authorId: author.id,
            body: described.body,
          },
          select: { id: true, channelId: true },
        });
      }

      const event = await tx.repoEvent.create({
        data: {
          spaceId: repo.spaceId,
          repoId: repo.id,
          type: described.type,
          payload: {
            deliveryId,
            event,
            raw: payload,
          } as Prisma.InputJsonValue,
          messageId: created?.id ?? null,
          occurredAt: new Date(),
        },
        select: { id: true },
      });

      return { message: created, eventId: event.id };
    });
```

> **주의:** 위 `data.event` 는 이 메서드의 인자 `event`(문자열)다. `const event =`
> 로 가리면 그 자리에서 자기 자신을 참조하게 된다. **`const repoEventRow =`** 로
> 이름을 달리 쓸 것.

소켓 발행부:

```ts
    if (result.message) {
      const full = await this.prisma.message.findUnique({
        where: { id: result.message.id },
        include: {
          author: { select: { id: true, name: true, avatarUrl: true } },
        },
      });
      // **목록 응답과 같은 모양이어야 한다** — 앱이 소켓으로 받은 메시지와
      // REST 로 받은 메시지를 구분하지 않는다.
      if (full) {
        this.realtime.toChannel(result.message.channelId, 'message:new', {
          ...full,
          repoEventId: result.eventId,
        });
      }
    }
```

- [ ] **Step 3: 검사**

```bash
npm --prefix server run typecheck && npm run server:lint && npm --prefix server run test
```

Expected: 통과 · **160개**(153 + Task 1 의 7개)

- [ ] **Step 4: 커밋**

```bash
git add server/src/messages/messages.service.ts server/src/repos/webhooks.service.ts
git commit -m "feat(server): 메시지에 repoEventId — 10-3b"
```

---

## Task 3: GitHub 커밋 호출

**Files:**
- Modify: `server/src/oauth/github-oauth.client.ts`

**Interfaces:**
- Produces:
  - `listCommits(cfg, token, fullName, ref, page): Promise<GithubResult<CommitSummary[]>>`
  - `getCommit(cfg, token, fullName, sha): Promise<GithubResult<{ summary: CommitSummary; files: ChangedFile[] }>>`
  - `interface ChangedFile { path: string; status: 'added' | 'removed' | 'modified' }`

- [ ] **Step 1: 타입과 호출을 더한다**

`server/src/oauth/github-oauth.client.ts` 의 `GithubContentDir` **아래**에:

```ts
export interface ChangedFile {
  path: string;
  status: 'added' | 'removed' | 'modified';
}
```

클래스 안, `getContents()` **아래**에:

```ts
  /**
   * 브랜치 이력. **파일 수를 주지 않아 `changedCount` 는 `null` 이다** —
   * 채우려고 커밋마다 상세를 부르면 스무 번 왕복이 된다 (설계 §2).
   */
  async listCommits(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    ref: string,
    page: number,
  ): Promise<GithubResult<CommitSummary[]>> {
    const query = new URLSearchParams({ per_page: '30', page: String(page) });
    if (ref) query.set('sha', ref);

    const res = await this.call<Array<Record<string, unknown>>>(
      `${cfg.apiBase}/repos/${fullName}/commits?${query.toString()}`,
      token,
    );
    if (!res.ok) return res;

    const commits: CommitSummary[] = [];
    for (const raw of res.value) {
      const summary = toCommitSummary(raw);
      if (summary) commits.push(summary);
    }
    return { ok: true, value: commits };
  }

  async getCommit(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    sha: string,
  ): Promise<GithubResult<{ summary: CommitSummary; files: ChangedFile[] }>> {
    const res = await this.call<Record<string, unknown>>(
      `${cfg.apiBase}/repos/${fullName}/commits/${encodeURIComponent(sha)}`,
      token,
    );
    if (!res.ok) return res;

    const summary = toCommitSummary(res.value);
    if (!summary) return { ok: false, status: 422 };

    const rawFiles = Array.isArray(res.value.files) ? res.value.files : [];
    const files: ChangedFile[] = [];
    for (const item of rawFiles) {
      const file = item as Record<string, unknown>;
      const path = typeof file.filename === 'string' ? file.filename : null;
      if (!path) continue;
      files.push({ path, status: toStatus(file.status) });
    }

    return { ok: true, value: { summary, files } };
  }
```

파일 아래쪽(클래스 밖)에 헬퍼 둘:

```ts
/** GitHub 커밋 JSON → 우리 요약. **payload 에서 온 것과 같은 모양이어야 한다.** */
function toCommitSummary(raw: Record<string, unknown>): CommitSummary | null {
  const sha = typeof raw.sha === 'string' ? raw.sha : null;
  if (!sha) return null;

  const commit = raw.commit as Record<string, unknown> | undefined;
  const author = commit?.author as Record<string, unknown> | undefined;

  return {
    sha,
    message: typeof commit?.message === 'string' ? commit.message : '',
    authorName: typeof author?.name === 'string' ? author.name : null,
    committedAt: typeof author?.date === 'string' ? author.date : null,
    // 목록 API 는 파일 수를 주지 않는다.
    changedCount: null,
  };
}

/** `renamed` · `copied` 등은 `modified` 로 접는다 — 화면이 할 일이 같다. */
function toStatus(value: unknown): ChangedFile['status'] {
  if (value === 'added') return 'added';
  if (value === 'removed') return 'removed';
  return 'modified';
}
```

파일 위 import 에 `CommitSummary` 를 더한다:

```ts
import type { CommitSummary } from '../repos/push-commits';
```

> **의존 방향에 주의.** `oauth` 가 `repos` 의 타입을 가져온다. 타입만 가져오므로
> (`import type`) 런타임 의존은 생기지 않고, **커밋 요약의 모양이 한 곳에만
> 있다**는 이점이 그 대가보다 크다.

- [ ] **Step 2: 검사하고 커밋**

```bash
npm --prefix server run typecheck && npm run server:lint
git add server/src/oauth/github-oauth.client.ts
git commit -m "feat(server): GitHub 커밋 호출 — 10-3b"
```

---

## Task 4: 라우트 셋

**Files:**
- Create: `server/src/repos/repo-events.controller.ts`
- Modify: `server/src/repos/repo-browse.service.ts`
- Modify: `server/src/repos/repos.controller.ts`
- Modify: `server/src/repos/repos.module.ts`

**Interfaces:**
- Produces:
  - `GET /api/spaces/:spaceId/repo-events/:eventId`
  - `GET /api/spaces/:spaceId/repos/:repoId/commits?ref=&cursor=`
  - `GET /api/spaces/:spaceId/repos/:repoId/commits/:sha`

- [ ] **Step 1: 서비스에 셋을 더한다**

`server/src/repos/repo-browse.service.ts` 의 `blob()` **아래**에:

```ts
  /**
   * push 이벤트의 커밋들. **GitHub 을 부르지 않는다** — payload 에 이미 있다
   * (설계 §0). 토큰도 필요 없어 연결이 없어도 열린다.
   */
  async eventCommits(spaceId: string, eventId: string) {
    const event = await this.prisma.repoEvent.findFirst({
      where: { id: eventId, spaceId },
      select: { type: true, payload: true, repo: { select: { id: true, fullPath: true } } },
    });
    if (!event) throw new NotFoundException('이벤트를 찾을 수 없습니다');

    const view = readPushCommits(event.payload);
    return {
      type: event.type,
      repoId: event.repo.id,
      repoFullPath: event.repo.fullPath,
      ref: view.ref,
      commits: view.commits,
    };
  }

  /** 브랜치 이력. 커서는 GitHub 의 page 번호를 그대로 쓴다. */
  async commits(
    spaceId: string,
    userId: string,
    repoId: string,
    ref: string,
    cursor: string,
  ) {
    const { repo, cfg, token } = await this.ready(spaceId, userId, repoId);
    const useRef = ref || repo.defaultBranch || '';
    const page = Number(cursor) > 0 ? Number(cursor) : 1;

    const res = await this.github.listCommits(cfg, token, repo.fullPath, useRef, page);
    if (!res.ok) throw toHttpError(res.status, res.retryAfter);

    return {
      ref: useRef,
      commits: res.value,
      // per_page=30 이 꽉 찼으면 다음 장이 있다고 본다(10-2b 의 저장소 목록과 같다).
      nextCursor: res.value.length === 30 ? String(page + 1) : null,
    };
  }

  async commitDetail(spaceId: string, userId: string, repoId: string, sha: string) {
    const { repo, cfg, token } = await this.ready(spaceId, userId, repoId);

    const res = await this.github.getCommit(cfg, token, repo.fullPath, sha);
    if (!res.ok) throw toHttpError(res.status, res.retryAfter);

    return { ...res.value.summary, files: res.value.files };
  }
```

import 에 `readPushCommits` 를 더한다:

```ts
import { readPushCommits } from './push-commits';
```

- [ ] **Step 2: 이벤트 컨트롤러를 쓴다**

`server/src/repos/repo-events.controller.ts`:

```ts
import { Controller, Get, Param, ParseUUIDPipe, UseGuards } from '@nestjs/common';
import { SpaceGuard } from '../spaces/guards/space.guard';
import { RepoBrowseService } from './repo-browse.service';

/**
 * 저장소 이벤트 하나. **저장소 밑이 아니다** — 앱이 메시지에서 얻는 것은
 * `eventId` 하나이고, 그것으로 `repoId` 를 찾는 것은 서버의 일이다 (설계 §2).
 *
 * `SpaceRoleGuard` 를 걸지 않는다 — 읽기다.
 */
@Controller('spaces/:spaceId/repo-events')
@UseGuards(SpaceGuard)
export class RepoEventsController {
  constructor(private readonly browse: RepoBrowseService) {}

  @Get(':eventId')
  detail(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('eventId', new ParseUUIDPipe()) eventId: string,
  ) {
    return this.browse.eventCommits(spaceId, eventId);
  }
}
```

- [ ] **Step 3: 커밋 라우트 둘을 더한다**

`server/src/repos/repos.controller.ts` — `blob()` **아래**에:

```ts
  /** 브랜치 이력. `ref` 를 비우면 기본 브랜치다. */
  @Get(':repoId/commits')
  commits(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('repoId', new ParseUUIDPipe()) repoId: string,
    @CurrentUser('id') userId: string,
    @Query('ref') ref = '',
    @Query('cursor') cursor = '',
  ) {
    return this.browse.commits(spaceId, userId, repoId, ref, cursor);
  }

  /** 커밋 하나 — 메시지와 **바뀐 파일 목록**. diff 본문은 주지 않는다. */
  @Get(':repoId/commits/:sha')
  commit(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('repoId', new ParseUUIDPipe()) repoId: string,
    @Param('sha') sha: string,
    @CurrentUser('id') userId: string,
  ) {
    return this.browse.commitDetail(spaceId, userId, repoId, sha);
  }
```

> **`:sha` 에 `ParseUUIDPipe` 를 걸지 않는다.** sha 는 uuid 가 아니다.

- [ ] **Step 4: 모듈에 등록하고 검사**

`repos.module.ts` 의 `controllers` 에 `RepoEventsController` 를 더한다.

```bash
npm --prefix server run typecheck && npm run server:lint && npm --prefix server run test
```

- [ ] **Step 5: 커밋**

```bash
git add server/src/repos
git commit -m "feat(server): 커밋 목록·상세와 이벤트 커밋 — 10-3b"
```

---

## Task 5: 계약 검증

**Files:**
- Modify: `server/scripts/check-browse.mjs`
- Modify: `CLAUDE.md` (개수)

- [ ] **Step 1: 가짜 GitHub 에 커밋 엔드포인트와 호출 카운터를 더한다**

`check-browse.mjs` 의 `let rateLimited = false;` 위에:

```js
/** **가짜 GitHub 이 받은 요청 수.** "GitHub 을 부르지 않는다"를 확인하는 데 쓴다. */
let apiHits = 0;

const COMMIT = {
  sha: 'abc123def456',
  commit: {
    message: 'fix: 무언가를 고친다\n\n본문',
    author: { name: 'dbsrjs', date: '2026-08-21T00:00:00Z' },
  },
  files: [
    { filename: 'a.ts', status: 'added' },
    { filename: 'b.ts', status: 'removed' },
    { filename: 'c.ts', status: 'renamed' },
  ],
};
```

`fake` 핸들러 맨 위(`const url = …` 다음)에:

```js
  apiHits++;
```

`contents` 분기 **아래**에 커밋 분기 둘:

```js
  const oneCommit = url.pathname.match(/^\/repos\/([^/]+\/[^/]+)\/commits\/(.+)$/);
  if (req.method === 'GET' && oneCommit) {
    if (decodeURIComponent(oneCommit[2]) !== COMMIT.sha) {
      json(404, { message: 'No commit found' });
      return;
    }
    json(200, COMMIT);
    return;
  }

  if (req.method === 'GET' && url.pathname === `/repos/${REPO.full_name}/commits`) {
    const page = Number(url.searchParams.get('page') ?? '1');
    json(200, page === 1 ? [COMMIT] : []);
    return;
  }
```

> **순서에 주의.** `/commits/:sha` 를 `/commits` 보다 먼저 봐야 한다 — 뒤에 두면
> 목록 분기가 sha 요청까지 삼킨다.

- [ ] **Step 2: 케이스를 더한다**

`main()` 의 격리 확인 **앞**에:

```js
  // ── 커밋 목록 · 상세 (10-3b) ─────────────────────────
  const commits = await api('GET', at('commits'), { token: alice.token });
  check('커밋 목록 200', commits.status === 200, String(commits.status));
  check('한 건이 온다', commits.json?.commits?.length === 1);
  check('sha 가 온다', commits.json?.commits?.[0]?.sha === COMMIT.sha);
  check('메시지를 자르지 않는다', commits.json?.commits?.[0]?.message?.includes('본문'));
  check('목록에는 changedCount 가 없다', commits.json?.commits?.[0]?.changedCount === null);
  check('마지막 장이면 nextCursor 가 null', commits.json?.nextCursor === null);

  const page2 = await api('GET', at('commits', '?cursor=2'), { token: alice.token });
  check('2페이지는 빈 목록', page2.json?.commits?.length === 0);

  const detail = await api('GET', at(`commits/${COMMIT.sha}`), { token: alice.token });
  check('커밋 상세 200', detail.status === 200, String(detail.status));
  check('파일 셋', detail.json?.files?.length === 3);
  check(
    'status 세 값 — renamed 는 modified 로 접힌다',
    JSON.stringify(detail.json?.files?.map((f) => f.status)) ===
      JSON.stringify(['added', 'removed', 'modified']),
    JSON.stringify(detail.json?.files),
  );
  const missing = await api('GET', at('commits/deadbeef'), { token: alice.token });
  check('없는 커밋은 404', missing.status === 404, String(missing.status));

  // ── 이벤트 커밋 — **GitHub 을 부르지 않는다** ────────
  // 웹훅을 직접 쏴서 메시지와 이벤트를 만든다.
  const channels = await api('GET', `/spaces/${spaceId}/channels`, { token: alice.token });
  const channelId = channels.json?.[0]?.id;
  await api('PATCH', at('').replace(/\/$/, ''), {
    token: alice.token,
    body: { linkedChannelId: channelId },
  }).catch(() => {});
  // 저장소에 채널을 붙이는 API 가 없으면 connect 로 다시 붙인다.
  await api('POST', `/spaces/${spaceId}/repos/connect`, {
    token: alice.token,
    body: { githubRepoId: REPO.id, linkedChannelId: channelId },
  });

  const pushBody = {
    ref: 'refs/heads/main',
    before: 'aaa',
    after: 'bbb',
    pusher: { name: 'dbsrjs' },
    commits: [
      {
        id: COMMIT.sha,
        message: 'fix: 무언가를 고친다',
        timestamp: '2026-08-21T00:00:00Z',
        author: { name: 'dbsrjs' },
        added: ['a.ts'],
        removed: [],
        modified: ['b.ts'],
      },
    ],
  };
  const secret = (await api('POST', at('secret'), { token: alice.token })).json?.webhookSecret;
  const rawBody = JSON.stringify(pushBody);
  const sig =
    'sha256=' +
    (await import('node:crypto'))
      .createHmac('sha256', secret)
      .update(rawBody)
      .digest('hex');
  const hookRes = await fetch(`${BASE}/webhooks/github/${repoId}`, {
    method: 'POST',
    headers: {
      'content-type': 'application/json',
      'x-github-event': 'push',
      'x-github-delivery': `d-${stamp}`,
      'x-hub-signature-256': sig,
    },
    body: rawBody,
  });
  check('웹훅 수신 200', hookRes.status === 200, String(hookRes.status));

  const msgs = await api('GET', `/channels/${channelId}/messages`, { token: alice.token });
  const botMsg = msgs.json?.items?.find((m) => m.repoEventId);
  check('메시지에 repoEventId 가 실린다', !!botMsg, JSON.stringify(msgs.json?.items?.[0]));

  const before = apiHits;
  const evt = await api('GET', `/spaces/${spaceId}/repo-events/${botMsg.repoEventId}`, {
    token: alice.token,
  });
  check('이벤트 200', evt.status === 200, String(evt.status));
  check('커밋이 온다', evt.json?.commits?.length === 1);
  check('changedCount 가 payload 에서 채워진다', evt.json?.commits?.[0]?.changedCount === 2);
  check('ref 에서 refs/heads/ 가 벗겨진다', evt.json?.ref === 'main');
  // **이 조각의 핵심 판단이라 말로만 두지 않는다** (설계 §4).
  check('GitHub 을 부르지 않았다', apiHits === before, `${before} → ${apiHits}`);

  const foreignEvt = await api(
    'GET',
    `/spaces/${spaceId}/repo-events/${botMsg.repoEventId}`,
    { token: bob.token },
  );
  check('남의 스페이스 이벤트는 404', foreignEvt.status === 404, String(foreignEvt.status));
```

> **메시지 목록 라우트를 확인할 것.** 위 `/channels/:id/messages` 는 실제 경로와
> 다를 수 있다 — `server/src/messages/messages.controller.ts` 를 읽고 맞춘다.

- [ ] **Step 3: 돌린다**

```bash
npm run check:browse
```

Expected: **통과 53 · 실패 0** (33 + 20)

- [ ] **Step 4: 개수를 고치고 커밋**

`CLAUDE.md` 의 `check:browse` 줄에서 33 을 새 개수로 고친다.

```bash
git add server/scripts/check-browse.mjs CLAUDE.md
git commit -m "test(server): 커밋 열람 계약 검증 — 10-3b"
```

---

## Task 6: 앱 — 모델과 캐시

**Files:**
- Modify: `app/lib/domain/models/repo_browse.dart`
- Modify: `app/lib/domain/models/message.dart`
- Modify: `app/lib/data/local/app_database.dart`
- Modify: `app/lib/data/api/browse_api.dart`
- Modify: 메시지를 캐시에 넣는 곳(`data/repositories/message_repository.dart` 등)
- Test: `app/test/commit_model_test.dart`

**Interfaces:**
- Produces: `CommitSummary` · `ChangedFile` · `CommitDetail` · `PushEventView`, `Message.repoEventId`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`app/test/commit_model_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:nexus_app/domain/models/repo_browse.dart';

void main() {
  test('커밋 요약을 읽는다', () {
    final c = CommitSummary.fromJson(const {
      'sha': 'abc123def456',
      'message': 'fix: 고친다\n\n본문',
      'authorName': 'dbsrjs',
      'committedAt': '2026-08-21T00:00:00.000Z',
      'changedCount': 2,
    });

    expect(c.sha, 'abc123def456');
    expect(c.changedCount, 2);
  });

  test('짧은 sha 는 앞 7자다', () {
    const c = CommitSummary(sha: 'abc123def456', message: 'x');

    expect(c.shortSha, 'abc123d');
  });

  test('제목은 첫 줄이다 — 목록이 문단이 되면 안 된다', () {
    const c = CommitSummary(sha: 'a', message: 'fix: 고친다\n\n본문이 길다');

    expect(c.title, 'fix: 고친다');
  });

  test('changedCount 가 없으면 null 이다 — 0 과 다르다', () {
    // 브랜치 이력은 파일 수를 모른다. 0 으로 두면 "안 바뀐 커밋"으로 읽힌다.
    const c = CommitSummary(sha: 'a', message: 'x');

    expect(c.changedCount, isNull);
  });

  test('변경 파일의 status 세 값', () {
    const added = ChangedFile(path: 'a.ts', status: 'added');
    const removed = ChangedFile(path: 'b.ts', status: 'removed');
    const modified = ChangedFile(path: 'c.ts', status: 'modified');

    final labels = {added.statusLabel, removed.statusLabel, modified.statusLabel};
    expect(labels.length, 3);
  });
}
```

- [ ] **Step 2: 실패를 확인하고 모델을 더한다**

```bash
cd app && flutter test test/commit_model_test.dart
```

`app/lib/domain/models/repo_browse.dart` 끝에:

```dart
/// 커밋 한 줄. **payload 에서 온 것과 GitHub 에서 온 것이 같은 모양이다** —
/// 다르게 보이면 사용자가 두 목록을 다른 것으로 읽는다 (설계 §2).
@freezed
abstract class CommitSummary with _$CommitSummary {
  const factory CommitSummary({
    required String sha,
    required String message,
    String? authorName,
    DateTime? committedAt,

    /// 바뀐 파일 수. **브랜치 이력에서는 `null`** 이다 — GitHub 목록 API 가
    /// 주지 않는다. 0 으로 두면 "안 바뀐 커밋"으로 읽힌다.
    int? changedCount,
  }) = _CommitSummary;

  const CommitSummary._();

  factory CommitSummary.fromJson(Map<String, dynamic> json) =>
      _$CommitSummaryFromJson(json);

  String get shortSha => sha.length <= 7 ? sha : sha.substring(0, 7);

  /// 제목 줄만. 본문까지 넣으면 목록이 문단이 된다(10-1 과 같은 규칙).
  String get title => message.split('\n').first.trim();
}

@freezed
abstract class ChangedFile with _$ChangedFile {
  const factory ChangedFile({
    required String path,
    required String status,
  }) = _ChangedFile;

  const ChangedFile._();

  factory ChangedFile.fromJson(Map<String, dynamic> json) =>
      _$ChangedFileFromJson(json);

  String get statusLabel => switch (status) {
        'added' => '추가',
        'removed' => '삭제',
        _ => '수정',
      };
}

@freezed
abstract class CommitDetail with _$CommitDetail {
  const factory CommitDetail({
    required String sha,
    required String message,
    String? authorName,
    DateTime? committedAt,
    @Default(<ChangedFile>[]) List<ChangedFile> files,
  }) = _CommitDetail;

  const CommitDetail._();

  factory CommitDetail.fromJson(Map<String, dynamic> json) =>
      _$CommitDetailFromJson(json);

  String get shortSha => sha.length <= 7 ? sha : sha.substring(0, 7);
  String get title => message.split('\n').first.trim();

  /// 제목 아래 본문. 없으면 빈 문자열이다.
  String get bodyText => message.split('\n').skip(1).join('\n').trim();
}

/// `repo-events/:eventId` 응답.
@freezed
abstract class PushEventView with _$PushEventView {
  const factory PushEventView({
    required String type,
    required String repoId,
    required String repoFullPath,
    String? ref,
    @Default(<CommitSummary>[]) List<CommitSummary> commits,
  }) = _PushEventView;

  factory PushEventView.fromJson(Map<String, dynamic> json) =>
      _$PushEventViewFromJson(json);
}
```

- [ ] **Step 3: 메시지에 `repoEventId` 를 더한다**

`app/lib/domain/models/message.dart` 의 `pinned` 아래에:

```dart
    /// 저장소 이벤트가 만든 메시지면 그 이벤트 id. **있으면 커밋을 볼 수
    /// 있다**(10-3b). 사람이 쓴 메시지에는 없다.
    String? repoEventId,
```

`app/lib/data/local/app_database.dart` 의 `CachedMessages` 에서 `attachments`
아래에:

```dart
  /// 저장소 이벤트 id. 있으면 그 push 의 커밋을 열 수 있다(10-3b).
  TextColumn get repoEventId => text().nullable()();
```

그리고 **`schemaVersion` 을 16 으로 올린다.** 빠뜨리면 `no such column` 으로
앱이 멈춘다.

메시지를 캐시에 쓰는 곳과 캐시에서 읽어 모델로 만드는 곳에 이 필드를 잇는다
(`message_repository.dart` 에서 `CachedMessage` ↔ `Message` 를 옮기는 자리).

- [ ] **Step 4: API 를 더한다**

`app/lib/data/api/browse_api.dart` 에:

```dart
  /// 그 push 의 커밋들. **서버가 GitHub 을 부르지 않는다**(설계 §0).
  Future<PushEventView> eventCommits(String spaceId, String eventId) async {
    try {
      final res = await _client.dio.get<Map<String, dynamic>>(
        '/spaces/$spaceId/repo-events/$eventId',
      );
      return PushEventView.fromJson(res.data ?? const {});
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }

  Future<({List<CommitSummary> commits, String? nextCursor})> commits(
    String spaceId,
    String repoId, {
    required String ref,
    String cursor = '',
  }) async {
    try {
      final res = await _client.dio.get<Map<String, dynamic>>(
        '/spaces/$spaceId/repos/$repoId/commits',
        queryParameters: {'ref': ref, 'cursor': cursor},
      );
      final raw = (res.data?['commits'] as List<dynamic>? ?? const [])
          .cast<Map<String, dynamic>>();
      return (
        commits: raw.map(CommitSummary.fromJson).toList(growable: false),
        nextCursor: res.data?['nextCursor'] as String?,
      );
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }

  Future<CommitDetail> commit(String spaceId, String repoId, String sha) async {
    try {
      final res = await _client.dio.get<Map<String, dynamic>>(
        '/spaces/$spaceId/repos/$repoId/commits/$sha',
      );
      return CommitDetail.fromJson(res.data ?? const {});
    } on DioException catch (e) {
      throw ApiException(classifyDioException(e));
    }
  }
```

- [ ] **Step 5: 코드 생성 · 검사 · 커밋**

```bash
cd app && dart run build_runner build --delete-conflicting-outputs
cd app && flutter analyze && flutter test
```

Expected: **153개**(148 + 5)

```bash
git add app/lib app/test/commit_model_test.dart
git commit -m "feat(app): 커밋 모델과 메시지 repoEventId — 10-3b"
```

---

## Task 7: 앱 — 화면 둘과 진입점

**Files:**
- Create: `app/lib/features/repo/commits_screen.dart`
- Create: `app/lib/features/repo/commit_detail_screen.dart`
- Modify: `app/lib/features/repo/browse_screen.dart` ('커밋' 탭)
- Modify: `app/lib/core/router.dart`
- Modify: 채널 메시지 위젯(진입점)
- Test: `app/test/commits_screen_test.dart`

- [ ] **Step 1: 실패하는 위젯 테스트를 쓴다**

`app/test/commits_screen_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:nexus_app/domain/models/repo_browse.dart';
import 'package:nexus_app/features/repo/commit_detail_screen.dart';
import 'package:nexus_app/features/repo/commits_screen.dart';

void main() {
  testWidgets('커밋 목록이 제목과 짧은 sha 를 보여 준다', (tester) async {
    await tester.pumpWidget(ProviderScope(
      child: MaterialApp(
        home: Scaffold(
          body: CommitList(
            commits: const [
              CommitSummary(
                sha: 'abc123def456',
                message: 'fix: 고친다\n\n본문',
                authorName: 'dbsrjs',
                changedCount: 2,
              ),
            ],
            onTap: (_) {},
          ),
        ),
      ),
    ));
    await tester.pumpAndSettle();

    expect(find.text('fix: 고친다'), findsOneWidget);
    expect(find.textContaining('abc123d'), findsOneWidget);
    // 본문은 목록에 나오지 않는다 — 목록이 문단이 되면 안 된다.
    expect(find.textContaining('본문'), findsNothing);
  });

  testWidgets('변경 파일이 상태와 함께 보인다', (tester) async {
    await tester.pumpWidget(ProviderScope(
      child: MaterialApp(
        home: Scaffold(
          body: ChangedFileList(
            files: const [
              ChangedFile(path: 'a.ts', status: 'added'),
              ChangedFile(path: 'b.ts', status: 'removed'),
            ],
            onTap: (_) {},
          ),
        ),
      ),
    ));
    await tester.pumpAndSettle();

    expect(find.text('a.ts'), findsOneWidget);
    expect(find.text('추가'), findsOneWidget);
    expect(find.text('삭제'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 화면을 쓴다**

`commits_screen.dart` 에 **재사용 위젯 `CommitList`** 와 화면을 함께 둔다 —
이벤트 커밋과 브랜치 이력이 같은 목록을 쓴다(설계 §2). 화면은
`eventId` 또는 `repoId+ref` 중 하나를 받아 적재원을 고른다.

`commit_detail_screen.dart` 에 `ChangedFileList` 와 화면. 파일을 누르면
**10-3a 의 파일 보기를 그 sha 로 연다** — `/s/:spaceId/repos/:repoId/browse` 로
가되 `ref` 를 sha 로, `path` 를 그 파일로 주는 초기 상태가 필요하다.
`BrowseScreen` 에 `initialRef` · `initialPath` 선택 인자를 더한다.

- [ ] **Step 3: 라우트와 진입점**

`router.dart`:

```dart
          GoRoute(
            path: 'repo-events/:eventId',
            builder: (_, state) => CommitsScreen(
              spaceId: state.pathParameters['spaceId']!,
              eventId: state.pathParameters['eventId'],
            ),
          ),
          GoRoute(
            path: 'repos/:repoId/commits/:sha',
            builder: (_, state) => CommitDetailScreen(
              spaceId: state.pathParameters['spaceId']!,
              repoId: state.pathParameters['repoId']!,
              sha: state.pathParameters['sha']!,
            ),
          ),
```

채널 메시지 위젯에서 `message.repoEventId != null` 일 때만 누를 수 있게 하고,
누르면 `/s/${spaceId}/repo-events/${message.repoEventId}` 로 간다.

- [ ] **Step 4: 검사하고 커밋**

```bash
cd app && flutter analyze && flutter test
```

Expected: **155개**(153 + 2)

```bash
git add app/lib app/test/commits_screen_test.dart
git commit -m "feat(app): 커밋 목록·상세 화면 — 10-3b"
```

---

## Task 8: 종단 확인과 문서

- [ ] **Step 1: `.env` 를 진짜 GitHub 으로 되돌리고 서버 재시작**

```
GITHUB_OAUTH_BASE=
GITHUB_API_BASE=
```

- [ ] **Step 2: 사람이 본다**

1. 채널의 "커밋 N개를 올렸습니다"를 누르면 그 커밋들이 뜬다
2. 사람이 쓴 메시지는 눌러도 아무 일이 없다
3. 커밋을 누르면 **바뀐 파일 목록**이 뜨고 추가/수정/삭제가 구분된다
4. 파일을 누르면 **그 시점의 전문**이 열린다
5. 탐색 화면의 '커밋' 탭에 브랜치 이력이 뜨고 "더 불러오기"가 동작한다

- [ ] **Step 3: 문서 · 커밋**

`CLAUDE.md` §4 에 `### 10-3b 완료` 절(확인한 것/못한 것을 나눠서), §5 표,
`docs/전환-계획.md` 를 고친다.

```bash
git add CLAUDE.md docs/전환-계획.md
git commit -m "docs: 10-3b 완료를 반영한다"
```

---

## 자체 점검 결과

| 스펙 절 | 덮는 Task |
|---|---|
| §0 payload 에서 읽기 · 상한 방어 안 함 | Task 1 |
| §1 `repoEventId` · N+1 안 만들기 | Task 2 |
| §2 API 계약 · 같은 커밋 모양 · `changedCount` null | Task 3 · 4 · 6 |
| §3 화면 둘 · 진입점 둘 · 파일은 blob 재사용 | Task 7 |
| §4 검증 · **GitHub 을 부르지 않음** | Task 5 |

**계획에서 정한 것 둘.**

1. **`CommitSummary` 타입이 `repos/push-commits.ts` 에 있고 `oauth` 가 `import type` 으로 가져온다.** 의존 방향이 뒤집히지만 타입만이라 런타임 의존은 없고, **커밋 요약의 모양이 한 곳에만 있다**는 이점이 크다.
2. **`BrowseScreen` 에 `initialRef` · `initialPath` 를 더한다.** 커밋 상세에서 파일을 열려면 그 sha·경로로 시작해야 한다.
