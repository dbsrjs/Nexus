# 11단계 PR 열람 구현 계획

> **에이전트에게:** 이 계획은 `superpowers:subagent-driven-development`(권장) 또는
> `superpowers:executing-plans` 로 태스크 단위로 실행한다. 단계는 체크박스
> (`- [ ]`)로 추적한다.

**목표:** 저장소에 붙은 GitHub PR 을 앱에서 목록 → 상세 → 바뀐 파일 → 그 시점
전문까지 읽는다. 쓰기는 없다.

**접근:** DB 사본을 두지 않고 GitHub 을 프록시한다. diff 렌더러를 만들지 않고,
파일을 누르면 10-3a 의 파일 보기가 head 브랜치로 열린다. 채널의 PR 이벤트
메시지에서도 들어갈 수 있게 `repo-events` 응답을 이벤트 종류로 가른다.

**기술 스택:** NestJS · Prisma · Jest / Flutter · Riverpod · freezed · go_router

**스펙:** [docs/superpowers/specs/2026-08-30-PR-열람-design.md](../specs/2026-08-30-PR-열람-design.md)

## 전역 제약

- **테넌트 격리** — 모든 조회는 `spaceId` 로 좁힌다. 볼 수 없으면 403 이 아니라 **404**
- **읽기에 `@MinRole` 을 걸지 않는다** — `SpaceGuard` 만. 10-3a 의 판단
- **GitHub 원본을 그대로 흘리지 않는다** — `_links` · `diff_url` · `patch_url` ·
  `patch` 를 뺀다. **`html_url` 은 싣는다**(공개 주소다)
- **캐시하지 않는다** — 앱 `schemaVersion` 을 올리지 않는다
- **주석 · 커밋 메시지 · 문서는 한국어**. 커밋에 `Co-Authored-By` 를 넣지 않는다
- **`.freezed.dart` · `.g.dart` 는 커밋한다.** 모델을 고치면
  `cd app && dart run build_runner build` 를 돌린다
- 파일 목록 상한 **300**, 넘으면 `truncated: true`
- 429 는 `retryAfter` 를 **응답 본문 객체에** 담아 던진다 — 전역 필터
  (`common/filters/http-exception.filter.ts:41`)가 그것을 `Retry-After`
  **헤더로** 옮긴다. 헤더를 직접 세팅하지 않는다

---

## Task 1: 공용 배관 추출 — `toGithubHttpError` · `RepoAccessService`

지금 `toHttpError` 가 `github-repos.controller.ts:76` 와 `repo-browse.service.ts:191`
에 **똑같이 두 벌** 있다. PR 서비스가 세 번째 사본을 만들기 전에 모은다.
`ready()`(저장소 · 설정 · 토큰 확인)도 같은 이유로 뺀다.

**Files:**
- Create: `server/src/repos/github-error.ts`
- Create: `server/src/repos/github-error.spec.ts`
- Create: `server/src/repos/repo-access.service.ts`
- Modify: `server/src/repos/repo-browse.service.ts` (자기 `toHttpError` · `ready()` 삭제 후 주입)
- Modify: `server/src/repos/github-repos.controller.ts` (자기 `toHttpError` 삭제 후 import)
- Modify: `server/src/repos/repos.module.ts` (`RepoAccessService` 를 providers 에)

**Interfaces:**
- Produces:
  - `toGithubHttpError(status: number, retryAfter?: number): HttpException`
  - `class RepoAccessService { ready(spaceId: string, userId: string, repoId: string): Promise<{ repo: { fullPath: string; defaultBranch: string | null }; cfg: GithubOauthConfig; token: string }> }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/repos/github-error.spec.ts`:

```ts
import { HttpStatus } from '@nestjs/common';
import { toGithubHttpError } from './github-error';

describe('toGithubHttpError', () => {
  it('429 는 retryAfter 를 본문 객체에 담는다 — 전역 필터가 헤더로 옮긴다', () => {
    const err = toGithubHttpError(429, 60);
    expect(err.getStatus()).toBe(HttpStatus.TOO_MANY_REQUESTS);
    expect(err.getResponse()).toMatchObject({ retryAfter: 60 });
  });

  it('Retry-After 가 없으면 null 이다 — 없는 것과 0 초는 다르다', () => {
    expect(toGithubHttpError(429).getResponse()).toMatchObject({ retryAfter: null });
  });

  it('404 는 그대로 전달한다', () => {
    expect(toGithubHttpError(404).getStatus()).toBe(HttpStatus.NOT_FOUND);
  });

  it('401 은 다시 연결하라고 말한다', () => {
    expect(toGithubHttpError(401).getStatus()).toBe(HttpStatus.UNAUTHORIZED);
  });

  it('그 밖은 502 다 — 우리 잘못이 아니라 GitHub 에서 못 받은 것이다', () => {
    expect(toGithubHttpError(500).getStatus()).toBe(HttpStatus.BAD_GATEWAY);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
npm --prefix server test -- github-error
```

기대: `Cannot find module './github-error'`

- [ ] **Step 3: `github-error.ts` 를 만든다**

`repo-browse.service.ts:191` 의 `toHttpError` 본문을 **그대로** 옮기고 이름만
`toGithubHttpError` 로 바꾼다. 주석도 함께 옮긴다.

```ts
import { HttpException, HttpStatus } from '@nestjs/common';

/**
 * GitHub 의 실패를 우리 응답으로 옮긴다.
 *
 * **`retryAfter` 를 본문 객체에 담는다.** 전역 예외 필터가 그것을 읽어
 * `Retry-After` 헤더로 내보낸다 — 필터는 본문을 일정한 봉투로 다시 빚어
 * 커스텀 필드를 버리기 때문이다(10-3a 에서 잡은 결함).
 *
 * **404 는 그대로 전달한다** — 없는 브랜치 · 없는 경로 · 없는 PR 번호가 여기로
 * 온다. 저장소 자체가 없는 것과 구분되지 않지만, 앱이 할 일은 어느 쪽이든 같다.
 */
export function toGithubHttpError(status: number, retryAfter?: number): HttpException {
  if (status === 429) {
    return new HttpException(
      {
        statusCode: HttpStatus.TOO_MANY_REQUESTS,
        message: 'GitHub 요청 한도를 넘었습니다. 잠시 뒤 다시 시도해 주세요.',
        retryAfter: retryAfter ?? null,
      },
      HttpStatus.TOO_MANY_REQUESTS,
    );
  }
  if (status === 404) return new HttpException('찾을 수 없습니다', HttpStatus.NOT_FOUND);
  if (status === 401) {
    return new HttpException(
      'GitHub 연결이 만료되었습니다. 다시 연결해 주세요.',
      HttpStatus.UNAUTHORIZED,
    );
  }
  return new HttpException('GitHub 에서 받지 못했습니다.', HttpStatus.BAD_GATEWAY);
}
```

- [ ] **Step 4: 테스트가 통과하는지 본다**

```bash
npm --prefix server test -- github-error
```

- [ ] **Step 5: `RepoAccessService` 를 만든다**

`repo-browse.service.ts` 의 `private async ready(...)` 를 **그대로** 옮긴다.

```ts
import {
  BadRequestException,
  Injectable,
  NotFoundException,
  ServiceUnavailableException,
} from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { RepoProvider } from '@prisma/client';
import { PrismaService } from '../prisma/prisma.service';
import { resolveGithubOauth } from '../config/oauth.config';
import { OauthService } from '../oauth/oauth.service';

/**
 * 저장소 · GitHub 설정 · 토큰이 모두 있어야 열람이 된다.
 *
 * 열람 서비스가 둘(`RepoBrowseService` · `PullsService`)이 되어 여기로 모았다.
 * **토큰은 그 순간 요청한 사람의 것**을 쓴다(10-2b) — 등록자를 기록해 두면
 * 그 사람이 연결을 해제한 순간 아무도 못 보게 된다.
 */
@Injectable()
export class RepoAccessService {
  constructor(
    private readonly prisma: PrismaService,
    private readonly config: ConfigService,
    private readonly oauth: OauthService,
  ) {}

  async ready(spaceId: string, userId: string, repoId: string) {
    const repo = await this.prisma.repo.findFirst({
      where: { id: repoId, spaceId, provider: RepoProvider.github },
      select: { fullPath: true, defaultBranch: true },
    });
    // 403 이 아니라 404 다 — 403 은 "그 저장소가 존재한다"를 알려 준다.
    if (!repo) throw new NotFoundException('저장소를 찾을 수 없습니다');

    const cfg = resolveGithubOauth(this.config);
    if (!cfg) {
      throw new ServiceUnavailableException(
        'GitHub 연결이 설정되지 않았습니다. 서버 관리자가 .env 를 채워야 합니다.',
      );
    }

    const token = await this.oauth.githubTokenFor(userId);
    if (!token) throw new BadRequestException('GitHub 계정을 먼저 연결해야 합니다');

    return { repo, cfg, token };
  }
}
```

- [ ] **Step 6: 두 호출부를 갈아 끼운다**

`repo-browse.service.ts` — `toHttpError` 함수와 `private ready()` 를 지우고,
생성자에 `private readonly access: RepoAccessService` 를 주입한다.
`this.ready(...)` → `this.access.ready(...)`, `toHttpError(...)` →
`toGithubHttpError(...)`. `ConfigService` · `OauthService` 주입이 더 이상
쓰이지 않으면 지운다(`eventCommits` — Task 6 에서 `eventDetail` 이 된다 — 은 둘 다 안 쓴다).

`github-repos.controller.ts` — 파일 아래의 `toHttpError` 를 지우고
`import { toGithubHttpError } from './github-error';` 로 바꾼다.

`repos.module.ts` — `providers` 에 `RepoAccessService` 를 더한다.

- [ ] **Step 7: 서버 전체 테스트와 타입 검사**

```bash
npm --prefix server run typecheck
npm --prefix server test
```

기대: 기존 160개가 그대로 통과 + `github-error` 5개.

- [ ] **Step 8: 커밋**

```bash
git add server/src/repos
git commit -m "refactor(server): GitHub 오류 변환과 열람 준비를 한 곳으로 모은다"
```

커밋 본문:

```
toHttpError 가 컨트롤러와 열람 서비스에 똑같이 두 벌 있었다. PR 서비스가
세 번째 사본을 만들기 전에 github-error.ts 로 모으고, 저장소·설정·토큰을
확인하는 ready() 도 RepoAccessService 로 뺐다. 열람 서비스가 둘이 된다.
```

---

## Task 2: `pull-view.ts` — 순수 매퍼 셋

GitHub JSON 을 우리 모양으로 바꾸는 로직만 모은다. **네트워크를 모른다** —
그래서 단위 테스트로 전부 덮인다(`push-commits.ts` 와 같은 자리).

**Files:**
- Create: `server/src/repos/pull-view.ts`
- Create: `server/src/repos/pull-view.spec.ts`

**Interfaces:**
- Produces:
  - `type PullState = 'open' | 'merged' | 'closed'`
  - `type PullReviewState = 'approved' | 'changes_requested'`
  - `interface PullSummary { number: number; title: string; state: PullState; draft: boolean; authorLogin: string | null; authorAvatarUrl: string | null; sourceBranch: string | null; targetBranch: string | null; htmlUrl: string | null; openedAt: string | null; mergedAt: string | null; closedAt: string | null }`
  - `interface PullDetail extends PullSummary { body: string | null; additions: number | null; deletions: number | null; changedFiles: number | null; review: PullReviewState | null }`
  - `interface PullChangedFile { path: string; status: 'added' | 'modified' | 'removed' | 'renamed'; additions: number; deletions: number; previousPath: string | null }`
  - `toPullSummary(raw: unknown): PullSummary | null`
  - `toPullDetail(raw: unknown): Omit<PullDetail, 'review'> | null`
  - `foldReviewState(raw: unknown): PullReviewState | null`
  - `toPullFile(raw: unknown): PullChangedFile | null`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`server/src/repos/pull-view.spec.ts`:

```ts
import { foldReviewState, toPullFile, toPullSummary } from './pull-view';

const OPEN = {
  number: 12,
  title: '멘션을 파서 안으로 넣는다',
  state: 'open',
  draft: false,
  user: { login: 'dbsrjs', avatar_url: 'https://avatars/1' },
  head: { ref: 'feat/x' },
  base: { ref: 'main' },
  html_url: 'https://github.com/o/r/pull/12',
  created_at: '2026-08-01T00:00:00Z',
  merged_at: null,
  closed_at: null,
};

describe('toPullSummary', () => {
  it('열린 PR 은 open 이다', () => {
    expect(toPullSummary(OPEN)).toMatchObject({
      number: 12,
      state: 'open',
      sourceBranch: 'feat/x',
      targetBranch: 'main',
    });
  });

  it('머지된 것은 closed 가 아니라 merged 다 — 사람에게 전혀 다른 일이다', () => {
    const merged = { ...OPEN, state: 'closed', merged_at: '2026-08-02T00:00:00Z' };
    expect(toPullSummary(merged)?.state).toBe('merged');
  });

  it('머지 없이 닫힌 것은 closed 다', () => {
    const closed = { ...OPEN, state: 'closed', closed_at: '2026-08-02T00:00:00Z' };
    expect(toPullSummary(closed)?.state).toBe('closed');
  });

  it('number 가 없으면 null 이다 — 남이 보내는 값이라 모양을 강제할 수 없다', () => {
    expect(toPullSummary({ ...OPEN, number: undefined })).toBeNull();
  });
});

describe('foldReviewState', () => {
  it('리뷰어마다 마지막 것만 센다', () => {
    expect(
      foldReviewState([
        { user: { login: 'a' }, state: 'CHANGES_REQUESTED' },
        { user: { login: 'a' }, state: 'APPROVED' },
      ]),
    ).toBe('approved');
  });

  it('한 명이라도 변경을 요청하면 changes_requested 다', () => {
    expect(
      foldReviewState([
        { user: { login: 'a' }, state: 'APPROVED' },
        { user: { login: 'b' }, state: 'CHANGES_REQUESTED' },
      ]),
    ).toBe('changes_requested');
  });

  it('COMMENTED · DISMISSED 는 승인도 거절도 아니라 버린다', () => {
    expect(
      foldReviewState([
        { user: { login: 'a' }, state: 'COMMENTED' },
        { user: { login: 'b' }, state: 'DISMISSED' },
      ]),
    ).toBeNull();
  });

  it('리뷰가 없으면 null 이다 — pending 이 아니라 그릴 것이 없다는 뜻이다', () => {
    expect(foldReviewState([])).toBeNull();
    expect(foldReviewState(null)).toBeNull();
  });
});

describe('toPullFile', () => {
  it('patch 를 떨어뜨린다 — diff 를 그리지 않으므로 쓸 곳이 없다', () => {
    const file = toPullFile({
      filename: 'a/b.ts',
      status: 'modified',
      additions: 12,
      deletions: 3,
      patch: '@@ -1 +1 @@',
      blob_url: 'https://example',
    });
    expect(file).toEqual({
      path: 'a/b.ts',
      status: 'modified',
      additions: 12,
      deletions: 3,
      previousPath: null,
    });
  });

  it('renamed 는 이전 경로를 싣는다', () => {
    expect(
      toPullFile({ filename: 'new.ts', status: 'renamed', previous_filename: 'old.ts' }),
    ).toMatchObject({ status: 'renamed', previousPath: 'old.ts' });
  });

  it('모르는 status 는 modified 로 본다 — 목록에서 빠지는 것보다 낫다', () => {
    expect(toPullFile({ filename: 'a.ts', status: 'changed' })?.status).toBe('modified');
  });

  it('filename 이 없으면 null 이다', () => {
    expect(toPullFile({ status: 'added' })).toBeNull();
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
npm --prefix server test -- pull-view
```

기대: `Cannot find module './pull-view'`

- [ ] **Step 3: `pull-view.ts` 를 만든다**

```ts
/**
 * GitHub 의 PR JSON 을 우리 모양으로 바꾼다. **네트워크를 모른다** —
 * 그래서 단위 테스트로 전부 덮인다(`push-commits.ts` 와 같은 자리).
 */

export type PullState = 'open' | 'merged' | 'closed';
export type PullReviewState = 'approved' | 'changes_requested';

export interface PullSummary {
  number: number;
  title: string;
  state: PullState;
  draft: boolean;
  authorLogin: string | null;
  authorAvatarUrl: string | null;
  sourceBranch: string | null;
  targetBranch: string | null;
  htmlUrl: string | null;
  openedAt: string | null;
  mergedAt: string | null;
  closedAt: string | null;
}

export interface PullDetail extends PullSummary {
  body: string | null;
  additions: number | null;
  deletions: number | null;
  changedFiles: number | null;
  review: PullReviewState | null;
}

export interface PullChangedFile {
  path: string;
  status: 'added' | 'modified' | 'removed' | 'renamed';
  additions: number;
  deletions: number;
  previousPath: string | null;
}

function rec(value: unknown): Record<string, unknown> | null {
  return value && typeof value === 'object' && !Array.isArray(value)
    ? (value as Record<string, unknown>)
    : null;
}
function str(value: unknown): string | null {
  return typeof value === 'string' && value ? value : null;
}
function num(value: unknown): number | null {
  return typeof value === 'number' && Number.isFinite(value) ? value : null;
}

export function toPullSummary(raw: unknown): PullSummary | null {
  const pr = rec(raw);
  if (!pr || typeof pr.number !== 'number') return null;

  const mergedAt = str(pr.merged_at);

  return {
    number: pr.number,
    title: str(pr.title) ?? '',
    // **GitHub 의 state 를 그대로 쓰지 않는다.** 거기엔 open · closed 뿐이고
    // 머지 여부는 merged_at 에 있다. 둘은 사람에게 전혀 다른 일이다.
    state: mergedAt ? 'merged' : pr.state === 'closed' ? 'closed' : 'open',
    draft: pr.draft === true,
    authorLogin: str(rec(pr.user)?.login),
    authorAvatarUrl: str(rec(pr.user)?.avatar_url),
    sourceBranch: str(rec(pr.head)?.ref),
    targetBranch: str(rec(pr.base)?.ref),
    // **html_url 은 싣는다** — 토큰 없이 열리는 공개 주소다. diff_url ·
    // patch_url · _links 는 싣지 않는다.
    htmlUrl: str(pr.html_url),
    openedAt: str(pr.created_at),
    mergedAt,
    closedAt: str(pr.closed_at),
  };
}

/** 상세에만 있는 것들. 목록 API 는 additions 를 주지 않는다(설계 §1). */
export function toPullDetail(raw: unknown): Omit<PullDetail, 'review'> | null {
  const summary = toPullSummary(raw);
  if (!summary) return null;
  const pr = rec(raw) as Record<string, unknown>;

  return {
    ...summary,
    body: str(pr.body),
    additions: num(pr.additions),
    deletions: num(pr.deletions),
    changedFiles: num(pr.changed_files),
  };
}

/**
 * 리뷰 목록을 한 값으로 접는다.
 *
 * GitHub 은 리뷰를 **시간순으로 전부** 준다. 같은 사람이 변경을 요청했다가
 * 나중에 승인하면 두 행이 다 남아, 그대로 세면 승인과 변경 요청이 동시에 뜬다.
 * **리뷰어마다 마지막 것만 남긴다.**
 *
 * `COMMENTED` · `DISMISSED` 는 승인도 거절도 아니라 버린다. 남은 것이 없으면
 * `null` 이다 — **`pending` 이 아니다.** `pending` 은 제출하지 않은 초안이라
 * 남에게 보이지도 않는 값이고, 우리에게 `null` 은 "그릴 것이 없음"이다.
 */
export function foldReviewState(raw: unknown): PullReviewState | null {
  if (!Array.isArray(raw)) return null;

  const last = new Map<string, string>();
  for (const item of raw) {
    const review = rec(item);
    const login = str(rec(review?.user)?.login);
    const state = str(review?.state)?.toUpperCase();
    if (!login || !state) continue;
    if (state !== 'APPROVED' && state !== 'CHANGES_REQUESTED') continue;
    last.set(login, state);
  }

  const states = [...last.values()];
  if (states.includes('CHANGES_REQUESTED')) return 'changes_requested';
  if (states.includes('APPROVED')) return 'approved';
  return null;
}

export function toPullFile(raw: unknown): PullChangedFile | null {
  const file = rec(raw);
  const path = str(file?.filename);
  if (!path) return null;

  const status = ((): PullChangedFile['status'] => {
    switch (file?.status) {
      case 'added':
      case 'removed':
      case 'renamed':
        return file.status;
      // copied · changed · unchanged 는 우리 화면에서 modified 와 같이 다룬다.
      // 목록에서 빠지는 것보다 낫다.
      default:
        return 'modified';
    }
  })();

  return {
    path,
    status,
    additions: num(file?.additions) ?? 0,
    deletions: num(file?.deletions) ?? 0,
    previousPath: str(file?.previous_filename),
  };
}
```

- [ ] **Step 4: 테스트가 통과하는지 본다**

```bash
npm --prefix server test -- pull-view
```

기대: 12개 통과.

- [ ] **Step 5: 커밋**

```bash
git add server/src/repos/pull-view.ts server/src/repos/pull-view.spec.ts
git commit -m "feat(server): PR JSON 을 우리 모양으로 바꾸는 순수 매퍼"
```

커밋 본문:

```
머지된 PR 을 closed 가 아니라 merged 로 갈라 준다 - GitHub 은 state 에
open·closed 만 두고 머지 여부를 merged_at 에 두는데, 둘은 사람에게 전혀
다른 일이다. 리뷰는 리뷰어마다 마지막 것만 세고 COMMENTED·DISMISSED 는
버린다. 파일에서 patch 를 떨어뜨린다 - diff 를 그리지 않으므로 쓸 곳이 없다.
```

---

## Task 3: `pr-event.ts` — 웹훅 payload 에서 PR 번호

**Files:**
- Create: `server/src/repos/pr-event.ts`
- Create: `server/src/repos/pr-event.spec.ts`

**Interfaces:**
- Produces: `readPullNumber(payload: unknown): number | null`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```ts
import { readPullNumber } from './pr-event';

describe('readPullNumber', () => {
  it('payload 의 number 를 읽는다 — GitHub 을 부르지 않는다', () => {
    expect(readPullNumber({ number: 12, pull_request: { title: 't' } })).toBe(12);
  });

  it('number 가 없으면 pull_request.number 를 본다', () => {
    expect(readPullNumber({ pull_request: { number: 7 } })).toBe(7);
  });

  it('둘 다 없으면 null 이다', () => {
    expect(readPullNumber({ pull_request: {} })).toBeNull();
    expect(readPullNumber(null)).toBeNull();
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
npm --prefix server test -- pr-event
```

- [ ] **Step 3: 구현한다**

```ts
/**
 * PR 웹훅 payload 에서 번호만 꺼낸다.
 *
 * **GitHub 을 부르지 않는다** — 10-3b 가 커밋에서 세운 규칙과 같다. 저장소
 * 접근 권한이 나중에 사라져도 지난 기록으로 들어갈 수 있다.
 */
export function readPullNumber(payload: unknown): number | null {
  if (!payload || typeof payload !== 'object') return null;
  const data = payload as Record<string, unknown>;

  if (typeof data.number === 'number') return data.number;

  const pr = data.pull_request;
  if (pr && typeof pr === 'object') {
    const inner = (pr as Record<string, unknown>).number;
    if (typeof inner === 'number') return inner;
  }
  return null;
}
```

- [ ] **Step 4: 통과를 확인하고 커밋한다**

```bash
npm --prefix server test -- pr-event
git add server/src/repos/pr-event.ts server/src/repos/pr-event.spec.ts
git commit -m "feat(server): PR 웹훅 payload 에서 번호를 읽는다"
```

---

## Task 4: `GithubOauthClient` 에 PR 호출 넷

**Files:**
- Modify: `server/src/oauth/github-oauth.client.ts` (`listCommits` 아래에 추가)

**Interfaces:**
- Consumes: Task 2 의 `PullSummary` · `PullDetail` · `PullChangedFile` ·
  `PullReviewState` · `toPullSummary` · `toPullDetail` · `foldReviewState` · `toPullFile`
- Produces:
  - `listPulls(cfg, token, fullName: string, state: 'open' | 'closed', page: number): Promise<GithubResult<PullSummary[]>>`
  - `getPull(cfg, token, fullName: string, num: number): Promise<GithubResult<Omit<PullDetail, 'review'>>>`
  - `getPullReview(cfg, token, fullName: string, num: number): Promise<GithubResult<PullReviewState | null>>`
  - `listPullFiles(cfg, token, fullName: string, num: number): Promise<GithubResult<{ files: PullChangedFile[]; truncated: boolean }>>`

- [ ] **Step 1: import 를 더한다**

파일 맨 위 `import type { CommitSummary } from '../repos/push-commits';` 아래에:

```ts
import {
  foldReviewState,
  toPullDetail,
  toPullFile,
  toPullSummary,
  type PullChangedFile,
  type PullDetail,
  type PullReviewState,
  type PullSummary,
} from '../repos/pull-view';
```

**`import type` 이 아닌 것에 주의한다** — 매퍼 넷은 런타임에 쓰인다. 타입에만
`type` 을 붙인다.

- [ ] **Step 2: 네 메서드를 더한다** (`listCommits` 바로 아래)

```ts
  /** 열린/닫힌 PR 목록. **변경량은 오지 않는다** — 단건에만 있다(설계 §1). */
  async listPulls(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    state: 'open' | 'closed',
    page: number,
  ): Promise<GithubResult<PullSummary[]>> {
    const query = new URLSearchParams({
      state,
      per_page: '30',
      page: String(page),
      sort: 'updated',
      direction: 'desc',
    });

    const res = await this.call<Array<Record<string, unknown>>>(
      `${cfg.apiBase}/repos/${fullName}/pulls?${query.toString()}`,
      token,
    );
    if (!res.ok) return res;

    const pulls: PullSummary[] = [];
    for (const raw of res.value) {
      const view = toPullSummary(raw);
      if (view) pulls.push(view);
    }
    return { ok: true, value: pulls };
  }

  async getPull(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    num: number,
  ): Promise<GithubResult<Omit<PullDetail, 'review'>>> {
    const res = await this.call<Record<string, unknown>>(
      `${cfg.apiBase}/repos/${fullName}/pulls/${num}`,
      token,
    );
    if (!res.ok) return res;

    const view = toPullDetail(res.value);
    // 모양이 깨진 응답을 422 로 접는다 — `getCommit` 과 같은 처리다.
    if (!view) return { ok: false, status: 422 };
    return { ok: true, value: view };
  }

  /** 리뷰 목록을 받아 **한 값으로 접어서** 준다(설계 §2). */
  async getPullReview(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    num: number,
  ): Promise<GithubResult<PullReviewState | null>> {
    const res = await this.call<unknown>(
      `${cfg.apiBase}/repos/${fullName}/pulls/${num}/reviews?per_page=100`,
      token,
    );
    if (!res.ok) return res;
    return { ok: true, value: foldReviewState(res.value) };
  }

  /**
   * 바뀐 파일. **상한 300** 에서 끊고 `truncated` 로 알린다 — GitHub 이
   * 페이지당 100개를 주므로 세 장까지 본다. 조용히 자르지 않는 것이 상한을
   * 둘 수 있는 조건이다(9-1 의 규칙).
   */
  async listPullFiles(
    cfg: GithubOauthConfig,
    token: string,
    fullName: string,
    num: number,
  ): Promise<GithubResult<{ files: PullChangedFile[]; truncated: boolean }>> {
    const files: PullChangedFile[] = [];

    for (let page = 1; page <= 3; page++) {
      const res = await this.call<Array<Record<string, unknown>>>(
        `${cfg.apiBase}/repos/${fullName}/pulls/${num}/files?per_page=100&page=${page}`,
        token,
      );
      if (!res.ok) return res;

      for (const raw of res.value) {
        const view = toPullFile(raw);
        if (view) files.push(view);
      }

      // 한 장이 덜 찼으면 마지막 장이다.
      if (res.value.length < 100) return { ok: true, value: { files, truncated: false } };
    }

    // 세 장이 모두 꽉 찼다 — 더 있을 수 있다.
    return { ok: true, value: { files, truncated: true } };
  }
```

- [ ] **Step 3: 타입 검사**

```bash
npm --prefix server run typecheck
npm --prefix server test
```

기대: 타입 오류 없음, 기존 테스트 전부 통과.

- [ ] **Step 4: 커밋**

```bash
git add server/src/oauth/github-oauth.client.ts
git commit -m "feat(server): GitHub PR 호출 넷 - 목록 · 단건 · 리뷰 · 파일"
```

커밋 본문:

```
파일은 상한 300 에서 끊고 truncated 로 알린다. 리뷰는 목록을 그대로 주지
않고 한 값으로 접어서 준다 - 화면이 언제나 상세와 함께 쓰는데 왕복을 둘로
나눌 이유가 없다.
```

---

## Task 5: `PullsService` · `PullsController`

**Files:**
- Create: `server/src/repos/pulls.service.ts`
- Create: `server/src/repos/pulls.controller.ts`
- Modify: `server/src/repos/repos.module.ts`

**Interfaces:**
- Consumes: Task 1 의 `RepoAccessService` · `toGithubHttpError`, Task 4 의 네 메서드
- Produces:
  - `GET /api/spaces/:spaceId/repos/:repoId/pulls?state=open&page=1`
    → `{ pulls: PullSummary[]; nextPage: number | null }`
  - `GET /api/spaces/:spaceId/repos/:repoId/pulls/:number` → `PullDetail`
  - `GET /api/spaces/:spaceId/repos/:repoId/pulls/:number/files`
    → `{ files: PullChangedFile[]; truncated: boolean }`

- [ ] **Step 1: 서비스를 만든다**

```ts
import { BadRequestException, Injectable } from '@nestjs/common';
import { GithubOauthClient } from '../oauth/github-oauth.client';
import { toGithubHttpError } from './github-error';
import { RepoAccessService } from './repo-access.service';
import type { PullDetail } from './pull-view';

/**
 * PR 열람. **DB 사본을 두지 않고 GitHub 을 프록시한다**(설계 §0).
 *
 * `pull_requests` 테이블은 스키마에 있지만 쓰지 않는다 — 사본을 두면 연결 전
 * PR 이 안 보이는 백필 문제가 생기고, 웹훅을 놓치면 행이 영영 어긋난다.
 */
@Injectable()
export class PullsService {
  constructor(
    private readonly access: RepoAccessService,
    private readonly github: GithubOauthClient,
  ) {}

  async list(
    spaceId: string,
    userId: string,
    repoId: string,
    state: string,
    page: number,
  ) {
    // **open · closed 만 받는다.** 머지된 PR 은 closed 로 조회된다 — GitHub 이
    // 그렇게 나누고, 목록에서 머지 여부로 거를 방법이 없다.
    if (state !== 'open' && state !== 'closed') {
      throw new BadRequestException('state 는 open 또는 closed 입니다');
    }

    const { repo, cfg, token } = await this.access.ready(spaceId, userId, repoId);
    const res = await this.github.listPulls(cfg, token, repo.fullPath, state, page);
    if (!res.ok) throw toGithubHttpError(res.status, res.retryAfter);

    return {
      pulls: res.value,
      // per_page=30 이 꽉 찼으면 다음 장이 있다고 본다(커밋 목록과 같다).
      nextPage: res.value.length === 30 ? page + 1 : null,
    };
  }

  /** 상세 — **리뷰 상태를 접어 함께 준다**(설계 §2). */
  async detail(
    spaceId: string,
    userId: string,
    repoId: string,
    num: number,
  ): Promise<PullDetail> {
    const { repo, cfg, token } = await this.access.ready(spaceId, userId, repoId);

    const pull = await this.github.getPull(cfg, token, repo.fullPath, num);
    if (!pull.ok) throw toGithubHttpError(pull.status, pull.retryAfter);

    const review = await this.github.getPullReview(cfg, token, repo.fullPath, num);
    // **리뷰를 못 받아도 상세는 준다.** 리뷰는 곁가지라, 그것 때문에 PR 이
    // 통째로 안 열리면 손해가 더 크다.
    return { ...pull.value, review: review.ok ? review.value : null };
  }

  async files(spaceId: string, userId: string, repoId: string, num: number) {
    const { repo, cfg, token } = await this.access.ready(spaceId, userId, repoId);

    const res = await this.github.listPullFiles(cfg, token, repo.fullPath, num);
    if (!res.ok) throw toGithubHttpError(res.status, res.retryAfter);
    return res.value;
  }
}
```

- [ ] **Step 2: 컨트롤러를 만든다**

**`CurrentUser` 의 import 경로는 `repos.controller.ts` 가 쓰는 것을 그대로
따른다** — 아래는 그 파일을 열어 확인한 뒤 맞춘다.

```ts
import {
  Controller,
  Get,
  Param,
  ParseIntPipe,
  ParseUUIDPipe,
  Query,
  UseGuards,
} from '@nestjs/common';
import { CurrentUser } from '../common/decorators/current-user.decorator';
import { SpaceGuard } from '../spaces/guards/space.guard';
import { PullsService } from './pulls.service';

/**
 * PR 열람. `repos.controller.ts` 에 붙이지 않은 이유는 그 파일이 이미
 * **등록·관리(POST)와 열람(GET)을 함께** 들고 있어서다. 늘리는 대신 PR 은
 * 자기 파일로 시작한다.
 *
 * **`SpaceRoleGuard` 를 걸지 않는다** — 읽기다(10-3a 와 같다). 걸면 코드를
 * 볼 수 있는 사람과 저장소 설정을 바꿀 수 있는 사람이 같아진다.
 */
@Controller('spaces/:spaceId/repos/:repoId/pulls')
@UseGuards(SpaceGuard)
export class PullsController {
  constructor(private readonly pulls: PullsService) {}

  @Get()
  list(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('repoId', new ParseUUIDPipe()) repoId: string,
    @CurrentUser('id') userId: string,
    @Query('state') state = 'open',
    @Query('page') page = '1',
  ) {
    const parsed = Number(page);
    return this.pulls.list(
      spaceId,
      userId,
      repoId,
      state,
      Number.isFinite(parsed) && parsed > 0 ? parsed : 1,
    );
  }

  @Get(':number')
  detail(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('repoId', new ParseUUIDPipe()) repoId: string,
    @Param('number', new ParseIntPipe()) num: number,
    @CurrentUser('id') userId: string,
  ) {
    return this.pulls.detail(spaceId, userId, repoId, num);
  }

  @Get(':number/files')
  files(
    @Param('spaceId', new ParseUUIDPipe()) spaceId: string,
    @Param('repoId', new ParseUUIDPipe()) repoId: string,
    @Param('number', new ParseIntPipe()) num: number,
    @CurrentUser('id') userId: string,
  ) {
    return this.pulls.files(spaceId, userId, repoId, num);
  }
}
```

- [ ] **Step 3: 모듈에 등록한다**

`repos.module.ts` 의 `controllers` 에 `PullsController`, `providers` 에
`PullsService` 를 더한다.

- [ ] **Step 4: 타입 검사와 테스트**

```bash
npm --prefix server run typecheck
npm --prefix server test
```

- [ ] **Step 5: 커밋**

```bash
git add server/src/repos
git commit -m "feat(server): PR 목록 · 상세 · 바뀐 파일 라우트"
```

커밋 본문:

```
state 는 open·closed 만 받는다. 머지된 PR 은 closed 로 조회되고 응답의
state 에서 merged 로 갈린다. 리뷰를 못 받아도 상세는 준다 - 곁가지 때문에
PR 이 통째로 안 열리면 손해가 더 크다.
```

---

## Task 6: `repo-events` 를 이벤트 종류로 가른다

10-3b 가 *"PR · 이슈 이벤트 메시지를 눌렀을 때는 화면으로 보지 않았다"* 고
남긴 자리다. 지금은 종류와 무관하게 커밋을 돌려준다.

**Files:**
- Modify: `server/src/repos/repo-browse.service.ts` (`eventCommits` → `eventDetail`)
- Modify: `server/src/repos/repo-events.controller.ts`

**Interfaces:**
- Consumes: Task 3 의 `readPullNumber`
- Produces: `GET /api/spaces/:spaceId/repo-events/:eventId` →
  - `{ kind: 'push'; repoId; repoFullPath; ref; commits }`
  - `{ kind: 'pr'; repoId; repoFullPath; number }`
  - `{ kind: 'other'; repoId; repoFullPath }`

- [ ] **Step 1: `eventDetail` 로 바꾼다**

`repo-browse.service.ts` 의 `eventCommits` 를 갈아 끼운다.
`RepoEventType`(`@prisma/client`)과 `readPullNumber` 를 import 한다.

```ts
  /**
   * 이벤트 하나. **GitHub 을 부르지 않는다** — payload 에 이미 있다(설계 §0).
   * 토큰도 필요 없어 연결이 끊긴 뒤에도 지난 기록은 열린다.
   *
   * **종류로 갈라 준다.** 앱은 `kind` 로 커밋 화면과 PR 화면 중 하나를 연다.
   * 예전에는 종류와 무관하게 커밋을 돌려주어, PR 이벤트를 누르면 빈 목록이
   * 왔다(10-3b 가 미확인으로 남긴 자리).
   */
  async eventDetail(spaceId: string, eventId: string) {
    const event = await this.prisma.repoEvent.findFirst({
      where: { id: eventId, spaceId },
      select: {
        type: true,
        payload: true,
        repo: { select: { id: true, fullPath: true } },
      },
    });
    if (!event) throw new NotFoundException('이벤트를 찾을 수 없습니다');

    const base = { repoId: event.repo.id, repoFullPath: event.repo.fullPath };

    if (event.type === RepoEventType.push) {
      const view = readPushCommits(event.payload);
      return { kind: 'push' as const, ...base, ref: view.ref, commits: view.commits };
    }

    if (event.type === RepoEventType.pr) {
      const number = readPullNumber(event.payload);
      // 번호를 못 읽으면 열 곳이 없다 — `other` 로 접어 앱이 누르지 못하게 한다.
      if (number !== null) return { kind: 'pr' as const, ...base, number };
    }

    // 이슈 · 릴리스. **앱에서 누를 수 없다** — 눌러 봐야 빈 화면인 버튼은
    // 없느니만 못하다(7-5 에서 답글 고정을 시트에서 감춘 것과 같다).
    return { kind: 'other' as const, ...base };
  }
```

- [ ] **Step 2: 컨트롤러의 호출을 고친다**

`repo-events.controller.ts` 의 `this.browse.eventCommits(...)` →
`this.browse.eventDetail(...)`.

- [ ] **Step 3: 타입 검사**

```bash
npm --prefix server run typecheck
npm --prefix server test
```

기대: 오류 없음(`eventCommits` 를 부르는 다른 곳이 없다).

- [ ] **Step 4: 커밋**

```bash
git add server/src/repos
git commit -m "feat(server): 저장소 이벤트를 종류로 가른다 - push · pr · other"
```

커밋 본문:

```
지금까지 종류와 무관하게 커밋을 돌려주어 PR 이벤트를 누르면 빈 목록이 왔다.
10-3b 가 미확인으로 남긴 자리다. PR 번호는 payload 에 이미 있어 이 진입도
GitHub 을 부르지 않는다.
```

---

## Task 7: `check:pulls` 계약 검증

**Files:**
- Create: `server/scripts/check-pulls.mjs`
- Modify: `package.json` (`check:pulls` 스크립트)
- Modify: `.github/workflows/ci.yml` (계약 검증 단계에 추가)

**Interfaces:**
- Consumes: Task 5 의 세 라우트, Task 6 의 `kind`
- Produces: `npm run check:pulls`

`server/scripts/check-browse.mjs` 를 **골격 그대로** 따른다 — 가짜 GitHub
(4599) 을 스스로 띄우고, `lib/api.mjs` · `lib/checks.mjs` · `lib/preflight.mjs`
를 쓴다. **그 파일을 먼저 읽고 시작한다.**

- [ ] **Step 1: 가짜 GitHub 에 PR 분기를 더한다**

`check-browse.mjs` 의 `createServer` 핸들러를 참고해 넷을 더한다.
**`/pulls/:n/files` 를 `/pulls/:n` 보다 먼저 본다** — 순서를 뒤집으면 파일
요청이 단건 분기에 잡힌다(커밋에서 같은 함정을 겪었다).

```js
// 순서가 중요하다 — 더 긴 경로를 먼저 본다.
const files = url.pathname.match(/^\/repos\/[^/]+\/[^/]+\/pulls\/(\d+)\/files$/);
if (req.method === 'GET' && files) { /* 12=짧은 한 장 · 99=100개씩 세 장 */ }

const reviews = url.pathname.match(/^\/repos\/[^/]+\/[^/]+\/pulls\/(\d+)\/reviews$/);
if (req.method === 'GET' && reviews) { /* 같은 사람의 CHANGES_REQUESTED → APPROVED */ }

const one = url.pathname.match(/^\/repos\/[^/]+\/[^/]+\/pulls\/(\d+)$/);
if (req.method === 'GET' && one) { /* 12=open · 13=merged · 14=closed · 404 는 그 밖 */ }

if (req.method === 'GET' && url.pathname.endsWith('/pulls')) { /* state 로 갈라 준다 */ }
```

429 를 태우려면 `check-browse.mjs` 가 쓰는 방식을 그대로 따른다 — 특정 번호
(예: `/pulls/429`)에 `429` 와 `retry-after: 42` 를 돌려준다.

- [ ] **Step 2: 케이스를 쓴다**

```js
// 1) 목록
check('열린 PR 목록이 온다', list.status === 200 && list.json.pulls.length === 1);
check('목록에 변경량이 없다 — GitHub 이 주지 않는다',
  list.json.pulls[0].additions === undefined);
check('state 가 이상하면 400', bad.status === 400);

// 2) 상세
check('머지된 것은 closed 가 아니라 merged', d13.json.state === 'merged');
check('머지 없이 닫힌 것은 closed', d14.json.state === 'closed');
check('리뷰가 접혀서 온다 — 같은 사람의 마지막 것', d12.json.review === 'approved');
check('본문과 변경량이 온다', typeof d12.json.additions === 'number');
check('htmlUrl 은 싣는다', typeof d12.json.htmlUrl === 'string');
check('diff_url · _links 는 싣지 않는다',
  d12.json.diff_url === undefined && d12.json._links === undefined);

// 3) 파일
check('patch 가 응답에 없다', f12.json.files.every((f) => f.patch === undefined));
check('경로 필드는 path 다 — 10-3b 의 ChangedFile 과 같다',
  typeof f12.json.files[0].path === 'string');
check('renamed 는 previousPath 를 싣는다', renamed.previousPath === 'old.ts');
check('상한을 넘으면 truncated 다', f99.json.truncated === true);
check('상한에서 끊는다', f99.json.files.length === 300);

// 4) 격리 · 실패
check('타 스페이스의 저장소는 404', other.status === 404);
check('GitHub 계정을 연결하지 않았으면 400 이다 — 목록이 아니라 안내로 떨어진다',
  noToken.status === 400);
// 서버에 GitHub 설정이 아예 없을 때의 503 은 **이 스크립트가 태울 수 없다** —
// .env 를 비우고 서버를 재시작해야 한다. check:oauth 와 같이 **안내만 찍는다**.
console.log('  --  미설정 503 은 .env 의 GITHUB_CLIENT_ID 를 비우고 재시작해 확인한다');
check('없는 PR 번호는 404', missing.status === 404);
check('429 는 Retry-After 를 헤더로 준다',
  rate.status === 429 && rate.headers.get('retry-after') === '42');

// 5) 이벤트 진입 (Task 6)
check('PR 이벤트는 kind:pr 과 번호를 준다',
  ev.json.kind === 'pr' && ev.json.number === 12);
check('push 이벤트는 여전히 커밋을 준다', evPush.json.kind === 'push');
check('이슈 이벤트는 other 다', evIssue.json.kind === 'other');
check('이벤트 조회는 GitHub 을 부르지 않는다', hitsAfter === hitsBefore);
```

**마지막 케이스가 이 조각의 핵심 판단이다** — 가짜 GitHub 이 받은 요청 수를
이벤트 조회 전후로 비교한다(10-3b 가 같은 방식으로 고정했다).

**단언에 `undefined === undefined` 를 만들지 않는다.** 10-2b 에서 앞 단계가
실패했는데 거짓 통과한 전례가 있다 — 값이 **있는 것**부터 확인한다.

- [ ] **Step 3: package.json 과 CI 에 등록한다**

```json
"check:pulls": "node server/scripts/check-pulls.mjs",
```

`.github/workflows/ci.yml` 의 계약 검증 단계에 `npm run check:pulls` 를
**기존 줄들과 같은 자리에 같은 모양으로** 넣는다.

- [ ] **Step 4: 실제로 돌린다**

```bash
npm run db:up
```

다른 창에서 `npm run server:dev` 를 띄운 뒤:

```bash
npm run check:pulls
```

기대: 전부 통과. **하나라도 실패하면 코드를 고친다** — 검증을 고쳐 통과시키지
않는다.

- [ ] **Step 5: 커밋**

```bash
git add server/scripts/check-pulls.mjs package.json .github/workflows/ci.yml
git commit -m "test(server): PR 열람 계약 검증 - 가짜 GitHub 으로 돈다"
```

커밋 본문:

```
이벤트 조회가 GitHub 을 부르지 않는 것을 요청 수 비교로 고정했다. 이 조각의
핵심 판단이라 말로만 두지 않는다.
```

---

## Task 8: 앱 모델과 API

**Files:**
- Create: `app/lib/domain/models/pull.dart`
- Create: `app/lib/data/api/pulls_api.dart`
- Modify: `app/lib/domain/models/repo_browse.dart` (`PushEventView` → `RepoEventView`)
- Modify: `app/lib/data/api/browse_api.dart` (`eventCommits` → `event`)
- Create: `app/test/pull_test.dart`

**Interfaces:**
- Consumes: Task 5 · 6 의 응답 모양
- Produces:
  - `PullSummary` · `PullDetail` · `PullChangedFile` (freezed)
  - `PullsApi.list(spaceId, repoId, {state, page})` →
    `({List<PullSummary> pulls, int? nextPage})`
  - `PullsApi.detail(spaceId, repoId, number)` → `PullDetail`
  - `PullsApi.files(spaceId, repoId, number)` →
    `({List<PullChangedFile> files, bool truncated})`

- [ ] **Step 1: 모델을 쓴다**

`repo_browse.dart` 의 `CommitSummary` · `ChangedFile` 을 그대로 본떠 쓴다.
**`ChangedFile` 을 고쳐 쓰지 않는다** — PR 의 파일은 `additions` ·
`deletions` · `previousPath` 가 더 붙으므로 `PullChangedFile` 로 따로 두되,
화면은 같은 행 위젯을 쓴다.

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'pull.freezed.dart';
part 'pull.g.dart';

/// PR 의 상태. **머지와 그냥 닫힘은 사람에게 전혀 다른 일이라 갈라 둔다.**
enum PullState { open, merged, closed }

/// 리뷰를 접은 값. **`null` 은 `pending` 이 아니라 "그릴 것이 없음"이다.**
enum PullReviewState {
  approved,
  @JsonValue('changes_requested')
  changesRequested,
}

@freezed
abstract class PullSummary with _$PullSummary {
  const factory PullSummary({
    required int number,
    required String title,
    required PullState state,
    @Default(false) bool draft,
    String? authorLogin,
    String? authorAvatarUrl,
    String? sourceBranch,
    String? targetBranch,
    String? htmlUrl,
    String? openedAt,
    String? mergedAt,
    String? closedAt,
  }) = _PullSummary;

  factory PullSummary.fromJson(Map<String, dynamic> json) =>
      _$PullSummaryFromJson(json);
}

@freezed
abstract class PullDetail with _$PullDetail {
  const factory PullDetail({
    required int number,
    required String title,
    required PullState state,
    @Default(false) bool draft,
    String? body,
    String? authorLogin,
    String? authorAvatarUrl,
    String? sourceBranch,
    String? targetBranch,
    String? htmlUrl,
    String? openedAt,
    String? mergedAt,
    String? closedAt,
    /// **서버가 모르면 `null` 이다.** 0 으로 두면 "안 바뀐 PR"로 읽힌다.
    int? additions,
    int? deletions,
    int? changedFiles,
    PullReviewState? review,
  }) = _PullDetail;

  factory PullDetail.fromJson(Map<String, dynamic> json) =>
      _$PullDetailFromJson(json);
}

@freezed
abstract class PullChangedFile with _$PullChangedFile {
  const factory PullChangedFile({
    required String path,
    required String status,
    @Default(0) int additions,
    @Default(0) int deletions,
    String? previousPath,
  }) = _PullChangedFile;

  factory PullChangedFile.fromJson(Map<String, dynamic> json) =>
      _$PullChangedFileFromJson(json);
}
```

- [ ] **Step 2: 코드 생성**

```bash
cd app && dart run build_runner build --delete-conflicting-outputs
```

멈추면 `.dart_tool/build` 를 지우고 다시 돌린다(전례가 있다).

- [ ] **Step 3: 모델 테스트를 쓰고 돌린다**

`app/test/pull_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:nexus_app/domain/models/pull.dart';

void main() {
  test('머지된 PR 은 merged 로 읽힌다', () {
    final pull = PullSummary.fromJson({
      'number': 12,
      'title': 't',
      'state': 'merged',
      'mergedAt': '2026-08-02T00:00:00Z',
    });
    expect(pull.state, PullState.merged);
  });

  test('리뷰가 없으면 null 이다 — pending 이 아니다', () {
    final detail = PullDetail.fromJson({
      'number': 12,
      'title': 't',
      'state': 'open',
      'review': null,
    });
    expect(detail.review, isNull);
  });

  test('changes_requested 를 읽는다', () {
    final detail = PullDetail.fromJson({
      'number': 12,
      'title': 't',
      'state': 'open',
      'review': 'changes_requested',
    });
    expect(detail.review, PullReviewState.changesRequested);
  });

  test('변경량은 모르면 null 이다 — 0 이 아니다', () {
    final detail = PullDetail.fromJson({
      'number': 12,
      'title': 't',
      'state': 'open',
    });
    expect(detail.additions, isNull);
  });

  test('renamed 는 이전 경로를 갖는다', () {
    final file = PullChangedFile.fromJson({
      'path': 'new.ts',
      'status': 'renamed',
      'previousPath': 'old.ts',
    });
    expect(file.previousPath, 'old.ts');
  });
}
```

```bash
cd app && flutter test test/pull_test.dart
```

- [ ] **Step 4: API 를 쓴다**

`browse_api.dart` 의 모양을 그대로 따른다 — **오류를 `ApiFailure` 로 분류하는
try/catch 를 그 파일에서 보고 똑같이 옮긴다.** 아래는 그 부분을 뺀 뼈대다.

```dart
import '../../domain/models/pull.dart';
import 'api_client.dart';

/// PR 열람. **캐시하지 않는다** — 저장소 열람 · 이슈 댓글과 같은 취급이다.
class PullsApi {
  PullsApi(this._client);

  final ApiClient _client;

  Future<({List<PullSummary> pulls, int? nextPage})> list(
    String spaceId,
    String repoId, {
    String state = 'open',
    int page = 1,
  }) async {
    final res = await _client.dio.get<Map<String, dynamic>>(
      '/spaces/$spaceId/repos/$repoId/pulls',
      queryParameters: {'state': state, 'page': page},
    );
    final data = res.data!;
    return (
      pulls: (data['pulls'] as List<dynamic>)
          .map((e) => PullSummary.fromJson(e as Map<String, dynamic>))
          .toList(),
      nextPage: data['nextPage'] as int?,
    );
  }

  Future<PullDetail> detail(String spaceId, String repoId, int number) async {
    final res = await _client.dio.get<Map<String, dynamic>>(
      '/spaces/$spaceId/repos/$repoId/pulls/$number',
    );
    return PullDetail.fromJson(res.data!);
  }

  Future<({List<PullChangedFile> files, bool truncated})> files(
    String spaceId,
    String repoId,
    int number,
  ) async {
    final res = await _client.dio.get<Map<String, dynamic>>(
      '/spaces/$spaceId/repos/$repoId/pulls/$number/files',
    );
    final data = res.data!;
    return (
      files: (data['files'] as List<dynamic>)
          .map((e) => PullChangedFile.fromJson(e as Map<String, dynamic>))
          .toList(),
      truncated: data['truncated'] as bool? ?? false,
    );
  }
}
```

- [ ] **Step 5: `PushEventView` 를 `kind` 에 맞춘다**

`repo_browse.dart` 의 `PushEventView` 에 `kind` 와 `number` 를 더하고 이름을
`RepoEventView` 로 바꾼다 — push 만 담지 않게 됐다.

```dart
@freezed
abstract class RepoEventView with _$RepoEventView {
  const factory RepoEventView({
    required String kind, // push · pr · other
    required String repoId,
    String? repoFullPath,
    String? ref,
    int? number,
    @Default(<CommitSummary>[]) List<CommitSummary> commits,
  }) = _RepoEventView;

  factory RepoEventView.fromJson(Map<String, dynamic> json) =>
      _$RepoEventViewFromJson(json);
}
```

`browse_api.dart` 의 `eventCommits` 를 `event` 로 바꾸고 반환 타입을 갈아
끼운다. 호출부를 함께 고친다 — `flutter analyze` 가 전부 짚어 준다.

- [ ] **Step 6: 코드 생성 · 분석 · 테스트**

```bash
cd app && dart run build_runner build --delete-conflicting-outputs
cd app && flutter analyze
cd app && flutter test
```

기대: analyze 0건, 기존 223개 + 새 5개.

- [ ] **Step 7: 커밋**

```bash
git add app/lib app/test/pull_test.dart
git commit -m "feat(app): PR 모델과 API"
```

커밋 본문:

```
변경량은 모르면 null 이다 - 서버가 목록에서 주지 않는다. 0 으로 두면 안 바뀐
PR 로 읽힌다. 리뷰의 null 은 pending 이 아니라 그릴 것이 없다는 뜻이다.
PushEventView 를 RepoEventView 로 바꿨다 - push 만 담지 않게 됐다.
```

---

## Task 9: PR 목록 · 상세 화면

**Files:**
- Create: `app/lib/features/repo/pulls_screen.dart`
- Create: `app/lib/features/repo/pull_detail_screen.dart`
- Modify: `app/lib/core/router.dart`
- Create: `app/test/pulls_screen_test.dart`

**Interfaces:**
- Consumes: Task 8 의 `PullsApi` · 모델
- Produces: 라우트 `/s/:spaceId/repos/:repoId/pulls` ·
  `/s/:spaceId/repos/:repoId/pulls/:number`

`commits_screen.dart` · `commit_detail_screen.dart` 를 골격으로 삼는다.
**그 두 파일을 먼저 읽고 시작한다** — 로딩 · 오류 · 빈 상태의 모양을 맞춘다.

**provider 를 먼저 정한다.** 상세는 **한 provider 가 상세와 파일을 함께** 받는다 —
화면이 스피너 하나만 그리면 되고, 위젯 테스트가 override 할 자리도 하나다.

```dart
// pull_detail_screen.dart
typedef PullKey = ({String spaceId, String repoId, int number});
typedef PullView = ({PullDetail pull, List<PullChangedFile> files, bool truncated});

final pullDetailProvider = FutureProvider.family<PullView, PullKey>((ref, key) async {
  final api = ref.read(pullsApiProvider);
  final pull = await api.detail(key.spaceId, key.repoId, key.number);
  final files = await api.files(key.spaceId, key.repoId, key.number);
  return (pull: pull, files: files.files, truncated: files.truncated);
});

// pulls_screen.dart
typedef PullListKey = ({String spaceId, String repoId, String state});

final pullsProvider =
    FutureProvider.family<({List<PullSummary> pulls, int? nextPage}), PullListKey>(
  (ref, key) => ref.read(pullsApiProvider).list(key.spaceId, key.repoId, state: key.state),
);
```

`pullsApiProvider` 는 `browseApiProvider` 가 선언된 자리에 같은 모양으로 둔다.

- [ ] **Step 1: 목록 화면**

- 앱바 제목 «Pull Request», 필터는 «열림 / 닫힘» `SegmentedButton`
- 한 줄: `#12 · 제목` / 둘째 줄 `feat/x → main` / 오른쪽에 상태 칩
- **상태 칩**: 열림 · 머지됨 · 닫힘. `draft` 면 «초안»을 먼저 보여 준다
- **변경량을 그리지 않는다** — 목록에 없다(설계 §1)
- `nextPage` 가 있으면 «더 불러오기»(커밋 목록과 같은 모양)
- 비면 «열린 PR 이 없습니다»
- 미연결 실패면 «GitHub 을 연결하세요» + 저장소 화면으로 가는 버튼

- [ ] **Step 2: 상세 화면**

- 제목 · `#번호` · 상태 칩 · `작성자 · feat/x → main`
- **본문은 `MarkdownBody` 로 그린다**(PR 본문은 마크다운으로 쓰인다)
- `additions` · `deletions` · `changedFiles` 가 **`null` 이 아닐 때만** 그린다
- **`review` 가 `null` 이면 리뷰 칸을 아예 만들지 않는다**
- 바뀐 파일 목록 — `status` 별 색, `+12 −3`, `renamed` 는 `old.ts → new.ts`
- **`removed` 는 누를 수 없다**(회색). 그 밖은 누르면
  `/s/:spaceId/repos/:repoId/browse?ref=<sourceBranch>&path=<path>` 로 간다
- `truncated` 면 목록 아래에 «파일이 많아 300개까지만 보여 줍니다» + GitHub 링크
- 앱바에 «GitHub 에서 열기»(`htmlUrl`)

- [ ] **Step 3: 라우트를 더한다**

`router.dart` 의 커밋 라우트 **바로 아래**에, 같은 모양으로:

```dart
      GoRoute(
        path: '/s/:spaceId/repos/:repoId/pulls',
        builder: (_, state) => PullsScreen(
          spaceId: state.pathParameters['spaceId']!,
          repoId: state.pathParameters['repoId']!,
        ),
      ),
      GoRoute(
        path: '/s/:spaceId/repos/:repoId/pulls/:number',
        builder: (_, state) => PullDetailScreen(
          spaceId: state.pathParameters['spaceId']!,
          repoId: state.pathParameters['repoId']!,
          number: int.parse(state.pathParameters['number']!),
        ),
      ),
```

- [ ] **Step 4: 위젯 테스트를 쓴다**

9-3 에서 **서버만 만들고 화면을 빠뜨린 채 완료로 보고한 일**이 있었다.
그때처럼 **화면이 없으면 실패하는 테스트**를 둔다.

`app/test/pulls_screen_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:nexus_app/domain/models/pull.dart';
import 'package:nexus_app/features/repo/pull_detail_screen.dart';

/// `repos_screen_test.dart` 와 같은 방식이다 — **override 목록이 아니라 적재
/// 함수를 받는다.** Riverpod 3 은 `Override` 를 export 하지 않아 목록에 타입을
/// 붙일 수 없다.
Widget harness(Future<PullView> Function() load) {
  return ProviderScope(
    overrides: [pullDetailProvider.overrideWith((ref, key) => load())],
    child: const MaterialApp(
      home: PullDetailScreen(spaceId: 'space-1', repoId: 'repo-1', number: 12),
    ),
  );
}

PullView view({
  PullState state = PullState.open,
  PullReviewState? review,
  int? additions,
  List<PullChangedFile> files = const [],
  bool truncated = false,
}) {
  return (
    pull: PullDetail(
      number: 12,
      title: '멘션을 파서 안으로 넣는다',
      state: state,
      review: review,
      additions: additions,
      deletions: additions == null ? null : 3,
      changedFiles: additions == null ? null : 1,
      sourceBranch: 'feat/x',
      targetBranch: 'main',
    ),
    files: files,
    truncated: truncated,
  );
}

void main() {
  testWidgets('리뷰가 null 이면 리뷰 칸을 그리지 않는다', (tester) async {
    await tester.pumpWidget(harness(() async => view()));
    await tester.pumpAndSettle();

    // 혼자 쓰는 저장소에서는 늘 비는 값이다. 빈 칸은 잡음이다.
    expect(find.text('리뷰'), findsNothing);
  });

  testWidgets('승인되면 리뷰 칸이 생긴다', (tester) async {
    await tester.pumpWidget(
      harness(() async => view(review: PullReviewState.approved)),
    );
    await tester.pumpAndSettle();

    expect(find.text('승인됨'), findsOneWidget);
  });

  testWidgets('변경량이 null 이면 그리지 않는다 — 0 으로 보이면 안 된다', (tester) async {
    await tester.pumpWidget(harness(() async => view()));
    await tester.pumpAndSettle();

    expect(find.textContaining('+'), findsNothing);
  });

  testWidgets('변경량을 알면 그린다', (tester) async {
    await tester.pumpWidget(harness(() async => view(additions: 12)));
    await tester.pumpAndSettle();

    expect(find.textContaining('+12'), findsOneWidget);
  });

  testWidgets('머지된 PR 은 머지됨 칩이 뜬다', (tester) async {
    await tester.pumpWidget(harness(() async => view(state: PullState.merged)));
    await tester.pumpAndSettle();

    expect(find.text('머지됨'), findsOneWidget);
    expect(find.text('닫힘'), findsNothing);
  });

  testWidgets('지워진 파일은 누를 수 없다', (tester) async {
    await tester.pumpWidget(harness(() async => view(files: const [
          PullChangedFile(path: 'gone.dart', status: 'removed'),
          PullChangedFile(path: 'live.dart', status: 'modified'),
        ])));
    await tester.pumpAndSettle();

    // 그 커밋 시점에 없어 열어도 404 다.
    final gone = tester.widget<ListTile>(
      find.ancestor(of: find.text('gone.dart'), matching: find.byType(ListTile)),
    );
    expect(gone.onTap, isNull);

    final live = tester.widget<ListTile>(
      find.ancestor(of: find.text('live.dart'), matching: find.byType(ListTile)),
    );
    expect(live.onTap, isNotNull);
  });

  testWidgets('truncated 면 안내가 뜬다 — 조용히 자르지 않는다', (tester) async {
    await tester.pumpWidget(harness(() async => view(truncated: true)));
    await tester.pumpAndSettle();

    expect(find.textContaining('300개'), findsOneWidget);
  });
}
```

**파일 행은 `ListTile` 로 그린다** — 위 테스트가 `onTap` 이 `null` 인지로
"누를 수 없음"을 확인하기 때문이다. 다른 위젯을 쓰면 그 확인이 성립하지 않는다.

- [ ] **Step 5: 분석 · 테스트 · 커밋**

```bash
cd app && flutter analyze && flutter test
git add app/lib app/test/pulls_screen_test.dart
git commit -m "feat(app): PR 목록과 상세 화면"
```

커밋 본문:

```
목록에 변경량을 그리지 않는다 - 서버가 주지 않는다. 리뷰가 없으면 칸을 아예
만들지 않는다. 지워진 파일은 누를 수 없다 - head 시점에 없어 열어도 404 다.
```

---

## Task 10: 진입 두 곳 — 저장소 아이콘 · 채널 메시지

**Files:**
- Modify: `app/lib/features/repo/browse_screen.dart:149-160` (앱바 actions)
- Modify: 메시지에서 `repoEventId` 를 눌렀을 때의 분기 (10-3b 가 만든 자리)

- [ ] **Step 1: 저장소 화면에 PR 아이콘**

`browse_screen.dart` 의 커밋 `IconButton` **바로 앞**에 더한다.
**`TabBar` 로 묶지 않는다** — 이 화면은 경로와 브랜치라는 자기 상태를 들고
있어 탭 전환마다 그것이 흔들린다.

```dart
          IconButton(
            tooltip: 'Pull Request',
            icon: const Icon(Icons.merge_type, size: 20),
            onPressed: () => context.push(
              '/s/${widget.spaceId}/repos/${widget.repoId}/pulls',
            ),
          ),
```

**`ref` 를 붙이지 않는다** — PR 은 브랜치에 매이지 않는다.

- [ ] **Step 2: 메시지에서 PR 로**

10-3b 가 만든 «메시지 → 커밋» 진입을 `kind` 로 가른다.

```dart
switch (view.kind) {
  case 'push':
    context.push('/s/$spaceId/repos/${view.repoId}/commits');
  case 'pr':
    context.push('/s/$spaceId/repos/${view.repoId}/pulls/${view.number}');
  case _:
    break; // other 는 열 곳이 없다
}
```

지금 코드가 `repoEventId` 유무로 누를 수 있는지 정하고 있으니 그 자리는 그대로
두고, `other` 일 때 조용히 아무것도 하지 않는다.

- [ ] **Step 3: 분석 · 테스트 · 커밋**

```bash
cd app && flutter analyze && flutter test
git add app/lib
git commit -m "feat(app): PR 로 들어가는 길 둘 - 저장소 아이콘과 채널 메시지"
```

커밋 본문:

```
TabBar 로 묶지 않았다. 탐색 화면은 경로와 브랜치라는 자기 상태를 들고 있어
탭 전환마다 그것이 흔들린다. 커밋이 이미 아이콘 → 별도 라우트다.
```

---

## Task 11: 눈으로 확인하고 문서를 갱신한다

**Files:**
- Modify: `CLAUDE.md` (§4 에 절 추가 · §5 표에 행 추가 · 알려진 빚 갱신)
- Modify: `docs/전환-계획.md` (§3.3 의 `repos/pulls/` 체크 · §6 표)
- Modify: `docs/백엔드-설계.md` (뒤집은 두 줄에 취소선과 이유)

- [ ] **Step 1: Windows 데스크톱으로 직접 본다**

```bash
cd app && flutter run -d windows --dart-define=API_BASE=http://127.0.0.1:3000
```

**개발 빌드는 시작 메뉴에 없어 자동화 도구가 창을 잡지 못한다 — 사람이 봐야
한다.** 확인할 것:

1. 저장소 → PR 아이콘 → 열린 PR 목록
2. 필터를 «닫힘»으로 → 머지된 것에 «머지됨» 칩
3. 상세 — 본문이 마크다운으로 그려짐 · 변경량 · **리뷰 칸이 없음**(혼자 쓰는
   저장소라 비는 것이 정상이다)
4. 파일을 눌러 그 브랜치 시점의 전문
5. 지워진 파일은 눌리지 않음
6. 채널의 «PR #n 를 열었습니다» 를 눌러 상세로
7. push 메시지는 여전히 커밋 목록으로

- [ ] **Step 2: 확인한 것과 확인하지 못한 것을 나눠 적는다**

CLAUDE.md §4 에 «11 PR 열람» 절을 더한다. 다른 절들과 같은 형식으로 —
**판단과 그 이유**를 쓰고, 끝에 «확인한 것» · «확인하지 못한 것»을 나눈다.
빌드가 통과한 것과 동작하는 것은 다르다(§6).

§5 표에 `| 11 | **PR 열람** — 목록 · 상세 · 바뀐 파일 · 채널 진입 | ✅ |`.

- [ ] **Step 3: 뒤집은 판단을 원본 문서에 적는다**

`docs/백엔드-설계.md` §3 의 `pull_requests` upsert 줄과 §API 의 `/diff` ·
`/reviews` 줄에 **취소선과 이유**를 단다. 코드 하이라이팅 때와 같은 방식이다 —
지우지 않는 이유는 그때 왜 그렇게 적었는지가 함께 남아야 하기 때문이다.

- [ ] **Step 4: 커밋**

```bash
git add CLAUDE.md docs/
git commit -m "docs: 11단계 PR 열람을 반영한다"
```

커밋 본문:

```
백엔드 설계의 pull_requests upsert 와 /diff · /reviews 줄에 취소선을 달고
뒤집은 이유를 함께 적었다. 지우지 않은 것은 그때 왜 그렇게 적었는지가
함께 남아야 하기 때문이다.
```
