# Nexus — 전환 계획

> 기존 "사내 통합 개발 환경" 코드에서 새 구조로 옮기기 위한 실행 목록.
> 방향은 [제품 기획서](./제품-기획.md), 설계는 [백엔드 설계](./백엔드-설계.md) · [앱 설계](./앱-설계.md).

## 0. 전제

- 커밋이 **초기 커밋 1개**뿐이고 운영 데이터가 없다. → 마이그레이션을 이어붙이지 않고 **리셋**하는 게 훨씬 싸다.
- 작업은 `main`에서 직접 하지 말고 `feat/pivot-nexus` 브랜치에서 진행한다.
- 아래 **삭제 항목은 아직 실행하지 않았다.** 승인 후 일괄 처리한다.

## 1. 기존 자산 처분

| 경로 | LOC | 처분 | 사유 |
|---|---|---|---|
| `www/` | 2,038 + 벤더 | **삭제** | Flutter가 웹을 담당. React UI는 폐기 |
| `desktop/` | 92 | **삭제** | Flutter 데스크톱으로 대체 |
| `mobile/` | — | **삭제** | Capacitor → Flutter |
| `index.html` (루트) | 1,443 | **삭제** | API 연동 이전의 죽은 목업. `www/index.html`의 구세대 |
| `design-source/` | — | **아카이브** | 사내 UI 디자인이라 새 방향과 불일치. 화면 흐름 참고용으로 `docs/archive/`로 이동 후 판단 |
| `.claude/launch.json` | — | **수정** | python http.server → Flutter 웹 개발 서버 |
| `package.json` (루트) | — | **재작성** | 스크립트 전면 교체 |
| `server/` | 2,815 | **개편** | §3 |

> **삭제 전 확인**: `www/index.html`의 화면 구성(사이드바 · 채널 목록 · 메시지 · 보드 · 권한 화면)은 Flutter UI를 짤 때 유용한 참고 자료다. 삭제 커밋 이전 리비전에 남아 있으므로 `git show`로 언제든 꺼낼 수 있다. 별도 보존이 필요하면 `docs/archive/`로 옮긴다.

## 2. 새 최상위 구조

```
nexus/
├─ app/          Flutter — 모바일 · 데스크톱 · 웹        ← 신규
├─ server/       NestJS 백엔드                          ← 개편
├─ docs/         기획 · 설계
└─ README.md
```

루트 `package.json`은 서버 개발 편의 스크립트만 남긴다. (Flutter 쪽은 `flutter` CLI를 직접 쓴다.)

## 3. 서버 개편 작업

### 3.1 삭제
- [ ] `src/attendance/` — 근태
- [ ] `src/worklogs/` — 업무일지
- [ ] `src/notices/` — 공지
- [ ] `app.module.ts`에서 위 3개 모듈 등록 해제

### 3.2 스키마 재작성
- [ ] `prisma/migrations/` 전체 삭제 후 새 init 생성
- [ ] `Space` · `SpaceMember` · `Invite` 추가
- [ ] 모든 테넌트 소속 테이블에 `spaceId` 추가 (비정규화 포함)
- [ ] `Role` 전역 enum → `SpaceMember.role`로 이동
- [ ] `Category` 추가, `Channel.categoryId` 연결
- [ ] `Message.parentId` · `replyCount` · `lastReplyAt` · `pinned` · `deletedAt` 추가
- [ ] `Reaction` · `Mention` 추가
- [ ] `File` → `Attachment` 개명, `retained` 제거, `width`/`height` 추가
- [ ] `GitlabRepo` → `Repo` (provider enum) + `RepoEvent`
- [ ] `OauthAccount` · `AiRun` 추가
- [ ] `Attendance` · `Worklog` · `Notice` 모델 제거
- [ ] `prisma/seed.ts` 재작성 — 내 계정 + 기본 스페이스 1개 + 채널 몇 개

### 3.3 신규 모듈
- [ ] `spaces/` — CRUD · 멤버 · 초대
- [ ] `SpaceGuard` — `:spaceId` 멤버십 검증, `req.spaceMember` 주입
- [ ] `categories/`
- [ ] `reactions/`
- [ ] `mentions/` — 본문 파싱 · 알림 연동 · 뱃지 카운트
- [ ] `storage/` — `minio.service.ts`를 S3 호환 인터페이스로 일반화

### 3.4 기존 모듈 이관
- [ ] 모든 컨트롤러 라우트를 `/spaces/:spaceId/…` 아래로
- [ ] 모든 서비스 메서드 시그니처에 `spaceId` 첫 인자 추가
- [ ] `channels` — 카테고리 · 읽음 마커 실제 저장
- [ ] `messages` — 스레드 · 핀 · 소프트 삭제
- [ ] `issues` — `originMessageId`(대화 → 이슈) 추가
- [ ] `gitlab/` → `repos/` 개명 + GitHub provider 추가 + 웹훅 수신
- [ ] `ai/` — 501 스텁 제거, 실제 LLM 연동 + Redis 큐
- [ ] `permissions/` — 스페이스 역할 기준으로 재작성

### 3.5 기존 분석에서 발견된 결함 (이번에 함께 수정)

| # | 문제 | 위치 | 조치 |
|---|---|---|---|
| 1 | JWT 시크릿 폴백이 모듈마다 불일치 → `JWT_SECRET` 미설정 시 REST는 뜨는데 소켓만 전부 인증 실패 | `auth.module.ts:16`, `jwt.strategy.ts:28`, `realtime.gateway.ts:61` | 시크릿 해석을 단일 설정으로 통합. **하드코딩 폴백 제거**, 미설정 시 부팅 실패 |
| 2 | 소켓 룸이 연결 시점에만 join → 채널 추가·권한 회수가 재연결 전까지 미반영 | `realtime.gateway.ts:82` | `rooms:invalidate` / `rooms:sync` 도입 |
| 3 | `read` 이벤트가 `last_read_at`을 저장하지 않고 릴레이만 함 | `realtime.gateway.ts:104` | `ChannelMember`에 실제 저장 |
| 4 | `presence:changed` · `issue:updated` 미구현 | — | Redis 프레즌스 + 이슈 서비스 emit |
| 5 | `enableCors()` 무조건 허용 | `main.ts:20` | 환경변수 화이트리스트 |
| 6 | 소켓 토큰을 쿼리스트링으로도 수용 → 액세스 로그에 토큰 잔류 | `realtime.gateway.ts:160` | 쿼리 경로 제거, `auth.token`만 |
| 7 | 테스트 · 린트 · CI 전무 | — | §5 |

## 4. Flutter 앱 신규 구축

[앱 설계 §11](./앱-설계.md)의 구현 순서를 따른다. 초기 셋업만 여기 적는다.

- [ ] `flutter create --platforms=android,ios,windows,macos,linux,web --org com.nexus app`
- [ ] 의존성 추가 (riverpod · go_router · dio · socket_io_client · freezed · drift · flutter_secure_storage …)
- [ ] `core/env.dart` — `--dart-define=API_BASE` 주입. **`localhost` 하드코딩 금지** (기존 `www/api.js:6`이 이 문제로 실기기에서 연결 불가였다)
- [ ] `analysis_options.yaml` — `flutter_lints` + 프로젝트 규칙
- [ ] 라우터 · 테마 · 반응형 셸 스캐폴드

## 5. 개발 인프라 (지금까지 없던 것)

- [ ] `server/` — ESLint + Prettier 설정, `test` 스크립트 복구, Jest 셋업
- [ ] `app/` — `flutter analyze` · `flutter test`
- [ ] `.github/workflows/ci.yml` — 서버(lint · build · test) + 앱(analyze · test) 병렬 실행
- [ ] `server/.env.example` 갱신 — `JWT_SECRET` 필수 표기, `change-me` 제거, S3 · OAuth · LLM 키 항목 추가

## 6. 실행 순서

전환은 아래 순서를 지킨다. 특히 **스페이스 도입(2단계)이 다른 모든 서버 작업의 선행 조건**이다.

| 단계 | 내용 | 산출물 |
|---|---|---|
| **1** | 기존 자산 정리 (§1 삭제 · 아카이브), 루트 구조 재편 | 빈 슬레이트 |
| **2** | 스키마 재작성 + auth 개편 + `spaces` + `SpaceGuard` | 멀티테넌시 골격 |
| **3** | channels · messages 이관 + 스레드 · 리액션 · 멘션 | 채팅 API 완성 |
| **4** | realtime 개편 (§3.5의 2·3·4번 포함) | 실시간 완성 |
| **5** | Flutter 스캐폴드 → 인증 → 셸 → 채널 → 메시지 | **처음으로 눈에 보이는 앱** |
| **6** | drift 캐시 + 소켓 연동 + catch-up | 실사용 가능 (Phase 0 달성) |
| **7** | attachments · issues | |
| **8** | repos (웹훅) · ai | 차별점 완성 |
| **9** | 푸시 · 트레이 · 딥링크 · CI | Phase 1 준비 완료 |

**5단계에서 처음으로 실제 앱을 보게 된다.** 그 전까지는 서버만 쌓이므로, 중간에 확인이 필요하면 4단계 이후 서버 API를 먼저 검증하는 게 좋다.
